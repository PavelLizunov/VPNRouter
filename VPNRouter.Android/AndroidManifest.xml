<?xml version="1.0" encoding="utf-8"?>
<!--
  v3.0 Android port — Phase 0 scaffold AndroidManifest.

  Minimal: only declares permissions + VpnService registration.
  - The Avalonia MainActivity is auto-registered from `[Activity]`
    attribute on MainActivity.cs (don't list here).
  - Icons / themes default to system until we add resources/ in
    Phase 1.

  Reference: plans/vpnrouter-android-research.md §"Required Android
  manifest bits".
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
                     tools:ignore="QueryAllPackagesPermission" />

    <!--
      VpnService class — added in Phase 1 as a Kotlin/Java native shim
      extending android.net.VpnService. Once we have the class file,
      uncomment:

      <service android:name=".VpnRouterService"
               android:permission="android.permission.BIND_VPN_SERVICE"
               android:foregroundServiceType="systemExempted"
               android:exported="false">
          <intent-filter>
              <action android:name="android.net.VpnService" />
          </intent-filter>
          <meta-data
              android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"
              android:value="true" />
      </service>
    -->

    <application android:label="VPNRouter"
                 android:supportsRtl="true">
        <!-- Avalonia MainActivity auto-generated from [Activity] attribute. -->
    </application>

</manifest>
