# PR207 preservation exception

Closed PR207 branch `sentinel/scrub-secrets-prefixed-token-params-9502242564205557734`, tip `9193069593c43759f812be15e3669aea99f49d88`; closure says superseded by #219. Full merge-base diff is three files, not just redaction.

Redaction intent and three test keys (client_secret, refresh_token, auth_token) are present in accepted main2689ee77. Current query regex has the same separator-qualified prefix and additional keys; IgnoreCase option replaces inline flag. However the branch also changes RemoteVersionChecker's catch to `catch (OperationCanceledException) when (ct.IsCancellationRequested)`. Current main110-113 rethrows every OperationCanceledException. Thus internal HTTP timeout versus caller cancellation can take different fallback paths; this unrelated change is not preserved by the redaction replacement alone.

History follow-up: git log --all -S for the cancellation filter finds only this original commit; #219 description/comments discuss redaction only, not rejection of timeout fallback. No RemoteVersionCheckerTests.cs exists; repository-wide test references found only SubscriptionUrlRedactionTests.RemoteVersionChecker_LogsDoNotContainToken, not a cancellation-specific fixture. This is bounded search evidence, not proof no indirect coverage exists. Disposition: retain branch; intended timeout policy needs an explicit scoped decision rather than inferred rejection. No demonstrated runtime failure, product edit or automatic adoption claimed. Closure alone is insufficient proof of full-branch redundancy. No deletion performed.
