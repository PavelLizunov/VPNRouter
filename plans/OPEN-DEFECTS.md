# Open defect ledger

Canonical open-defect ledger read by `tools/check-open-p0.ps1` (the cut-stable
P0 gate, audit item 7, 2026-06-25). The `bug-hunt` skill appends survivors to
the **## Open** section below; the cut-stable gate (`cut-stable` skill pre-flight
condition 6.5) **BLOCKS a stable cut** while any `- [ ]` P0/P1 remains open here,
unless `tools/check-open-p0.ps1 -Waive '<reason>'` is run for that specific cut.

This file exists because a P0 found by the adversarial bug-hunt, written down and
deferred, reached v2.44.0/.1 stable and bit a user (auto-failover teardown, diag
20260624-235243) — nothing connected the deferred-defect record to the cut gate.

**Resolve** an entry: change `- [ ]` to `- [x]` and append `RESOLVED vX.Y.Z`.
Only the `## Open` section is gated; `## Resolved` is history. Keep entries to one
line: `- [ ] **P0** — <symptom> — <file:line or plan ref> — <target version>`.

## Open

- [x] **P2 PROTOCOL-MATRIX GEOGRAPHY ATTRIBUTION GAP / RESOLVED + LIVE VERIFIED 2026-08-09** — the completed 20-row WINBRAT matrix originally persisted only safe `ProtocolClass` + per-class ordinal coordinates and could not answer the owner's country-level question. `SelectProtocol` now derives only canonical `RegionCode`/`Country` from a visible Text descendant inside WINBRAT and discards the raw label; static contracts continue to forbid the endpoint-bearing ListItem name, raw text, endpoints, ports, keys, URLs and runtime IDs. The first local contract correctly rejected an explanatory forbidden-model reference; the first live attempt failed closed on unavailable `SubList`, after which the fixed caller explicitly opened/asserted Advanced/Subscribe. The final survey resolved 20/20 rows, restored Hysteria2 ordinal 0 plus Simple mode, and ended at GUI=1/core=0/TUN absent/Direct. Affected cohorts: Latvia/XHTTP ordinal 0 (1 ReplyGap) and Germany/AWG ordinal 1 (1 ReplyGap + 1 CookieFailure); neighboring rows passed, so this is not country-wide attribution. Source: owner report, `artifacts/brat-protocol-matrix/20260808-230027-matrix.jsonl`, live survey 2026-08-09; implementation: draft PR #125; disposition: **RESOLVED / LIVE VERIFIED**, test-evidence usability only, no product defect.

- [x] **P2 TEST-HARNESS ORCHESTRATION GAP / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — per-row `ProtocolLoad` is safe, but there is no outer fixed manifest, progress checkpoint or final restoration for the requested 20×3 run. An inline/background loop could leave the last row selected and provides no resumable sanitized ledger after a local interruption. Add a thin local-only coordinator that invokes only `brat-stability`/`brat-verify`, checkpoints class/ordinal/status, stops on infrastructure failures, continues structured network measurements, and restores the original HY2 ordinal 0 in a final cleanup. Source: Codex pre-run audit; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, draft PR #124.
- [x] **P2 TEST-HARNESS SAFETY GUARD / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — the first ordinal-selector hardening derived the owned sing-box root from the GUI path, but the shipped GUI is under `Program Files` while the canonical owned core is `C:\ProgramData\VPNRouter\bin\sing-box.exe`; a stopping core could therefore survive after TUN disappearance and escape the selector guard. Pin the same canonical core path used by the state verifier before the exhaustive run. Source: Codex pre-run review; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, draft PR #124.
- [x] **P2 TEST-HARNESS FAILURE CLASSIFICATION / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — `ProtocolLoad` stopped on `BLOCKED` but would continue after payload hash/missing/timeout/non-zero/invalid-output/internal failures returned as structured `FAIL`. Those are harness-integrity failures, not network measurements, and must stop the matrix; only `Completed`, `ReplyGap`, `CookieFailure` and `NetworkFailure` may become per-cell measurements. Source: Codex pre-run review; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, draft PR #124.
- [x] **P2 TEST-HARNESS MODE GAP / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — the first `ProtocolLoad` shakedown failed before connection because `brat-stability.ps1` only recognizes Simple-mode `Connect`/`Disconnect`, while safe protocol-row selection necessarily leaves the UI on Advanced Subscribe where the CTA names are `Start VPN`/`Stop VPN`. Cleanup also reported failure even though the state action proved one GUI, zero core, absent TUN and Direct route. Add both fixed localized CTA families and re-run the full cold shakedown before the matrix. Source: live WINBRAT evidence `artifacts/brat-stability/20260808-224004-ProtocolLoad.jsonl`; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, draft PR #124.
- [x] **P2 TEST-EVIDENCE BLOCKER / RESOLVED IN WORKTREE + LIVE VERIFIED 2026-08-09** — `SelectProtocol` accepts protocol ordinals beyond zero, but Avalonia recycles virtualized `ListItem` containers. The first hardening replaced `PageDown` with bounded `HOME`/`DOWN`, yet still treated `GetRuntimeId()` as a logical-row identity. The exhaustive matrix passed five cells, then stopped before loading VLESS WebSocket ordinal 1; an immediate clean-state reproduction failed with `Protocol selection failed and the original row restoration did not stick`. The direct observer was healthy and final cleanup passed, so this was test-infrastructure failure, not VPN evidence. The fixed selector now traverses exactly the 20-row WINBRAT fixture, classifies only allowlisted non-secret protocol labels, requires the complete 4/3/4/4/4/1 composition, then replays the verified absolute coordinate; it never reads row names or runtime IDs. WINBRAT selected WebSocket ordinal 1 as absolute ordinal 10, rejected absent TUIC with safe empty cleanup, then selected Hysteria2 ordinal 0 and remained disconnected with zero core/TUN. Focused tooling tests passed 29/29. Source: `artifacts/brat-protocol-matrix/20260808-230027-matrix.jsonl`, `artifacts/brat-stability/20260809-005314-ProtocolLoad.jsonl`, `tools/brat-verify.ps1`; implementation reference pending, draft PR #124.
- [ ] **P2 A/B FIXTURE PROVISIONING GAP / MEASUREMENT-GATED 2026-08-09** — the pinned official AmneziaWG 2.0.2 client is now installed on WINBRAT through the fixed hash/signature-checked `DO_NOT_LAUNCH` flow, and a separate fixed official-client runner provides a local watchdog, Tailscale/route/adapter attribution and exact cleanup. Read-only Control and Target preflights both return only `FixtureMissing`; neither tunnel nor workload has run. A valid comparison still needs operator-provisioned matched opaque AWG Control/Target fixtures and their protected Tailscale-safe markers; HY2 still needs its separate fixture and non-CGNAT runner gate. Hiddify or another sing-box wrapper is not an independent core A/B. Source: delegated readiness audit, `tools/verify-official-alt-clients.ps1`, live sanitized preflights and `tools/brat-official-{ab,awg-remote}.ps1`; status: **CONFIRMED / BLOCKS LIVE A/B UNTIL SAFE PROVISIONING**, no product defect.
- [ ] **P2 XHTTP A/B CLIENT GAP / MEASUREMENT-GATED 2026-08-09** — the prepared official AmneziaWG and Hysteria clients cannot compare VLESS XHTTP. Official Xray-core supports Windows TUN through Wintun and is an independent XHTTP implementation, but the current upstream releases are prereleases and a primary upstream report states that the same XHTTP+Reality configuration worked on v26.6.27 and stopped connecting on v26.7.11; the latest v26.7.28 release page does not expose enough changelog evidence to prove that regression fixed. Do not select an arbitrary latest Xray build or interpret a client-start failure as an A/B result. A safe XHTTP bracket requires a vetted exact Xray version/hash, bundled matching `wintun.dll`, an opaque matched fixture, the same management-route watchdog/attribution gates and a direct control. Sources: official [Xray Windows TUN README](https://github.com/XTLS/Xray-core/blob/main/proxy/tun/README.md), [official releases](https://github.com/XTLS/Xray-core/releases), [upstream issue #6482](https://github.com/XTLS/Xray-core/issues/6482); status: **CONFIRMED TOOLING/BASELINE GAP / DEFERRED UNTIL VERSION AND FIXTURE ARE PROVEN**, no VPNRouter product defect.
- [x] **P2 A/B ATTRIBUTION CONTRACT GAPS / RESOLVED IN WORKTREE 2026-08-09** — the fixed coordinator now performs the same three-cold-repeat sampling, requires three Control PASS cycles before three Target cycles, classifies watchdog/transport/cleanup uncertainty as `ABORTED`, rechecks fixed hashes/signatures, timestamps every cycle and stops after dirty cleanup. Route ownership plus positive adapter delta above a quiet window is required without inventing a protocol-independent byte ratio. A close-time VPNRouter re-bracket remains an evidence step after official Target execution, not missing harness safety. Focused contracts pass 12/12; no live official tunnel has run. Exact DeepSeek zero-tool review timed out without a verdict; Codex review is authoritative. Status: **RESOLVED HARNESS CONTRACT**, test tooling only.
- [ ] **P2 OFFICIAL AWG CONTROL-FIXTURE CONTRACT GAP / HARNESS RESOLVED, PROVISIONING BLOCKED 2026-08-09** — the plan originally named only the unhealthy AWG ordinal-1 target. The runner now accepts exactly two fixed identities: healthy ordinal-0 `Control` and ordinal-1 `Target`; it samples both three times and blocks Target unless all Control cycles pass. Both remain unreadable/unhashable to Codex. Live read-only preflights prove both fixtures are still absent, so the external provisioning portion remains open. Status: **BLOCKS LIVE A/B UNTIL TWO OPAQUE FIXTURES EXIST**, no product defect.
- [ ] **P1 OFFICIAL AWG `/0` KILL-SWITCH MANAGEMENT BLOCKER / CONFIRMED 2026-08-09** — official AmneziaWG for Windows enables restrictive WFP rules when a single peer contains an `AllowedIPs` `/0`; those rules block every non-tunnel packet except a narrow built-in allowlist, so the existing more-specific Tailscale route alone cannot guarantee that WinRM survives. The official workaround is split defaults `0.0.0.0/1` plus `128.0.0.0/1` and, when IPv6 is routed, `::/1` plus `8000::/1`. Because the verifier is forbidden to inspect opaque fixtures, each operator-provisioned Control/Target fixture needs a separately protected fixed-name Tailscale-safe attestation marker before launch, followed by runtime management-route validation and the local watchdog. Missing marker is `BLOCKED`; transport loss/watchdog firing is `ABORTED`, never a network failure. Source: official [AmneziaWG Windows network quirks](https://github.com/amnezia-vpn/amneziawg-windows-client/blob/2.0.2/docs/netquirk.md#firewall-considerations-for-0-allowed-ips); status: **CONFIRMED / BLOCKS LIVE PHASE B UNTIL OPERATOR ATTESTATION**, test-infrastructure safety; no product defect.
- [x] **P2 OFFICIAL A/B EVIDENCE-BINDING DEFECTS / RESOLVED IN WORKTREE 2026-08-09** — verifier and coordinator now bind every result to exact client/operation/profile, accumulate watchdog firing monotonically, retain watchdog protection until both fixed tunnels are absent, and reject mismatched status/lifecycle combinations. Focused contracts pass and read-only WINBRAT preflights preserve clean state. Source: Codex adversarial review and `tools/brat-official-{ab,awg-remote}.ps1` / `tools/brat-verify.ps1`; status: **RESOLVED HARNESS-ONLY**, no product defect.
- [x] **P2 OFFICIAL A/B TAILSCALE PREFLIGHT FALSE BLOCK / RESOLVED + LIVE VERIFIED 2026-08-09** — the stale exact-`/10` assumption was replaced with an opaque proof of running Tailscale service/adapter, the active established WinRM peer in CGNAT and `Find-NetRoute` for that peer through Tailscale. Neither peer, adapter, route nor address is returned. Both current Control and Target preflights preserve the management channel and stop only at `FixtureMissing`. Source: live sanitized WINBRAT preflights and `Test-ManagementRoute`; status: **RESOLVED HARNESS-ONLY**, no product defect.
- [x] **P1/P2 OFFICIAL A/B PRE-LIVE SAFETY AND EVIDENCE BLOCKERS / RESOLVED IN WORKTREE + PREFLIGHT VERIFIED 2026-08-09** — adversarial review found seven fail-closed gaps before any tunnel launch: running SYSTEM watchdog teardown race; reparse-unsafe recursive deletion; incomplete ACL rights/Deny validation; acceptance of a present Down `VPNRouter-TUN`; corruption/unknown replies misclassified as network failure; JSON Boolean string type confusion; and acceptance of PASS/FAIL with missing/partial aggregate metrics. The helper now stops and waits a running watchdog before unregister/root deletion, refuses any root/descendant reparse point, requires protected SYSTEM/test-admin FullControl on non-secret containers/markers with no Deny/foreign ACE, requires an absent VPNRouter TUN, classifies integrity evidence as `ABORTED`, requires runtime Boolean types and the complete fixed metric schema with counter/percentile invariants. Parsers pass, focused contracts pass 12/12 and both live read-only preflights fail closed at absent fixtures. Exact DeepSeek zero-tool review timed out without verdict; permissions were not relaxed. Status: **RESOLVED TEST-HARNESS BLOCKERS**, no product defect.
- [x] **P2 OFFICIAL AWG DPAPI ACL COMPATIBILITY DEFECT / RESOLVED IN WORKTREE 2026-08-09** — the first hardened fixture check required `SYSTEM` and the current administrator SID to hold `FullControl`, but pinned official AmneziaWG source creates `.conf.dpapi` with protected SDDL `O:SYG:SYD:PAI(A;;FA;;;SY)(A;;SD;;;BA)`: owner/group SYSTEM, SYSTEM FullControl and builtin Administrators Delete only. The checker now distinguishes non-secret container/marker ACLs from the secret fixture: it requires the official owner/group and least-privilege file rights, permits only optional `ReadPermissions`/`Synchronize` needed to inspect ACL metadata, rejects every data-read/write right, Deny/foreign/inherited ACE and reparse point, and never reads fixture bytes. Source: official [`filewriter_windows.go`](https://github.com/amnezia-vpn/amneziawg-windows/blob/v0.1.9/conf/filewriter_windows.go), AmneziaWG 2.0.2 [`enterprise.md`](https://github.com/amnezia-vpn/amneziawg-windows-client/blob/2.0.2/docs/enterprise.md), `tools/brat-official-awg-remote.ps1` and focused contracts; status: **RESOLVED HARNESS-ONLY**, no product defect.
- [ ] **P1 HYSTERIA A/B MANAGEMENT-ROUTE SAFETY BLOCKER / CONFIRMED 2026-08-09** — official Hysteria `app/v2.12.0` defaults its Windows TUN IPv4 address to `100.100.100.101/30`, which lies inside Tailscale's `100.64.0.0/10` management space used to reach WINBRAT. Starting an opaque fixture with that default can collide with the control plane. The fixture must explicitly use a non-CGNAT test address and exclude `100.64.0.0/10`; the runner must verify the created adapter address is outside that prefix without reading config content, and a local watchdog remains mandatory. No Hysteria alternative-client launch is permitted before these gates exist. Source: official [`app/cmd/client.go` at `app/v2.12.0`](https://github.com/apernet/hysteria/blob/app/v2.12.0/app/cmd/client.go#L1152-L1232); status: **CONFIRMED / BLOCKS PHASE B HY2**, test-infrastructure safety, not a VPNRouter defect.
- [x] **P2 LOAD ATTRIBUTION GAP / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — a fixed low-rate non-WINBRAT observer ran simultaneously with the protocol matrix against the same owned UDP endpoint without launching or mutating VPNRouter locally. It accepted no target/rate/profile input and emitted only authenticated aggregate metrics. The planned observer completed 84/84 five-minute cycles and enabled time-correlated underlay-versus-tunnel comparisons; a later replacement failed closed when its route was no longer independent. Evidence: `artifacts/direct-observer/20260808-232206/observer.jsonl` / `plans/phase2-winbrat-protocol-load-matrix-2026-08-08.md`; test tooling only, no product defect.

- [ ] **P2 CONFIRMED / MEASUREMENT-GATED 2026-08-08; RECONFIRMED 2026-08-09** — the 30-minute owned-endpoint GameUdp baseline from the dev host completed 36,366 sends with 1,739 losses (4.7819%), concentrated in runs 1-3 (882/604/249) while runs 4-6 lost 0/0/4. During the later independent direct observer, cycle 37 also degraded outside VPNRouter to 6,012/6,061 replies (49 lost, 0.808%) and a 2,507.4 ms maximum acknowledged gap, while the overlapping tunneled XHTTP repeat passed 6,060/6,060 with a 76.7 ms maximum gap. A later paired Hysteria2 interval supplied stronger common-path evidence: the tunneled run lost 45/6,061 with a 2,302.9 ms gap while the simultaneous direct observer lost 44/6,061 with a 2,246.8 ms gap; VPNRouter retained one core, an Up TUN and Tunnel routing. Conversely, during a clean Hysteria2 6,061/6,061 run the direct observer independently produced consecutive 58/55-packet loss intervals with 2,907.3/2,792.7 ms gaps. There were no duplicate, corruption or unknown replies. This proves the observer/owned-endpoint path can independently produce near-threshold UDP degradation, can also degrade almost identically across tunneled and direct paths, and may diverge sharply from a healthy tunnel; isolated incidents therefore cannot be assigned to VPNRouter alone. It still does not prove a LoadTarget defect and may be the public hairpin/NAT path. Preserve paired timing and obtain server receive/reply counters or another non-hairpin observer before setting any gate — `artifacts/brat-loadtest/20260808-182139-30m-endpoint-baseline.json` / `artifacts/direct-observer/20260808-232206/observer.jsonl` / `plans/phase2-winbrat-load-test-mvp-2026-08-08.md` — external-path attribution required
- [ ] **P2 CONFIRMED / TOOLING-STRICT / MEASUREMENT-GATED 2026-08-08** — the 30-minute WINBRAT soak kept one core and TUN Up with 81/83 healthy HTTP+UDP samples, two isolated non-consecutive UDP timeouts, a passing final 64/512/1200/1392 boundary probe, no crash/restart/FATAL and clean disconnect. One sanitized unclassified error-level lifecycle event made the harness return FAIL by design. No active product disconnect incident is confirmed; classify that event through a new redacted diagnostic captured for the same window rather than weakening the fail-closed rule — `artifacts/brat-stability/20260808-182140-Soak.jsonl` / `plans/phase2-winbrat-load-test-mvp-2026-08-08.md` — redacted attribution required
- [x] **P1 RESOLVED 2026-08-08** — the owned GameUdp endpoint and fixed client both used UDP `443`, but live ingress already owns it for Caddy HTTP/3. The shared fixed contract now uses dedicated UDP `19000`; exact source built in isolated LXC 105 and passed 17 focused tests plus local authenticated-cookie exchange without disturbing QUIC — `VPNRouter.Tools/LoadTest.Protocol/LoadTestProtocol.cs` — draft PR #124

### WINBRAT operator-owned load-test MVP — 2026-08-08

- [x] **P2 HISTORICAL SELECTOR FIX / SUPERSEDED 2026-08-09** — the first Home/PageDown restore fix covered only an existing row and keyboard mode. Its later runtime-ID restoration was invalidated by live Avalonia container recycling and is superseded by the fixed 20-row, full-composition selector above. The retained safety contract is fail-closed empty cleanup followed by an explicit fixed-coordinate restore; endpoint-bearing Names and arbitrary caller-supplied keyboard input remain forbidden — `tools/brat-verify.ps1` / `WinbratLoadTestMvpTests.cs`
- [ ] **P2 LIVE UDP INCIDENT / CROSS-PROTOCOL PATH CANDIDATE / MEASUREMENT-GATED 2026-08-08** — Hysteria2-category interval 1 completed 6,061/6,061, but interval 2 stopped with `ReplyGap`: 3,079 sent, 2,825 received, 254 outstanding/lost (8.25%) and 1,283.1 ms p99. A contemporaneous independent dev-host control against the same owned endpoint completed 6,035/6,061 with 26 losses (0.43%) and 163.1 ms maximum completed acknowledged gap. AWG then passed a five-minute shakedown plus five consecutive load intervals with only two losses across 30,305 sends, but its sixth interval also stopped with `ReplyGap`: 5,439 sent, 5,325 received and 114 outstanding/lost (2.10%). The reported 1,052.5/2,648.0 ms values are the last completed acknowledged gaps; each `ReplyGap` proves a terminal no-reply window of at least the fixed three-second threshold. During both protocol events TUN attribution remained true; immediately afterward the app still had one core and an Up TUN, HTTPS and UDP 64/512/1200/1392 passed, and lifecycle contained zero errors/FATAL/restart/failover. This refutes a Hysteria2-only or MTU-threshold explanation and shows why blind AWG switching is not a guaranteed recovery, but it does not identify VPNRouter, both provider paths, the underlay, the owned endpoint, or early-stop accounting as owner. Correlate server receive/reply/drop counters and repeat from another underlay before changing product failover policy — `artifacts/brat-loadtest/20260808-201137-GameUdp.json` and `20260808-204746` through the failed sixth AWG interval / `plans/phase2-winbrat-protocol-load-matrix-2026-08-08.md`
- [ ] **P2 LIVE UDP PATH DIVERGENCE / MEASUREMENT-GATED 2026-08-09** — in the resumed exhaustive matrix, VLESS WebSocket ordinal 1 repeat 2 completed without disconnect but lost 13/6,061 packets (0.2145%) and reached a 2,579.7 ms maximum acknowledged gap, just below the fixed three-second `ReplyGap` boundary. The overlapping independent direct observer lost 1/6,061 and reached only 133.7 ms; VPNRouter still had one core, Up TUN and Tunnel route afterward, with zero lifecycle errors/FATAL/restart. This is stronger evidence of a transient tunneled/provider-path divergence than ordinary endpoint noise, but one non-terminal interval cannot distinguish local XUDP-over-WebSocket/TCP, access network, provider node or server egress. Preserve the third repeat and later same-class rows; only escalate on recurrence or an A/B bracket with the same opaque configuration — `artifacts/brat-stability/20260809-010909-ProtocolLoad.jsonl` / `artifacts/direct-observer/20260808-232206/observer.jsonl`; no product change authorized.
- [ ] **P2 LIVE XHTTP UDP REPLY GAP / MEASUREMENT-GATED 2026-08-09** — VLESS XHTTP ordinal 0 repeat 1 reached the fixed terminal `ReplyGap` threshold after 1,680 sends and 1,619 authenticated replies, leaving 61 outstanding/lost; duplicate, reorder, corruption and unknown-reply counts were zero. Full-Tunnel route and TUN-byte correlation were both proven, and immediately after the gap VPNRouter still had one GUI, one owned core, an Up TUN and Tunnel route; lifecycle reported zero errors/FATAL/unknown events and `StayedConnected=true`. Repeats 2 and 3 then passed clean cold sessions: 6,060/6,061 and 6,061/6,061 replies, maximum acknowledged gaps 127.9/98.1 ms. The next XHTTP row also passed all three cold repeats with 18,181/18,182 replies, a 102.4 ms worst gap and no lifecycle error, while its direct observer independently reached 49/6,061 loss and a 2,507.4 ms gap in one interval. The original event remains isolated to the tunneled XHTTP/provider path at that time, not an application disconnect, but non-recurrence across five subsequent XHTTP repeats plus independent observer noise argues against a deterministic XHTTP product defect. Status: **CONFIRMED MEASUREMENT / AWAITING REMAINING XHTTP ROWS AND MATCHED A/B; no product change authorized** — `artifacts/brat-stability/20260809-015546-ProtocolLoad.jsonl` / `artifacts/brat-stability/20260809-021523-ProtocolLoad.jsonl` / `artifacts/direct-observer/20260808-232206/observer.jsonl`.
- [ ] **P2 LIVE XHTTP ROW-SPECIFIC UDP DEGRADATION / MEASUREMENT-GATED 2026-08-09** — VLESS XHTTP ordinal 2 reproduced a similar non-terminal degradation in its first two cold repeats: 6,023/6,061 and 6,025/6,061 replies, 38/36 lost (0.627%/0.594%) and maximum acknowledged gaps 1,949.9/1,843.6 ms. Both sessions proved Full Tunnel, TUN correlation and Tunnel routing; the app retained one core and Up TUN afterward, and lifecycle was clean. The two direct-observer intervals overlapping repeat 2 completed 12,122/12,122 with a 135.9 ms worst gap, isolating that repeat from endpoint-wide noise to the tunneled/selected-provider path. Repeat 3 then passed 6,061/6,061 with an 80.0 ms gap, so the degradation is intermittent rather than deterministic. This row is materially worse than XHTTP ordinal 1, but it remains below the fixed three-second failure boundary and still cannot distinguish the selected provider node/egress from XHTTP/XUDP integration. Do not generalize to the family or change product routing without a matched A/B — `artifacts/brat-stability/20260809-023818-ProtocolLoad.jsonl` / `artifacts/direct-observer/20260808-232206/observer.jsonl`; status: **CONFIRMED MEASUREMENT / MATCHED A/B PRIORITY**, no product change authorized.
- [ ] **P2 LIVE HYSTERIA2 ROW-SPECIFIC UDP DEGRADATION / MEASUREMENT-GATED 2026-08-09** — Hysteria2 ordinal 2 repeat 2 completed without disconnect but returned 6,027/6,061 replies, losing 34 (0.561%) and reaching a 1,759.0 ms maximum acknowledged gap. Full Tunnel, TUN correlation and Tunnel routing were proven; VPNRouter retained one core and an Up TUN, lifecycle had zero error/FATAL/unknown events, and clean disconnect succeeded. The simultaneously overlapping direct-observer interval completed 6,061/6,061 with a 106.9 ms worst gap, isolating this interval from common endpoint/observer-path degradation to the tunneled/selected-provider path. Repeats 1 and 3 were loss-free with 79.5/77.6 ms gaps, so the event did not recur in the immediate bracket. This is not an application disconnect and cannot yet distinguish the HY2 client path, selected proxy/provider path or UDP egress. Status: **CONFIRMED INTERMITTENT MEASUREMENT / MATCHED OFFICIAL-CLIENT A/B PRIORITY**, no product change authorized — `artifacts/brat-stability/20260809-041101-ProtocolLoad.jsonl` / `artifacts/direct-observer/20260808-232206/observer.jsonl`.
- [ ] **P2 LIVE AMNEZIAWG ROW-SPECIFIC UDP FAILURES / MEASUREMENT-GATED 2026-08-09** — AmneziaWG ordinal 1 failed two of three cold runs. Repeat 2 reached the fixed terminal `ReplyGap` after 1,597 sends and 1,535 authenticated replies, leaving 62 outstanding/lost; duplicate, reorder, corruption and unknown counts were zero. Repeat 3 then failed the initial authenticated UDP-cookie request before sending workload traffic (`CookieFailure`, 0/0), proving a second consecutive network-liveness failure on the same selected row. Full Tunnel, TUN correlation and Tunnel routing were proven both times, and immediately afterward VPNRouter still had one GUI, one owned core, an Up TUN and Tunnel route; lifecycle had zero error/FATAL/unknown events and `StayedConnected=true`, followed by clean disconnect. The observer overlapping repeat 2 completed 6,062/6,062 with a 92.0 ms worst gap, isolating that ReplyGap to the tunneled/selected-provider path. The broader observer interval overlapping repeat 3 also degraded by 49/6,061 with a 2,497.9 ms gap, so the CookieFailure coincided with real underlay/endpoint-path noise and is weaker product-attribution evidence. Repeat 1 passed 6,060/6,061 with a 153.3 ms gap. Neighbor ordinals 0, 2 and 3 passed all nine runs; ordinal 2 had one 42-packet/519.2 ms degraded interval and ordinal 3 one 39-packet/988.3 ms interval, but neither reproduced a controlled failure. This makes ordinal 1 a material row-level outlier rather than evidence that every AWG row fails. The row has reproducible liveness failures from at least two attribution patterns and the study now contains a second AWG ReplyGap, refuting blind AWG switching as guaranteed recovery; it still cannot distinguish the selected AWG server/provider path from VPNRouter/sing-box AWG integration. Status: **CONFIRMED REPRODUCIBLE MEASUREMENT / TOP MATCHED OFFICIAL AMNEZIAWG A/B PRIORITY**, no product change authorized — `artifacts/brat-stability/20260809-051923-ProtocolLoad.jsonl` / `artifacts/brat-stability/20260809-053334-ProtocolLoad.jsonl` / `artifacts/brat-stability/20260809-055613-ProtocolLoad.jsonl` / `artifacts/direct-observer/20260808-232206/observer.jsonl`.
- [x] **P1 RESOLVED IN DRAFT PR #124 / LIVE VERIFIED 2026-08-08** — the second and third five-minute Hysteria2-category shakedowns returned generic `PayloadFailed` with no metrics although VPNRouter stayed healthy. The payload now emits a fixed status plus aggregate snapshot for every controlled exit; the verifier drains redirected output, parses valid safe JSON even for non-zero exit, and otherwise distinguishes timeout, non-zero exit, missing, empty and invalid output without reading raw stderr. Subsequent HY2 and AWG PASS/`ReplyGap` runs preserved complete aggregate metrics — `VPNRouter.Tools/WinbratLoadGen/Program.cs` / `GameUdpMetrics.cs` / `tools/brat-verify.ps1`
- [x] **P1 RESOLVED IN DRAFT PR #124 / LIVE VERIFIED 2026-08-08** — the first real WINBRAT GameUdp failure result lacked `TunCorrelation`, and strict-mode reconstruction replaced the sanitized failure with a controller exception. Success/failure reconstruction is now schema-symmetric with safe defaults; later controlled failures returned `ReplyGap` plus metrics rather than a controller exception — `tools/brat-verify.ps1`
- [x] **P2 TOOLING GAP RESOLVED IN DRAFT PR #124 / LIVE VERIFIED 2026-08-08** — the verifier now selects a subscription category only while disconnected, only within `SubList`, and only by fixed non-secret protocol labels; no endpoint-bearing row Name is read or returned. Avalonia exposed neither standard scroll pattern on the virtualized list nor its scrollbar, so a bounded fixed Home/PageDown fallback on a materialized row was required. HY2 ordinal 0, AWG ordinal 0 and the original HY2 restoration all succeeded on WINBRAT — `tools/brat-verify.ps1` / `plans/phase2-winbrat-protocol-load-matrix-2026-08-08.md`
- [x] **P2 GAMEUDP/BROWSER LIVE LOAD PATH / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — the operator-owned endpoint, exact source-built GameUdp payload and Full-Tunnel BrowserBurst payload are approved and live verified. Both require fixed-route `Tunnel`, positive TUN-byte correlation, one owned core and exact archive hashes; BrowserBurst additionally requires the exact browser tree, strict fetch/WebSocket counters and route stability. Mixed remains `MeasurementGated` because it adds attribution complexity without a demonstrated simultaneous-failure symptom — `plans/phase2-winbrat-load-test-mvp-2026-08-08.md` / `tools/brat-verify.ps1` / `artifacts/brat-stability/20260809-092035-BrowserLoad.jsonl`; no product defect.
- [x] **P2 BROWSER ATTRIBUTION AND CLEANUP CONTRACT / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — Full-Tunnel BrowserBurst now uses one fixed no-argument payload/page, a deterministic pinned browser, an isolated profile, exact process-tree proof, positive TUN-byte delta, route checks before/after, separate fetch/WebSocket progress and path-validated cleanup. The initially proposed TCP-socket gate was rejected after live evidence showed it is invalid/redundant behind a transparent Full-Tunnel TUN; split mode remains blocked. Three cold ten-minute runs passed and final cleanup was clean. Mixed remains deliberately deferred. Source: Codex source/live review; exact zero-tool DeepSeek attempts timed out or failed closed without findings — `artifacts/brat-stability/20260809-092035-BrowserLoad.jsonl`; no product defect.
- [x] **P1 RESOLVED IN WORKTREE 2026-08-09** — final Codex review of the new isolated BrowserProbe payload found four pre-live defects: a five-second Edge exit timeout was not itself marked as cleanup failure if the profile happened to delete; no machine-wide single-run guard prevented duplicate fixed browser load; progress gaps used wall-clock `UtcNow`, so a Windows time correction could create a false stall/regression; and every lifecycle, including `InputRejected`, exited with process code 0. The payload now uses a global named binary semaphore, monotonic elapsed time, explicit fail-closed process-exit cleanup and non-zero exit for every lifecycle except `Completed`; focused browser/stability tests are 46/46, rejected input produces one sanitized JSON record and exit code 1, and the fixed source-build archive contains one executable with a matching SHA-256 sidecar. Source: Codex source review and local execution of `VPNRouter.Tools/WinbratBrowserProbe`; implementation: `VPNRouter.Tools/WinbratBrowserProbe/BrowserProbe.cs`, `Program.cs`, `VPNRouter.Tests/WinbratBrowserProbeTests.cs`, `tools/build-winbrat-browser-probe-payload.ps1`; commit reference pending; the final live BrowserLoad run passed 3/3 with clean teardown.
- [x] **P1 BROWSER LIVE-GATE DEFECTS / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — the pre-live BrowserBurst review found that the current verifier can pass after only one fetch burst and four WebSocket replies, the payload's 10-minute poll deadline races the page's own 10-minute completion timer, the Edge proof does not yet bind a spawned descendant socket to the fixed owned endpoint, and the isolated profile root sits outside the verifier-owned cleanup root. This can produce a false PASS or leave a browser profile/process after a run. Require fixed schedule-derived minimum counts plus per-channel progress gaps, an 11-minute poll budget, an opaque boolean descendant-socket proof for the fixed endpoint, process exit-code validation, and path-validated cleanup owned by the verifier before any live WINBRAT BrowserBurst. Source: Codex pre-live source review; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, test tooling only, no product defect.
- [x] **P2 BROWSER PREFLIGHT DIAGNOSTIC COLLAPSE / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — the first BrowserLoad attempt connected successfully with Full Tunnel and the fixed endpoint route on TUN, then stopped before payload launch as `MeasurementGated`; the verifier collapses browser signature, owned-core, TUN and endpoint-address proof into the same enum, so the safe aggregate cannot identify which non-secret precondition failed. Cleanup returned WINBRAT to one GUI, zero core and absent TUN. Return a fixed allowlisted preflight reason enum only (no signer, path, PID, IP or exception text), then correct only the proven blocker and retry. Evidence: `artifacts/brat-stability/20260809-070738-BrowserLoad.jsonl`; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P2 FIXED-BROWSER PATH GAP / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — after the diagnostic enum was separated, the next fail-closed preflight returned `BrowserMissing`: WINBRAT does not have Edge at the runner's sole fixed x86 path. Do not install or locate an arbitrary executable. Permit only the four standard machine-wide Edge/Chrome x86/x64 paths, choose deterministically inside the no-argument payload and verifier, require a valid Microsoft/Google Authenticode signature, and continue to bind process-tree/socket proof to that exact chosen canonical path. Evidence: `artifacts/brat-stability/20260809-071249-BrowserLoad.jsonl`; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P2 PORTABLE BROWSER FIXTURE GAP / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — none of the four fixed machine-wide Edge/Chrome candidates exists on WINBRAT, so the broadened preflight still returned `BrowserMissing` and cleaned the VM. Google's official Chrome for Testing is purpose-built for reproducible automation and the pinned Windows stable archive `150.0.7871.129` was fetched from the official `storage.googleapis.com/chrome-for-testing-public` URL, but its `chrome.exe` is not Authenticode-signed. Treat it like the already-approved unsigned HY2 fixture: require the exact source-reviewed archive SHA-256 `4543709a8b323e655b8550d2203468eeeed69cd0fa21e4ae0499f314d53e470d`, validate the archive shape, deploy only into the verifier-owned transient root, bind process/socket proof to its exact executable, and delete the entire root in `finally`; never install it system-wide or accept an arbitrary browser/archive. Evidence: `artifacts/brat-stability/20260809-072006-BrowserLoad.jsonl` and official Chrome for Testing availability API; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P2 BROWSER WORKLOAD-PROOF DIAGNOSTIC COLLAPSE / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — the first portable-browser run passed VPN/endpoint/full-tunnel preflight and kept GUI/core/TUN healthy, but after transient archive deployment returned `PayloadFailed` with no metrics; the verifier collapses failure to observe the exact browser process tree, its fixed-endpoint TCP socket, and positive TUN delta into one enum. Cleanup returned one GUI, zero core and absent TUN. Return fixed allowlisted reasons for process/socket/TUN proof only, without PID/path/IP/raw errors, and retry without weakening the proof. Evidence: `artifacts/brat-stability/20260809-073017-BrowserLoad.jsonl`; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P2 TCP-SOCKET ATTRIBUTION IS INVALID AS A FULL-TUNNEL BROWSER GATE / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — the separated retry proved the exact portable browser process tree but did not expose an `Established` browser-owned TCP/443 row for the fixed endpoint, while VPNRouter remained connected. The endpoint has only one IPv4 address; Chrome may use HTTP/3 and transparent TUN ownership is not guaranteed to remain attributable through `Get-NetTCPConnection`. For this specifically Full-Tunnel-only test, require instead the conjunction already available after completion: approved no-argument payload and exact fixed HTTPS page, strict returned fetch/WebSocket counters and per-channel progress gaps, fixed endpoint route on `VPNRouter-TUN` before and after, positive TUN-byte delta, exact browser process tree, and one stable owned core/TUN. This does not authorize the same relaxation in split mode. Remove the redundant socket gate rather than inventing ETW/WFP tooling. Evidence: `artifacts/brat-stability/20260809-073934-BrowserLoad.jsonl`; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P2 BROWSER PROBE LIFECYCLE LOST ON NON-ZERO EXIT / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — after process-tree and TUN proof passed, the next run returned only `PayloadExitNonZero`; the approved BrowserProbe always writes one sanitized lifecycle JSON before returning non-zero, but the verifier checks exit code before parsing that bounded output and discards the actual safe reason. Parse and schema-allowlist the single JSON record first, map non-`Completed` lifecycle to a fixed `BrowserProbe*` enum, keep non-zero as mandatory failure, and never read/copy stderr. Evidence: `artifacts/brat-stability/20260809-075050-BrowserLoad.jsonl`; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P2 BROWSER PROBE INTERNAL FAILURE LACKS STAGE / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — the reordered verifier now reports `BrowserProbeInternalFailure`; verifier-side evidence already proved the exact browser process tree and positive TUN activity, so the failure happened after launch, but the payload's broad `catch` cannot distinguish process I/O setup, DevTools-port discovery, exact-page discovery or page polling. Track only a fixed current-stage lifecycle and map unexpected exceptions to that stage; do not emit exception text or stderr. Evidence: `artifacts/brat-stability/20260809-075915-BrowserLoad.jsonl`; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P1 ASYNC NAMED-MUTEX OWNERSHIP DEFECT / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — stage mapping still returned the outer `BrowserProbeInternalFailure`, which isolates the exception outside `RunSingleAsync`. Source review confirms the root cause: `RunAsync` acquires a thread-affine named `Mutex`, awaits the entire browser run, then may call `ReleaseMutex` from a different pool thread; .NET throws `ApplicationException`, and the outer catch replaces the otherwise valid result with `InternalFailure`. Replace only this single-run guard with a named binary `Semaphore`, whose release is not thread-affine, retain zero-wait fail-closed `AlreadyRunning`, and pin a concurrent-run test. Evidence: `artifacts/brat-stability/20260809-080822-BrowserLoad.jsonl` plus .NET ownership semantics; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P1 BROWSER PAGE-STATE STARTUP RACE / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — after replacing the async mutex, the probe advances to `BrowserProbeInvalidPageState` immediately after browser/TUN proof. `FindExactPageAsync` can return the exact navigation target before the page script has initialized its lexical `state`; the first DevTools evaluation then has `exceptionDetails` and is treated as permanent corruption. Add a bounded startup-only retry for unavailable/invalid fixed state within the existing 30-second startup budget; after the first valid state, keep every malformed or regressing sample fail-closed. Evidence: `artifacts/brat-stability/20260809-081733-BrowserLoad.jsonl` plus `BrowserProbe.cs` initialization order; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P2 NON-ZERO BROWSER EXIT STILL COLLAPSES COMPLETED VS UNKNOWN LIFECYCLE / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — after the startup-race fix the first full 10-minute browser window ran, with VPNRouter connected throughout, but the final result was again `PayloadExitNonZero`. The JSON serializer contract is pinned to a string lifecycle, so the remaining safe distinction is whether the parsed lifecycle was exactly `Completed` despite non-zero process exit or was outside the allowlist. Return only `BrowserProbeCompletedExitNonZero` versus `BrowserProbeLifecycleUnrecognized`; do not expose the raw lifecycle, exit code or stderr. Evidence: `artifacts/brat-stability/20260809-082550-BrowserLoad.jsonl`; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P2 REMOTED PROCESS EXIT CODE CONTRADICTS PINNED COMPLETED CONTRACT / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — the differentiated full-window retry returned `BrowserProbeCompletedExitNonZero`. In the exact hash-pinned source, the probe serializes its final record and returns zero iff lifecycle is `Completed`; therefore the long-lived remoted `Start-Process` object's exit-code observation is unavailable/stale or otherwise unreliable. For this approved no-argument payload, remove the redundant exit-code gate only after one bounded JSON record parses as `Completed`, `Done=true`, strict fetch/WebSocket floors/gaps pass, fixed route is TUN before/after, TUN bytes grow and core/TUN remain stable. Every non-Completed/unknown lifecycle remains fail-closed; stderr remains unread. Evidence: `artifacts/brat-stability/20260809-084410-BrowserLoad.jsonl`; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P1 COMPLETED BROWSER METRIC FAILURE MISCLASSIFIED AS HARNESS FAILURE / RESOLVED IN WORKTREE / LIVE VERIFIED 2026-08-09** — after removing the unreliable exit-code observation, another full 10-minute window returned `PayloadFailed`: the remote verifier discards the allowlisted aggregate whenever any strict fetch/WebSocket floor/failure/gap threshold misses, so the coordinator treats a real measurement as harness corruption and stops before cycles 2–3. For lifecycle `Completed`, always return the bounded aggregate plus route/core/TUN booleans; evaluate thresholds at the controller, emit `Status=FAIL`/`MEASURED_FAILURE`, and continue the remaining fixed cycles. Non-Completed or malformed output remains fail-closed. Evidence: `artifacts/brat-stability/20260809-090211-BrowserLoad.jsonl`; status: **RESOLVED IN WORKTREE / LIVE VERIFIED**, harness-only.
- [x] **P1 PORTABLE BROWSER ARTIFACT IGNORE GAP / RESOLVED IN WORKTREE 2026-08-09** — the pinned Chrome for Testing archive and its expanded ~195 MB tree appear as untracked files because `/artifacts/chrome-for-testing/` is not ignored. An indiscriminate add could publish third-party binaries and bloat the PR. Add one narrow root ignore rule while keeping source scripts/tests tracked. Source: final `git status --untracked-files=all`; status: **RESOLVED IN WORKTREE**, verified zero Chrome artifact paths in `git status`, repository hygiene only.
- [x] **P3 BROWSER RUN TERMINAL EVIDENCE MARKER / RESOLVED IN WORKTREE 2026-08-09** — the final BrowserLoad artifact contains 3/3 BrowserCycleResult=PASS plus final CleanState, and the outer coordinator process exited, but the durable JSONL ends without a distinct success terminal event. A future unattended reader cannot prove outer success from the artifact alone. Emit one sanitized RunCompleted record only after final cleanup and before the summary; pin with a source-contract test. Evidence: `artifacts/brat-stability/20260809-092035-BrowserLoad.jsonl`; status: **RESOLVED IN WORKTREE**, parser and focused 47/47 green, harness-only, no product defect.
- [ ] **P3 DEEP-VERIFY CANCELLATION TEST FLAKE / MEASUREMENT-GATED 2026-08-09** — GitHub workflow run `31285642931` on commit `839c2623` failed only `DeepVerifyProbeCancellationTests.ExternalCancellation_Rethrows_NotHttpTimeout` because no exception was observed; the newer run `31285803443` on the exact same SHA passed the `test` job, and every latest named check for the last seven commits is green. Treat as a superseded timing flake, not a product regression. Do not alter cancellation semantics unless it recurs on a new SHA or under a bounded repeat test. Status: **DEFERRED / RECURRENCE-GATED**.
- [x] **P3 REFUTED 2026-08-08** — the mandated DeepSeek-in-Qwen read-only review was attempted with safe mode and a zero tool budget, but the worker tried one tool and exited fail-closed; no review finding was produced and its tool permissions were not relaxed — task evidence only
- [x] **P1 RESOLVED 2026-08-08** — GameUdp retained one short-lived UDP cookie for its entire fixed-duration profile, so valid traffic became deterministically unauthenticated shortly after the cookie expiry — fixed by `8b9a185a` and `d168f073`: refresh retry is one-second bounded and stale expiry cannot replace the current cookie; pinned by pure rotation/retry tests — `VPNRouter.Tools/WinbratLoadGen/Program.cs` — draft PR #124
- [x] **P1 RESOLVED 2026-08-08** — GameUdp coupled paced send and receive into one loop, imposing a receive wait on each packet and validating delayed replies against the wrong random payload — fixed by `8b9a185a`: independent bounded receiver plus deterministic sequence payload; pinned by scheduling and delayed/out-of-order contracts — `VPNRouter.Tools/WinbratLoadGen/Program.cs` — draft PR #124
- [x] **P1 RESOLVED 2026-08-08** — GameUdp interpreted any old outstanding datagram as a flow blackout and accepted unknown sequence numbers as acknowledgements, producing false failures and inflated success metrics — fixed by `8b9a185a` and `d168f073`: continuous-ack semantics, unknown classification and synchronized aggregation — `VPNRouter.Tools/WinbratLoadGen/GameUdpMetrics.cs` — draft PR #124
- [x] **P1 RESOLVED 2026-08-08** — UDP replay tracking could retain one sequence entry per fresh authenticated cookie without expiry/cap cleanup — fixed by `8b9a185a`: cookie-expiry cleanup plus fixed replay cap — `VPNRouter.Tools/LoadTest.Protocol/LoadTestProtocol.cs` — draft PR #124
- [x] **P2 RESOLVED 2026-08-08** — BrowserBurst timers could overlap and never terminated WebSocket/fetch activity after its fixed profile duration; already-started fetches were not aborted at stop and could be counted as false failures — fixed by `8b9a185a`, `e44021eb` and `d168f073`: non-overlap, fixed stop, socket/timer cleanup and abort-aware fetch cancellation — `VPNRouter.Tools/LoadTarget/Program.cs` — draft PR #124
- [x] **P1 RESOLVED 2026-08-08** — fixed HTTP/blob/WebSocket responses lacked request/message rate gates and its singleton limiter was unsafe under concurrent BrowserBurst traffic — fixed by `8b9a185a` and `d168f073`: fixed shared rate gates plus a single limiter lock; pinned by parallel cap contract — `VPNRouter.Tools/LoadTarget/Program.cs` / `VPNRouter.Tools/LoadTest.Protocol/LoadTestProtocol.cs` — draft PR #124
- [x] **P1 RESOLVED 2026-08-08** — the verifier load-test preflight used `System.Net.Http.HttpClient` in the remote Windows PowerShell session without loading its assembly, so it failed after tunnel route proof but before fixed health validation — fixed by `8b9a185a`: loads the assembly before constructing the client; pinned by source contract — `tools/brat-verify.ps1` — draft PR #124
- [x] **P2 RESOLVED 2026-08-08** — the endpoint did not impose the fixed ten-minute WebSocket session bound, and the browser page did not classify a remote close before its own completion as a WebSocket failure — fixed by `e44021eb`: server cancellation bound and client close classification — `VPNRouter.Tools/LoadTarget/Program.cs` — draft PR #124
- [x] **P2 RESOLVED 2026-08-08** — the final GameUdp check ran after its intentional drain even though its continuous-sending guard made it unreachable, obscuring the intended no-false-blackout semantics — fixed by `e44021eb`: removes the dead post-drain check while preserving in-flight continuous-send detection — `VPNRouter.Tools/WinbratLoadGen/Program.cs` — draft PR #124
- [x] **P1 RESOLVED 2026-08-08** — the live verifier needed a source-built fixed payload identified by an explicit SHA-256 allowlist without exposing remote execution or raw logs — fixed by `5708367d`: source-build archive tool, empty source-reviewed allowlist, and strict lifecycle enums; it remains intentionally blocked until the separate measurement gate is met — `tools/brat-verify.ps1` / `tools/brat-loadtest.ps1` — draft PR #124
- [x] **P1 RESOLVED 2026-08-08** — a lost UDP cookie refresh response left the client refresh flag permanently pending, so a five-minute flow could expire its valid cookie without retrying and report a false outage; stale delayed cookies could also shorten the active expiry — fixed by `d168f073`: one-second retry cadence and non-decreasing expiry acceptance — `VPNRouter.Tools/WinbratLoadGen/Program.cs` — draft PR #124
- [x] **P1 RESOLVED 2026-08-08** — the singleton HTTP/WebSocket rate limiter mutates its dictionary and counters concurrently, allowing a race, possible dictionary corruption or cap bypass under BrowserBurst traffic — fixed by `d168f073`: one lock covers the fixed accounting transaction; pinned by parallel cap test — `VPNRouter.Tools/LoadTest.Protocol/LoadTestProtocol.cs` — draft PR #124
- [ ] **P2 MEASUREMENT-GATED 2026-08-08** — the fixed GameUdp profile is moderate game-like traffic, not a capacity/stress claim and not the known high-burst UDP boundary. Establish the owned-endpoint baseline first; only then consider a separate fixed high-burst measurement without raising the public HTTP limiter — `plans/phase2-winbrat-load-test-mvp-2026-08-08.md` — post-baseline decision
- [x] **P1 RESOLVED 2026-08-08** — GameUdp sender and receiver concurrently mutate/read metric dictionaries, sets, lists and counters without synchronization, risking collection corruption, false loss/RTT and exceptions — fixed by `d168f073`: one lock covers every public mutation/read and percentile snapshot; pinned by parallel sender/receiver test — `VPNRouter.Tools/WinbratLoadGen/GameUdpMetrics.cs` — draft PR #124

### Setup/diagnostics wizard Qwen review — 2026-08-07

- [x] **P2 RESOLVED in draft PR #116** — final read-only Qwen review found that footer button labels and two card titles did not opt into wrapping at the supported 360px minimum width. They now follow the wizard's existing wrapping pattern — `VPNRouter.App/Views/SetupWizardWindow.axaml`
- [x] **P3 REFUTED 2026-08-07** — Qwen proposed a fresh timeout for `InvokeThen`'s follow-up lookup, but the helper contract intentionally bounds the complete atomic UI operation with one `TimeoutSeconds` deadline; resetting it would let one request exceed that advertised budget. Live WINBRAT menu-to-modal invocation completed in 7.3s with the current fail-closed bound — `tools/brat-verify.ps1`
- [x] **P2 RESOLVED in draft PR #116 / WINBRAT 2026-08-07** — step 3's repair explanation overflowed and clipped past the right edge at the normal 620px window width because `ScrollViewer.Padding` shifted the content without reducing its measured viewport. Horizontal scrolling is disabled and spacing now lives on the inner grid, so wrapped text is constrained on both sides — `VPNRouter.App/Views/SetupWizardWindow.axaml` / `artifacts/brat-verify/setup-wizard/step3-repair.png`
- [x] **P2 RESOLVED in draft PR #116** — remote UIA verification could address only the first top-level app window and one action per helper run, so an Avalonia flyout dismissed before its menu item could be invoked and modal wizard controls were unreachable. The fixed remote-only driver now searches the process-wide automation subtree and supports one semantic `InvokeThen` sequence for popover-to-dialog flows; no local UI fallback was added — `tools/brat-verify.ps1`
- [x] **P2 RESOLVED in draft PR #116 / WINBRAT 2026-08-07** — Avalonia's radio-button `TogglePattern` returned normally on WINBRAT without changing the checked state, while the verifier reported `OK`. The helper now polls the state transition and fails closed when the semantic action is a no-op, preventing a false PASS — `tools/brat-verify.ps1` / `artifacts/brat-verify/setup-wizard/step1-toggle-proof.png`
- [ ] **P3 TOOLING LIMITATION / MEASUREMENT-GATED 2026-08-07** — WINBRAT's Avalonia UIA provider is inconsistent for wizard radio buttons: a visible routing radio can expose a no-op `TogglePattern` or intermittently fail the PID-scoped `Name + ControlType.RadioButton` lookup. The verifier now fails closed, but semantic routing-toggle E2E remains unavailable; product routing apply/undo is pinned by direct ViewModel tests. Add stable `AutomationId` values or a remote keyboard fallback only if future UI verification needs this control — `tools/brat-verify.ps1` / `VPNRouter.Tests/SetupWizardViewModelTests.cs` / `artifacts/brat-verify/setup-wizard/final-routing-change.png`
- [x] **P1 RESOLVED in draft PR #116** — a failed settings save left the user on the repair step while the error text existed only on the result step; the repair step now renders `OperationStatus` directly — `VPNRouter.App/Views/SetupWizardWindow.axaml`
- [x] **P2 REFUTED 2026-08-07** — a proposed partial on-disk MTU write cannot occur: `SettingsLoader.Save` serializes first, writes and flushes a same-directory `.tmp`, then atomically renames it; the existing ViewModel catch restores the in-memory MTU/routing/profile snapshot — `VPNRouter.Core/Services/SettingsLoader.cs:538-556` / `MainWindowViewModel.Settings.cs`
- [x] **P2 REFUTED 2026-08-07** — `HealthCheck.RunAll` has no Avalonia/UI-thread dependency: it loads the atomically-written settings snapshot and performs file/process/network probes, and is already shared with the CLI doctor path; running it off the dispatcher avoids freezing the wizard — `VPNRouter.Core/Services/HealthCheck.cs`
- [x] **P2 RESOLVED in draft PR #116** — added direct coverage that MTU-only reset preserves the applied routing mode and that an apply exception leaves the repair step usable with visible feedback — `VPNRouter.Tests/SetupWizardViewModelTests.cs`
- [x] **P3 RESOLVED in draft PR #116** — removed the unused window field and wrapped the Russian title at the supported 360px minimum; the diagnostics export double-click concern is refuted by the existing `IsExportingDiagnostics` re-entry guard, and the wrapped menu label is intentional for narrow layouts — `SetupWizardWindow.axaml.cs` / `SetupWizardWindow.axaml` / `MainWindowViewModel.Settings.cs`

### Cross-page QoL and recovery audit — 2026-08-08

- [x] **P1 RESOLVED IN THIS DRAFT / ROTATION STILL OWNER-ACTION 2026-08-08** — a live credential-like subscription URL supplied for testing had been copied verbatim into tracked historical plans/evidence. All current-tree occurrences of that identifier are now neutral placeholders and prompts/reports omit it. Runtime logging was already redacted. Because the old value remains recoverable from Git history, the subscription link still needs owner-side rotation/revocation; no history rewrite is attempted — documentation/security hygiene, this draft
- [ ] **P1 CONFIRMED ON PUBLISHED v2.48.0-r8 / WINBRAT; CONNECTIVITY-FIRST** — opening `Servers > Custom Config (JSON)` with an enabled subscription leaves both transient mode flags false while `SaveSettings` correctly keeps persisted `ConfigMode=subscribe`. This is not only a wrong `custom · full` label: Simple Connect then skips its protocol-aware `ServerHealthProbe` + `ConnectionIntentScorer` and dials the persisted active row without Smart Connect; connected urltest-label refresh also stops. The resolver still builds subscription mode, so do not claim a wrong config/tunnel. Preserve Servers-page peek state, but make Simple behavioral consumers read one small effective-mode resolver; pin `Subscribe -> Servers -> Custom -> Simple -> Connect` and Smart Connect invocation — `MainWindowViewModel.cs:500-534,1674-1685,3723-3776` / `MainWindowViewModel.SimpleMode.cs:280-289,498-522` / `MainWindowViewModel.ConnStats.cs:173-176` / [audit §4/§10](qol-interface-and-recovery-audit-2026-08-08.md)
- [ ] **P2 CONFIRMED ON PUBLISHED v2.48.0-r8 / WINBRAT; DEFER FROM CONNECTIVITY PATCH** — Simple's current-state summary says Full Tunnel while Russian direct bypass can be effective. Routing itself already shows the bypass card beside Full Tunnel, so do not rewrite the routing-option subtitle. If addressed later, add a Simple-only suffix based on effective geo availability, not merely the toggle — [audit §4/§10](qol-interface-and-recovery-audit-2026-08-08.md)
- [ ] **P2 CONFIRMED ON PUBLISHED v2.48.0-r8 / WINBRAT; DEFER FROM CONNECTIVITY PATCH** — Public has two live status messages (`FcStatusEmpty`, `FcStatusNoDeepCandidates`) that instruct the user to use absent `Refresh` / `Refresh list` actions while the real primary action is `Find working configs`; correct both strings rather than add a duplicate button. `FcRefreshSources` is currently unused on desktop and is cleanup-only — `Strings.FreeConfigs.cs:19-22,271` / `FreeConfigsPageViewModel.cs:82,131,1637` / `FreeConfigsPage.axaml:75-76` / [audit §4](qol-interface-and-recovery-audit-2026-08-08.md)
- [ ] **P2 CONFIRMED ON PUBLISHED v2.48.0-r8 / WINBRAT** — the subscription row repeats a long opaque URL, exposing most of a bearer-like identifier in screenshots/screen sharing even though the editable value already exists in Simple. Mask the row display to origin plus ellipsis while preserving the stored value and existing edit/add path — `SubscribePage.axaml:326-360` / [audit §4](qol-interface-and-recovery-audit-2026-08-08.md)
- [ ] **P2 CONFIRMED BLANK UI / PERSISTENCE IMPACT MEASUREMENT-GATED ON v2.48.0-r8; CONNECTIVITY-ADJACENT** — switching EN→RU replaces the localized `ConnectionIntentChoices` array and leaves the ComboBox visibly blank while status falls to General. A two-way `SelectedIndex=-1` would map to `general` and immediately save, potentially losing the Gaming preference that selects AWG/HY2, but the disk write was not observed. First add an EN↔RU headless test; a negative-index guard is safe, and broader selection rebuilding is authorized only if that test proves it necessary — `MainWindowViewModel.cs:1087-1100,1214-1226,7656-7665` / `SubscribePage.axaml:279-290` / [audit §4/§10](qol-interface-and-recovery-audit-2026-08-08.md)
- [ ] **P2 CONFIRMED UI-ORDERING DEFECT ON PUBLISHED v2.48.0-r8; DEFER FROM CONNECTIVITY PATCH** — a final `Done. Verified: N / total` summary already exists, but the `Progress<T>` callback performs a second `Dispatcher.UIThread.Post`; its trailing inner update can overwrite the awaited final summary with processed `20 / 20`. Remove the redundant post first, then optionally add failed/inconclusive counters from existing row verdicts. This is the root cause of the stale progress observation in the MTU report, not a connection failure and not a verifier-threshold change — `MainWindowViewModel.ServerTesting.cs:400-425` / `VlessDeepVerifier.cs:155-180` / [audit §3/§10](qol-interface-and-recovery-audit-2026-08-08.md)
- [ ] **P3 CONFIRMED ON PUBLISHED v2.48.0-r8 / WINBRAT** — progress text materialized before an EN→RU switch remains English (`Done. Pinged…`, `Deep verify subscription…`) in the otherwise Russian page. Clear or recompute volatile status strings during language refresh; do not rebuild the test engine — `MainWindowViewModel.ServerTesting.cs:258-282` / [audit §4](qol-interface-and-recovery-audit-2026-08-08.md)
- [ ] **P3 UX CANDIDATE / MEASUREMENT-GATED 2026-08-08** — Public shows a page-level `Connect` for the selected public config and the shell `Start VPN` for the current profile at the same time. The semantic difference is real, but user confusion has not been measured; first try explicit labels/tooltips, not a merged connection pipeline — [audit §5](qol-interface-and-recovery-audit-2026-08-08.md)
- [ ] **P2 CONFIRMED STATE-VISIBILITY GAP / DEFER FROM CONNECTIVITY PATCH 2026-08-08** — RU bypass is emitted only when the toggle is on and both geo files pass the local sanity check; a download failure logs that bypass is disabled for the session but the UI still presents the toggle as active. Do not connect this to game disconnects. A later truthful-status patch may expose effective availability, but must handle post-download refresh rather than reading only the toggle — `ConfigGenerator.cs:223-239` / `CustomConfigInjector.cs:184-190` / `StartupPipeline.cs:546-568` / `GeoDataDownloader.cs:90-108`
- [ ] **P3 CONFIRMED AS A CONSEQUENCE OF MTU-5 / NO SEPARATE PATCH 2026-08-08** — because manual MTU edits are not persisted, the wizard snapshots the edited VM value rather than the prior disk value; Reset writes 1420 and Undo can then persist that previously-unsaved value after clamping (for example 1600 becomes 1500). Resolving the existing MTU-5 autosave defect removes this path; do not add wizard snapshot persistence machinery — `MainWindowViewModel.Settings.cs:123-161` / `SetupWizardViewModel.cs:135-170` / `MainWindowViewModel.cs:3834-3837`

### Gaming connection stability — priority decision 2026-08-08

- [ ] **P2 CANDIDATE / MEASUREMENT-GATED; DO NOT AUTO-SWITCH YET** — runtime `AutoFailoverEngine.PickNextCandidate` chooses the first structurally usable untried subscription row without liveness, latency, ConnectionIntent or protocol ranking, while Gaming pre-connect scoring prefers AWG then HY2/TUIC. This can make recovery quality poor, but it does not prove ownership of the reported game disconnect: HealthMonitor observes core/HTTP serving and may remain green during game-specific UDP degradation. On the next real event record exact time, active protocol/row, HealthMonitor serving state, restart/failover reason and a bounded UDP-vs-HTTP divergence probe; only then decide whether to reuse existing protocol-aware liveness/intent scoring in failover — `AutoFailoverEngine.cs:284-327` / `ConnectionIntentScorer.cs:26-75` / `ServerHealthProbe.cs:7-19,43-74` / `HealthMonitor.cs:768-855` — [audit §10](qol-interface-and-recovery-audit-2026-08-08.md)
- [ ] **P2 FIX IN DRAFT PR #123 / LIVE VERIFIED 2026-08-08** — the prior WINBRAT verifier could repeat UI Connect/Disconnect and scan raw errors, but could not prove clean owned core/TUN state or that shell HTTP/UDP samples actually traversed the VPN in Split Tunnel. A direct-routed probe could create a false stability PASS. Draft PR #123 adds redacted `state`, route-gated fixed `probe`, sanitized `lifecycle` and a cleanup-safe coordinator; product behavior/configuration remain unchanged. Published r8 passed one shakedown plus 10/10 cold cycles and a 121-minute soak with 357 paired samples, two successful boundary sweeps, no lifecycle errors/restarts/failover, and a clean final state — `tools/brat-verify.ps1` / `tools/brat-stability.ps1` / [phase brief](phase2-winbrat-stability-harness-2026-08-08.md); close on merge
- [x] **P2 RESOLVED IN STABILITY DRAFT 2026-08-08** — the first coordinator self-review found that a concurrent invocation which failed to acquire the named mutex would still enter unconditional disconnect cleanup and could terminate the legitimate active run. Final cleanup is now conditional on mutex ownership; the normal acquired-run cleanup remains unconditional — `tools/brat-stability.ps1` / `BratStabilityToolingContractTests.cs`
- [x] **P3 RESOLVED IN STABILITY DRAFT 2026-08-08** — the initial lifecycle cap limited each log file to 50,000 lines but could aggregate more than 50,000 recent entries across rotated files. The sanitized window now fails closed as soon as its total timestamped entries exceed the advertised cap — `tools/brat-verify.ps1`
- [ ] **P3 TEST-INFRA LIMITATION / DEFERRED 2026-08-08** — a full non-elevated Windows suite run passed 2,679/2,706 but 25 existing tests wrote directly to protected `C:\ProgramData\VPNRouter` or shared the global TUN lock and failed outside their supported environment. The stability-specific contracts pass 5/5 and the solution build is clean. Do not weaken production ACLs or add a test-only product path in this tooling branch; use the normal GitHub test workflow as the full-suite gate — `VPNRouter.Tests` / [phase brief](phase2-winbrat-stability-harness-2026-08-08.md)
- [ ] **P3 MEASUREMENT-GATED RESIDUAL 2026-08-08** — exact Qwen review noted a small check-then-act window between proving both fixed destinations route through `VPNRouter-TUN` and sending the HTTPS/STUN samples. Ten cold cycles showed Tunnel before every successful probe and no manifestation. Per-packet interface binding would add platform-specific complexity and still would not emulate a game; revisit only if live evidence shows route churn during a probe — `tools/brat-verify.ps1`
- [ ] **P2 MEASUREMENT EVIDENCE / NOT ATTRIBUTED 2026-08-08** — the 121-minute r8 WINBRAT soak produced 355 healthy `HH` samples and two isolated `HNotU` samples (#50 and #226, roughly one hour apart): GUI/core/TUN stayed connected, both fixed routes remained Tunnel and HTTPS returned 204, while one 64-byte STUN request exhausted its 8-second receive budget; each recovered on the next sample without restart/failover. One packet to one public STUN responder cannot distinguish endpoint, underlay or application ownership, so this is evidence consistent with the reported game-like UDP symptom but not a confirmed VPNRouter defect and not an auto-AWG trigger. Escalate only on three consecutive `HNotU` samples or two independent operator-controlled UDP reflectors — [phase brief](phase2-winbrat-stability-harness-2026-08-08.md)
- [ ] **P2 TOOLING GAP / MEASUREMENT-GATED 2026-08-08** — the 121-minute soak was a low-rate liveness test, not load: each sample issued one HTTPS request and one small STUN transaction, so it did not exercise concurrent browser streams, WebSocket/HTTP3 session survival, sustained UDP cadence, bursts, socket queues or mixed TCP+UDP contention. Public STUN/HTTP services must not be load-tested and cannot provide controlled server-side counts. Keep the current probes as canaries; implement load profiles only after an operator-owned, rate-limited HTTPS/H2/H3 + authenticated non-amplifying UDP echo endpoint exists. The first useful run is one tunnel-proven UDP echo flow under a real browser workload; never mutate split-app selection or claim Dota/Roblox protocol emulation — [phase brief follow-up](phase2-winbrat-stability-harness-2026-08-08.md#load-and-browser-game-follow-up-decision)
- [x] **P3 RESOLVED IN STABILITY DRAFT 2026-08-08** — the first UDP failure was reported as generic `Socket` even when `UdpClient.ReceiveTimeout` produced `SocketError.TimedOut`; the fixed error classifier now emits the existing non-sensitive `Timeout` enum for that case — `tools/brat-verify.ps1` / `BratStabilityToolingContractTests.cs`

### Android typed-subscription connect — 2026-08-07

- [x] **P1 RESOLVED pending v2.48.0-r7** — on a fresh Android install, typing an HTTP(S) subscription on the Simple page only persisted the URL; Connect immediately called `RequestConnect()` without refreshing/selecting a server, so `GetActiveServer()` returned null and surfaced "server not configured". Typed Connect now reuses the fetch/select/connect path, reloads subscriptions from `AndroidStorage` so Simple/QR cannot overwrite persisted entries, and re-enables a deliberately re-entered subscription — `VPNRouter.Android/AndroidApp.VpnLifecycle.cs` / `AndroidApp.QrScanApply.cs` / `VPNRouter.Tests/AndroidAppCharacterizationTests.cs`

### MTU end-to-end audit — 2026-08-03 (static contract defects; runtime underlay/Android impact remains measurement-gated — full report `plans/mtu-end-to-end-audit-2026-08-03.md`)
- [ ] **P2 FIX IN DRAFT PR #113** — AWG now emits `min(normalized user MTU, 1420)` instead of overwriting a deliberately lower setting; 1200 preservation, 1500 cap, and endpoint 1420 are regression-pinned. Actual underlay loss remains measurement-gated — [report §MTU-1](mtu-end-to-end-audit-2026-08-03.md) — [draft PR #113](https://github.com/PavelLizunov/VPNRouter/pull/113); close on merge
- [ ] **P2 FIX IN DRAFT PR #113** — one 576..1500 contract now drives desktop save, validation, generation fallback, and the example config; out-of-contract runtime values fall back to 1420 — [report §MTU-2](mtu-end-to-end-audit-2026-08-03.md) — [draft PR #113](https://github.com/PavelLizunov/VPNRouter/pull/113); close on merge
- [ ] **P2 FIX IN DRAFT PR #113** — IPv6-enabled TUN rejects and desktop save clamps values below 1280, while IPv4-only retains the supported 576..1279 range — [report §MTU-3](mtu-end-to-end-audit-2026-08-03.md) — [draft PR #113](https://github.com/PavelLizunov/VPNRouter/pull/113); close on merge
- [ ] **P3 FIX IN DRAFT PR #113** — the unchanged conservative Windows helper is now labeled as fixed-target IPv4 DF ping to `8.8.8.8`, explicitly not VPN-endpoint or IPv6 measurement — [report §MTU-4](mtu-end-to-end-audit-2026-08-03.md) — [draft PR #113](https://github.com/PavelLizunov/VPNRouter/pull/113); close on merge
- [ ] **P2 CONFIRMED ON WINBRAT 2026-08-03** — editing `TunMtu` manually updates the warning and VM but does not call `SaveSettings`; the footer still says `Auto-saved` and Apply is disabled while disconnected, so `1600` reverted to the prior `1420` after restart. An unrelated explicit save path normalized `1600→1500` and `575→576`, proving the contract repair works once invoked — [report §MTU-5](mtu-end-to-end-audit-2026-08-03.md) — deferred from PR #113 because its research/verification scope forbids further product changes; next focused MTU persistence PR
- [x] **P2 RESOLVED v2.48.0-r6 (WINBRAT A/B 2026-08-03)** — the fixed 15-second failure matched Wintun's `WaitForInterface`: one clean sing-box PID created a half-visible adapter, Wintun's deterministic `RequestedGUID` path returned `ERROR_ALREADY_EXISTS`, removed that adapter, and sing-tun's fallback then found nothing to open. The lx build now fail-closed patches that call to pass `nil`, letting Windows allocate the transient adapter GUID. Three clean cold starts kept one PID/one Up TUN through the former failure window; two passed DF UDP payload 1392 (inner IPv4 1420); every Stop left zero PID/TUN; the 15-minute r6 log window was clean. Random GUID/NLA profile accumulation is a measurement-gated tradeoff, not observed impact — [report §9.3](mtu-end-to-end-audit-2026-08-03.md) — `tools/build-singbox-lx.ps1`
- [ ] **P2 CONFIRMED / SEPARATE READINESS DEFECT 2026-08-03** — desktop TUN warmup uses an HTTP request from the VPNRouter process and marked r5 `Connected` about one second after spawn even though that same core was still in Wintun initialization and fatally exited at 15 seconds. r6 removes the measured core failure, but the readiness signal itself still does not prove TUN ownership or proxied egress. Replace it only with a measured core/TUN-local gate; do not add a fixed sleep — [report §9.3 / next Task 6a](mtu-end-to-end-audit-2026-08-03.md) — next focused lifecycle PR
- [ ] **P2 IMPACT IF CONFIRMED / CANDIDATE 2026-08-03; RECONFIRMED v2.48.0-r8 2026-08-08** — Deep verify disagrees with the real-subscription selected-family run: all 3 WS rows stayed untested with verifier `invalid public_key`, and all 4 XHTTP rows were protocol-failed, while the corresponding selected configurations later sustained UDP at inner IPv4 1420. The r8 QoL audit again processed 20 rows and logged the same three WS `invalid public_key` startup failures; this corroborates repeatability, not ownership. ConfigGenerator intentionally includes same-IP siblings, so the selected-family traffic has no single-outbound attribution and cannot distinguish product transport failure, verifier artifact, or dead subscription rows. Reproduce with secret-free WS/XHTTP fixtures and released lx `check` before assigning the defect; the separate progress-summary ambiguity is tracked in the QoL section above — [report §9.3 / exact Task 5](mtu-end-to-end-audit-2026-08-03.md) / [QoL audit §3](qol-interface-and-recovery-audit-2026-08-08.md) — measurement-gated, no implementation authorized
- [ ] **P3 CANDIDATE / MEASUREMENT-GATED 2026-08-03** — one Deep-verify-healthy AWG subscription row connected but black-holed UDP to both STUN and DNS even at 64 bytes, while a second healthy AWG row passed 64/512/1200/1392-byte UDP (inner IPv4 through 1420). This rules out an MTU threshold and a family-wide AWG failure, but does not distinguish provider endpoint state from row-specific parameters or a path middlebox. Recheck the same row from another underlay or with server-side evidence before assigning ownership — [report §9.3](mtu-end-to-end-audit-2026-08-03.md) — no product change

### Remote-only post-ship verification — 2026-08-03

- [x] **P2 TOOLING LIMITATION RESOLVED IN THIS DRAFT / WINBRAT 2026-08-08** — the remote verifier could not select Avalonia `ListBoxItem` navigation entries, blocking four top-level pages and nested tool sections. Native `SelectionItemPattern.Select()` with a selected-state postcondition now navigates the published r8 UI without coordinates or local input — `tools/brat-verify.ps1` / [audit §2](qol-interface-and-recovery-audit-2026-08-08.md)
- [x] **P2 TOOLING LIMITATION RESOLVED IN THIS DRAFT / WINBRAT 2026-08-08** — long Settings pages could not satisfy the bottom-of-page assertion. Native `ScrollItemPattern.ScrollIntoView()` now brings already-materialized semantic controls into view and fails closed when unsupported; Autostart bottom was verified on WINBRAT — `tools/brat-verify.ps1` / [audit §2](qol-interface-and-recovery-audit-2026-08-08.md)
- [ ] **P3 TOOLING LIMITATION / MEASUREMENT-GATED ON WINBRAT 2026-08-08** — the virtualized subscription list exposes stable `AutomationId=SubList` but reports `VerticallyScrollable=false`; unrealized lower rows cannot be found by `ScrollIntoView`. A native large-increment experiment failed closed and was removed. Add stable row targeting or Appium only when a release-critical scenario needs virtualized-list traversal; no coordinate fallback and no raw-config reads — `tools/brat-verify.ps1` / [audit §3](qol-interface-and-recovery-audit-2026-08-08.md)
- [x] **P1 RESOLVED pending v2.48.0-r4** — local mouse/keyboard MCP replaced by the fixed Tailscale target `100.115.182.0` with mandatory `WINBRAT` identity verification; credentials resolve local-first with primary-worktree fallback and are never copied — PR #88
- [x] **P1 RESOLVED pending v2.48.0-r4** — pre-commit build/test pipelines now enable `pipefail`, so a failed `dotnet` command cannot be hidden by `tail` — PR #88
- [x] **P1 RESOLVED pending v2.48.0-r4** — `.agents` and `.claude` post-ship instructions are byte-identical and the obsolete local-MCP scripts/project are removed — PR #88
- [x] **P1 RESOLVED pending v2.48.0-r4** — deploy now pins `WINBRAT` on the same WinRM session used for process stop/copy/install and fails when the launched app is not running; the former check-then-reconnect and false-success paths are closed — `tools/brat-verify.ps1` / `deploy-to-testpc.ps1`
- [x] **P2 RESOLVED pending v2.48.0-r4** — post-ship log scan now covers every rolled `vpnrouter*.log` touched inside the requested time window and fails closed when a 50k-line/file cap cannot prove full coverage; the former newest-1000-lines scan could miss earlier errors — `tools/brat-verify.ps1`
- [x] **P2 RESOLVED pending v2.48.0-r4** — UIA helper cleanup is verified fail-closed, request values are deleted immediately after reading, and secret-bearing screenshots are excluded by `/artifacts/brat-verify/` — `tools/brat-verify.ps1` / `.gitignore`
- [x] **P2 RESOLVED pending v2.48.0-r4** — post-ship power fallback explicitly uses the established localhost Proxmox tunnel, phase instructions use the Tailscale WINBRAT endpoint, and the CI preflight no longer treats successful `gh auth status` stderr as a terminating error — `.agents/skills/post-ship-mcp-verify/SKILL.md` / `.agents/skills/phase-task-launcher/SKILL.md` / `tools/verify-last-commit-ci.ps1`
- [ ] **P2** — screenshot matrix and about 20 stable AutomationIds remain measurement-driven follow-ups; add only for user scenarios that the remote UIA verifier cannot express reliably — after r4
- [ ] **P3** — duplicated localization/binding wrappers remain deferred until a headless/UIA safety net proves deletion is worthwhile — TBD

### r4 release-gate follow-up — 2026-08-03

- [x] **P2 RESOLVED pending v2.48.0-r4** — split-tunnel teardown test still expected direct `IProcessMonitor.Stop()` after PR #74 intentionally changed the owned monitor lifecycle to `Dispose()` (which calls `Stop()` and releases the ETW wait handle); aligned the missed sibling assertion with the existing lifecycle test — `VPNRouter.Tests/VpnEngineSplitTunnelLifecycleTests.cs`

### Codebase/context reduction audit — 2026-08-01 (static/call-graph verified, NO live behavior change for F1 — full report `plans/qwen-context-footprint-and-code-reduction-audit-2026-08-01.md`)
- [x] **P2 RESOLVED pending v2.48.0-r4** — removed the dead CustomDirectRules parser/generator/localization aliases and their obsolete self-only tests (~697 LOC, ~6-8k tokens); preserved `AppConfig.CustomDirectRules`, `CustomDirectRule`, `AppSettingsSane`, `SettingsMigrator.Migrate_1_to_2`, `YamlStaticContext`, and migration/roundtrip/robustness coverage — PR #98 / [report §F1](qwen-context-footprint-and-code-reduction-audit-2026-08-01.md)
- [ ] **P3** — dead settings schema cleanup: `AutoDownload` + `DownloadUrl` are dead, `ProcessScanInterval` is unused by runtime but has a live validator — remove only in a coordinated schema cleanup (validator + tests together); unknown YAML keys already safe via `IgnoreUnmatchedProperties` (~15 product LOC + tests) — [report §F3](qwen-context-footprint-and-code-reduction-audit-2026-08-01.md) — later cleanup
- [ ] **P3** — tiered whole-review context profile (search-driven Tier 0-3 retrieval; a dedicated review profile may exclude historical plans/evidence/design + bulk tests while keeping them searchable); adopt only after one dry-run proves <=1M token bundle; NO blanket default `.qwenignore`, NO repo deletion — developer tooling only — [report §5](qwen-context-footprint-and-code-reduction-audit-2026-08-01.md) — TBD
- [ ] **P3** — docs/context hygiene: context-exclude (optionally archive) closed plans + `plans/test-screenshots` dated XML/evidence + historical design artifacts from the whole-review bundle, and fix stale `CURRENT_STATE.md` + `plans/CLAUDE.md` active-version list + broken `docs/REVIEW_AGENT_PROMPT.md` target; NO mass archive/move until links validated — [report §6](qwen-context-footprint-and-code-reduction-audit-2026-08-01.md) — TBD

### Internet-optimization research — 2026-08-01 (static code/version exposure; NO live repro yet — full report `plans/qwen-internet-optimization-research-2026-08-01.md`; Claude Opus adversarially validated the Qwen pass on 2026-08-01 and corrected the overclaims below)

**Measurement gate for EVERY entry below:** these are version/code exposures only, NOT confirmed user bugs. NO implementation starts until the report's validation result justifies it — measure-first, do not optimize on static evidence alone.

- [ ] **P2** — Desktop sing-box core is the `Leadaxe/sing-box-lx` fork pinned at commit `c7a2592e` (source base proven by the fork's `go.mod` → `sing-tun` v0.8.10, NOT by the version string: only Windows labels itself `1.13.13-lx-awg` via ldflags, while mac/Linux forge `constant.Version` from the VPNRouter app version). Two upstream fixes are missing, plus a base-rotation audit — (a) **TUN system-stack TCP NAT collision**: sing-box `0b7ffba` is a sing-tun bump, the fix itself is sing-tun `8caaa93`; exact trigger = the same source IP:port reused for a DIFFERENT destination while the old NAT entry is still alive (Windows/Linux use the `system` stack, macOS `gvisor` is NOT exposed); validate with >20k short TCP connections in five minutes across >=2 destinations through system TUN, detecting cross-destination delivery; (b) **deterministic nested single-flight DNS self-deadlock** `72a8723e13b9574664f4c78e588069fa4aca6fc9`: outer query Q on transport T1 bootstraps T2 which needs the same Q — the only applicable candidates are the DoH `vpn-dns` HOSTNAME with `DomainResolver=local-dns` and proxy outbounds given as a hostname with `DomainResolver=local-dns`; validate by querying `dns.adguard-dns.com` through the tunnel with BlockAds on, then the real VLESS/proxy hostname, fork vs a vetted 1.13.15 candidate (no broad load needed); (c) **base rotation audit vs current upstream v1.13.15 (released 2026-07-29)** — brings a newer sing-tun, but its other changes are UNINSPECTED (do NOT assume they are safe/apply); must preserve the AWG/XHTTP + downstream patches and re-pin the Linux `libcronet` version + SHA256 in the same change — `tools/build-singbox-lx.ps1:31,37,162` / `tools/build-singbox-lx.sh:18,23,98` / `build.ps1:316,325-336` (upload requires fork tags; a local non-upload build may fall back to upstream) / `build-mac.sh:60` / `.github/workflows/build-linux.yml:101,106-112` / `VPNRouter.Core/Services/ConfigGenerator.cs:39-40,1003-1017,1643,2002,2121,2137-2166` — [report §F1/F2/F6](qwen-internet-optimization-research-2026-08-01.md), TBD
- [ ] **P3** — Android libbox is sing-box `1.13.10`, missing `d166f0d` (1.13.12) + `6548c17` — **REFUTED for DoH**: neither commit touches the HTTPS/DoH transport, so the "all Android DoH users affected" claim is dropped. Narrowed to the ONLY plain-UDP DNS path: the BypassRu RU resolver `77.88.8.8` (`type:udp`, detour `dns-direct`), present only when BypassRu is enabled. Desktop is NOT affected (fork base already carries the 1.13.12 fixes). Minimal measurement (socket/memory growth + DNS stalls) ONLY with BypassRu on; DoH-only run is the negative control. If confirmed — rotate libbox to a tested newer upstream, NOT local workarounds — `build-android.ps1:48` / `VPNRouter.Core/Services/ConfigGenerator.cs:848-854` — [report §F3](qwen-internet-optimization-research-2026-08-01.md), TBD
- [ ] **P3** — Android Avalonia `12.0.3` vs desktop `12.0.4` — NOT a defect, controlled upgrade RESEARCH only. Relevant fixes: TalkBack PointToScreen (#21402) + Skia stable bump (#21434); Android candidates: File properties (#21307) + system-back event (#21246). RU/EN bidi and ItemTemplate compiled-binding validation are NOT relevant (RU/EN are LTR; Android templates are C# `FuncDataTemplate`). The transitive SkiaSharp `3.119.4-preview.1.1` resolve is an INFER (Avalonia.Skia 12.0.3 minimum dependency; no direct Android pin, no lock file) — prove with `dotnet list package --include-transitive` when the SDK is available. Releases 12.0.5 / 12.1.0 / 12.1.1 exist, so validate a current vetted target instead of blindly pinning 12.0.4; keep/revert on evidence — `VPNRouter.Android/VPNRouter.Android.csproj:132-135` — [report §F5](qwen-internet-optimization-research-2026-08-01.md), TBD

### Qwen full-application audit — 2026-07-28 (18 parallel read-only reviewers; Codex-verified)
- [x] **P0 RESOLVED v2.48.0-r1** — desktop update SHA256 is fetched but discarded by the `IUpdateSource` adapter (`FullChecksumUrl=null`), so normal GitHub release staging skips the hash gate — `VPNRouter.Core/Services/UpdateChecker.cs:119,251` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — `TunOwnershipLock` is a disposed singleton: the second connect/disconnect lifecycle reacquires the semaphore but `Dispose` returns early and never releases it, blocking other VPNRouter processes until app exit — `VPNRouter.Core/Services/TunOwnershipLock.cs:114` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — pre-start and post-start failover share one `_failover ??=` slot with incompatible delegates; pre-start wiring can later bypass teardown/lifecycle/session guards, orphan sing-box/TUN state, or revive a disconnected tunnel — `VPNRouter.Core/Services/VpnEngine.cs:1495,1527` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1 (2026-07-29)** — Linux kill-switch accepts IPv6 server literals and then removes them from its allow-list while dropping all IPv6, so a crash blocks reconnect to an IPv6-only server until manual nft cleanup — fixed by ab00375f: `BuildRuleset` now splits server literals by family and emits IPv6 through `ip6 daddr`. Pinned by `LinuxFirewallManagerTests.BuildRuleset_MixedFamily_EmitsBoth` — `VPNRouter.Core/Platform/Linux/LinuxFirewallManager.cs:205,226`
- [x] **P1 RESOLVED v2.48.0-r1 (2026-07-29)** — macOS kill-switch emits accepted IPv6 server literals as IPv4-only `inet` PF rules; atomic PF load fails and leak protection never engages — fixed by ab00375f: `BuildRules` now selects `inet6` for IPv6 literals and `inet` for IPv4. Pinned by `MacFirewallManagerTests.BuildRules_MixedFamily_BothFamilies` — `VPNRouter.Core/Platform/macOS/MacFirewallManager.cs:340,359`
- [x] **P1 RESOLVED v2.48.0-r1** — settings save overwrites `config.yaml` directly; a crash or power loss during `File.WriteAllText` can truncate all settings and credentials — `VPNRouter.Core/Services/SettingsLoader.cs:536` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — subscription HTTP responses are buffered without a byte limit, allowing an untrusted provider/endpoint to exhaust process memory — `VPNRouter.Core/Services/PolicyHttpClient.cs:112-118` / `SubscriptionFetcher.cs:85` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1 (2026-07-29)** — Smart Connect stores its measured winning server, then `SaveSettings` immediately overwrites it from the stale selection and connects the wrong/dead server — fixed by 94ce7a71: syncs `SelectedSubscriptionServer` to the probed winner before `SaveSettings`. Pinned by `SmartConnectPersistenceTests.SmartConnect_SyncsSelectionBeforeSaveSettings_SourcePin` — `VPNRouter.App/ViewModels/MainWindowViewModel.SimpleMode.cs:536` / `MainWindowViewModel.cs:3783`
- [x] **P1 RESOLVED v2.48.0-r1 (2026-07-29)** — repair trampoline still uses inline PowerShell `-Command` download-and-execute, reintroducing the Defender ClickFix heuristic already removed from the app repair path — fixed by e3fdf121: writes the bootstrap to a temp .ps1 and launches PowerShell with `-File`, never inline `-Command`. Pinned by `TestRepairArgs_UsesFileNotCommand` + `TestRepairArgs_PathWithSpaces_PreservedAsSingleArg` — `VPNRouter.GUI/repair.go:58`
- [x] **P1 RESOLVED v2.48.0-r1 (2026-07-29)** — `vpnrouter stop` kills only the child PID; the owning `start` engine treats it as a crash, restarts sing-box after the state file is cleared, and leaves an untracked VPN — fixed by 799e0903: stop signals the owning start process through its named event so lifecycle cancellation performs graceful shutdown. Pinned by `P07CliStopSourceGuardTests.StopCommand_HasOwnershipGate_AndOwnerSignal` — `VPNRouter.CLI/Commands/StopCommand.cs:23`
- [x] **P1 RESOLVED v2.48.0-r1 (2026-07-29)** — CLI stop kills the recorded PID without revalidating executable ownership; PID reuse can terminate an unrelated process tree — fixed by 799e0903: `ProcessOwnership.IsOwnedSingBox` is checked before the kill fallback. Pinned by `P07CliStopSourceGuardTests.StopCommand_HasOwnershipGate_AndOwnerSignal` — `VPNRouter.CLI/Commands/StopCommand.cs:23`
- [x] **P1 RESOLVED v2.48.0-r1 (2026-07-29)** — Android broadcasts and logs raw libbox exception messages that can contain server addresses, UUIDs, or config fragments — fixed by 799e0903: `startTunnel` now uses `scrubSecrets(e.getMessage())`, logs only the exception class name + safeMsg without the Throwable, and broadcasts only safeMsg. Pinned by `P07CliStopSourceGuardTests.Android_StartTunnel_ScrubsError_NoRawThrowable` — `VPNRouter.Android/VpnRouterService.java:682`
- [x] **P1 RESOLVED v2.48.0-r1** — macOS build references undefined `${ARCH}` under `set -u` and aborts whenever the wgturn source/build branch is reachable — `build-mac.sh:90,93` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — Linux release CI downloads and executes mutable `continuous` appimagetool without a digest — `.github/workflows/build-linux.yml:169` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — Linux release CI downloads and bundles the sing-box/libcronet archive without verifying its digest — `.github/workflows/build-linux.yml:107` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1 (2026-07-29)** — full credential-bearing subscription URLs are written to logs, exposing provider tokens — fixed by d857fa6e: every `SubscriptionFetcher` URL log argument now uses `CanaryPolicy.RedactUrl`; runtime coverage is `SubscriptionUrlRedactionTests` + `CanaryPolicyTests` — `VPNRouter.Core/Services/SubscriptionFetcher.cs:62,72,88,101,106,110,324,343`
- [x] **P1 RESOLVED v2.48.0-r1** — `%ProgramData%\VPNRouter` inherits local Users read access for config and logs containing VPN credentials — `VPNRouter.Core/AppPaths.cs:112` / `packaging/windows/install.ps1` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1 (2026-07-29)** — Zapret update swallows locked-file copy failures but still writes the new version marker, suppressing repair of a mixed old-driver/new-executable installation — fixed by b2e9499b: recursive copy returns aggregate success and the version marker advances only when all files copied. Pinned by `ZapretUpdaterAtomicityTests.CopyDirectoryOverwrite_LockedFile_ReturnsFalse` + `MarkerGate_VersionWriteOnlyInSuccessBranch_SourcePin` — `VPNRouter.Core/Services/ZapretUpdater.cs:347-374,619-636`
- [x] **P1 RESOLVED v2.48.0-r1 (2026-07-29)** — Clash log stream writes the full WebSocket URI including `clash_api_secret`; the crash-report scrubber does not remove `ws://` query tokens — fixed by d857fa6e: the Information log now uses `RedactLogsUri(_logsUri)`, which strips the query/token. Pinned by `ClashLogStreamTests.RedactLogsUri_StripsQuery` + `RunAsync_InformationCall_UsesRedactLogsUri` — `VPNRouter.Core/Services/ClashLogStream.cs:133` / `CrashReporter.cs:169-199`

### r9 Android cut-readiness audit — 2026-07-10 (device-verified; one P3 copy follow-up)
- [ ] **P3** — the r8 F2 fork-feature-gate rejection message is desktop-worded and misleading on Android. `CustomConfigInjector.CheckForkFeatureSupport` tells a user pasting an AWG/xhttp custom config to "Use a VPNRouter build bundling the lx core" — but there is NO lx Android build (Android ships upstream SFA-libbox with AWG/XHTTP off; **verified**: bundled `libbox.so` has `with_wireguard/with_quic/with_utls/with_v2ray_api` but NO `with_awg`/`with_xhttp`, and 0 amnezia symbols). The REJECTION ITSELF IS CORRECT on Android — pre-r8 such a config hit libbox's `unknown transport type` FATAL; r8 turns it into an actionable up-front error (a strict improvement, NOT a regression). Only the copy needs a platform-neutral rewording (e.g. "…this build's sing-box core lacks with_awg — remove the AWG fields, or use a desktop build with the lx core; AmneziaWG isn't available in the Android build"). Shared Core string, improves desktop too. Edge path (custom AWG/xhttp config on a phone) — not cut-blocking — `VPNRouter.Core/Services/CustomConfigInjector.cs` CheckForkFeatureSupport — batch into next Core change

### r8 post-ship live-verify findings — brat 2026-07-10 (both pre-existing, NOT r8 regressions)
- [x] **P2 RESOLVED v2.47.0-r9 (2026-07-10)** — deep-verify "Стоп" cancel was UNREACHABLE from the UI: the bare `[RelayCommand]` on the four async batch-test commands generated AsyncRelayCommands that DISABLE the bound button while running, so the `if (IsTesting*) Cancel()` re-entry branch never fired (live-proven on brat: four cancel attempts, the 21-server batch completed every time). All four now carry `[RelayCommand(AllowConcurrentExecutions = true)]`; re-entry is safe (everything before the IsTesting* flag assignment is synchronous on the dispatcher, so a second click can only take the cancel branch). Pinned by `ServerTestCancelReentryTests` (4 source pins) — `VPNRouter.App/ViewModels/MainWindowViewModel.ServerTesting.cs`
- [x] **P2 RESOLVED v2.47.0-r9 (2026-07-10)** — transient false "Подключено через службу" while deep-verify runs: the 2s poll's `RuntimeStatusDetector.IsVpnRunning` counted our OWN probe-spawned sing-box processes (ownership filter passes — they run from ProgramData\bin) as a live tunnel. (The brat Tailscale-adapter theory was WRONG — `TunOwnershipLock` is a named semaphore, not an adapter check; the poll path was simply single-signal.) Fix: `DeepVerifyProbe.BeginProbeScope()` marks the probe window in both desktop verifiers, and while any probe is in flight the detector requires the second signal (`TunOwnershipLock.IsOwnedByAnyone()` — held only by a real tunnel owner). Outside a probe window the single ownership signal keeps its existing semantics. Pinned by `DeepVerifyProbeScopeTests` (counter behaviour + detector/verifier source pins) — `VPNRouter.Core/Services/RuntimeStatusDetector.cs` + `DeepVerifyProbe.cs`

### r8 review findings — Fable deep review 2026-07-10 of v2.46.0..r7 (all fixed same-day; goal: plans/goal-v2.47.0-r8-review-findings-2026-07-10.md)
- [x] **P1 RESOLVED v2.47.0-r8 (2026-07-10)** — user Cancel mid-deep-verify false-branded in-flight servers `ProtocolHandshakeBlockedLikely` (12h Auto-pool exclusion + RU-block warning): `DeepVerifyProbe.ProbeViaSocksAsync` swallowed EXTERNAL cancellation as `"http timeout"` → `ProxiedHttp` FAIL instead of the typed `Cancelled` phase. Now rethrows when `ct.IsCancellationRequested` (mirrors `MeasureBandwidthViaSocksAsync`); pinned by `DeepVerifyProbeCancellationTests` (2) + the `TcpOk_UserCancelledDeepVerify_ClassifiesAsUntested_NeverBlocked` e2e guardrail — `VPNRouter.Core/Services/DeepVerifyProbe.cs`
- [x] **P1 RESOLVED v2.47.0-r8 (2026-07-10)** — endpoints-only (WG/AWG endpoint egress) custom config passed `Validate` (since r1's WG-endpoint acceptance) but `Inject` wrote the fabricated `"custom-proxy"` tag into `route.final`/rules (the proxy-tag resolver scanned only `outbounds`) → sing-box FATAL at start ("valid" → opaque crash). `FindProxyOutboundTag` now falls through to the first wireguard ENDPOINT's tag; pinned by 2 new positive-Inject `CustomConfigInjectorForkGateTests` — `VPNRouter.Core/Services/CustomConfigInjector.cs`
- [x] **P2 RESOLVED v2.47.0-r8 (2026-07-10)** — build-android AppVersion gate compared only the version CORE, letting an -rN suffix drift through (the v2.25.0-r1→r2 in-app update-loop class; the APK versionName is tag-forced but the EMBEDDED AppVersion drives the in-app check). Now a full-string compare — `.github/workflows/build-android.yml`

### true-split (W1) — reviewer P1 fixes 2026-07-05 (3 fixes landed: engage-fail badge cleanup, null-inet-NIC guard, not-yet-running excluded resolve) — residual follow-ups (fail-SAFE, post-capture covers them)
- [ ] **P2** — true-split: excluded apps NOT on PATH and not running at connect get no driver-level 0-gap until a config change re-engages (ETW-late-engage follow-up) — `VPNRouter.Core/Services/VpnEngine.cs` `TryEngageSplitDriverAsync` — TBD. Fail-safe: `ResolveRunningPath`→`ResolveNameToPath` covers running + on-PATH apps; anything unresolved (e.g. Discord in `%LocalAppData%`) is still routed by the post-capture `process_name → direct` rule. Upgrade: on `EtwProcessMonitor.ProcessStarted` for an excluded name, dorezolve the started process's image path and re-engage (arch plan §5.4).
- [ ] **P2** — true-split: `ProcessImagePath.ResolveNameToPath` resolves only apps on `PATH` (via `where.exe`); an excluded app installed off-PATH and not yet running can't be pre-registered, so it splits only after the ETW-late-engage above lands or a hot-apply re-engages while it's running — `VPNRouter.Core/Services/ProcessImagePath.cs` `ResolveNameToPath` — TBD (same residual root as the ETW item; post-capture routes it meanwhile).

### true-split (W1) — bug-hunt survivors 2026-07-05 (all P2; 3× P1 + 2× P2 already FIXED in the same hunt; these are non-gating hygiene/defense-in-depth, all fail-SAFE today)
- [x] **P2 RESOLVED v2.47.0-r4 (2026-07-10)** — Runtime `.sys` integrity: `EngageLocked` now runs `VerifySysIntegrityLocked()` before `EnsureServiceLocked` — reads the sibling `checksums.sha256`, compares the `.sys` sha256, and on MISMATCH logs a named warning (stale/partial driver, ABI mismatch possible) instead of an opaque later IOCTL failure. Fail-OPEN by design (Windows signature enforcement is the tamper gate; this only diagnoses a stale-but-signed swap). Pure `SplitTunnelPolicy.ParseSidecarHashFor` pinned by 7 `SplitTunnelProtocolTests` — `VPNRouter.Core/Services/SplitTunnelDriverManager.cs`
- [x] **P2 RESOLVED v2.47.0-r3 (2026-07-10)** — SCM `StartService` 1058 (ERROR_SERVICE_DISABLED) now gets a targeted `LastFailureReason` ("service disabled; re-run the installer or `sc config mullvad-split-tunnel start= demand`") alongside the pre-existing 1072 (MARKED_FOR_DELETE → reboot) message, instead of the generic "StartService err=N" (the v2.31.7 silent-generic-failure lesson) — `VPNRouter.Core/Services/SplitTunnelDriverManager.cs:528`
- [ ] **P2** — `GetInternetInterfaceAddresses` filters only our TUN (by name) + WG/AWG/Tailscale (by `IsWireGuardName`); another virtual adapter that is Up + has a v4 gateway (Hyper-V vSwitch, VMware, OpenVPN TAP) could be picked → excluded apps bind to a wrong/dead IP. Design-accepted (§9 "multi-NIC exotica"), diagnosable via the `Internet NIC for split-bind: {Name}` log; requires the rogue NIC to carry a default gateway (Host-Only usually doesn't). Extend the keyword filter if a real mis-pick is reported — `VPNRouter.Core/Services/SplitTunnelDriverProtocol.cs:462` — defer

### SDR / realtime-games cluster — ROOT CAUSE CONFIRMED 2026-07-02 (supersedes the older AWG-payload / Dota / WSAENOBUFS entries below)

Full research: `plans/sdr-research-realtime-games-nat-2026-07-02.md` (Fable, source-read of exact shipped fork) + `plans/mtu-fragmentation-robustness-2026-07-02.md` (my sing-tun/wireguard-go source-read) + action plan `plans/goal-sdr-games-action-plan-2026-07-02.md` + handoffs `plans/handoff-sdr-diagnosis-2026-07-02.md`. Memory `[[sdr-fullcone-nat]]`.

**Confirmed by live r10 diag (Dota `-console`, 2026-07-02):** SDR `RelayNetworkStatus: avail=Failed config=OK anyrelay=Failed` "Unable to communicate with ANY of 28" on **AWG** = pure UDP-relay-ping failure, **NOT DNS** (config=OK proves DNS/config-fetch works → r8 DNS fix validated). On **Hy2 the SDR-failure spam is ABSENT** (works). So it is **transport-dependent = H1**, not NAT/DNS/exit.

- [x] **P1 FIXED v2.45.0-r11 (a1a8997d): WSAENOBUFS send-retry in lx-core; live burst-effect pending tester** — **AWG single-socket WSAENOBUFS choke = the Dota/SDR root cause (H1, CONFIRMED).** SDR fires ~330 UDP relay-pings in a burst; all multiplex into the ONE physical AWG UDP socket; it hits `wsasendmsg: … buffer space … queue was full` (dozens/sec, live-confirmed 07-02), and wireguard-go **drops the batch without retry** → pings never leave → all relays unreachable. Fix options: (2C) **retry-with-backoff on ENOBUFS** in the lx-core bind (`submodules/wireguard-go/conn/bind_std.go` `Send`, `device/send.go`) + raise SO_SNDBUF (7MB now, `conn/controlfns_windows.go`) — touches lx-core, respect the 3 build patches [[awg-windows-lx-patches]]; and/or (2D) **auto-route game-UDP via a Hy2 sibling** when the active profile is AWG (Hy2 rides the burst over one QUIC → works). Immediate user workaround: **play on Hy2**. — `tools/build-singbox-lx.ps1` patch block — v2.45.0-r11+
- [x] **P1 FIXED v2.45.0-r11 (bbcd500d): AWG TUN = endpoint 1420** — **MTU regression: r8's `AwgEndpointMtu=1280` breaks SDR match-connect.** SDR sends UDP payloads ≤1300B (1328B IP) with NO PMTUD (GameNetworkingSockets#22); our 1280 cap drops them even once pings work. Stack has no fallback (source-confirmed: sing-tun system stack DROPS fragments `stack_system.go:571-575`, no MSS-clamp, no PMTUD-signaling; wireguard-go `DefaultMTU=1420`). Fix: endpoint 1280→**1420**, TUN=min(user,1420), no inversion — `VPNRouter.Core/Services/ConfigGenerator.cs:58` — v2.45.0-r11
- [x] **P2 RESOLVED v2.46.0 (Codex r6-r36 MTU cluster)** — **Path-MTU robustness (underlay <1500).** Asked for "path-MTU probe on connect OR a user MTU setting + guidance" — shipped as: `HealthCheck.CheckPathMtu` DF-probe (on-demand doctor/health-check, not per-connect — accepted), user MTU setting with auto-pick (DF-ping descending 1420→1240, refuses below the 1332 Steam-SDR floor), inline <1332 / >1420 warnings — `VPNRouter.Core/Services/HealthCheck.cs:169`
- [x] **NOTE** — **r10 `endpoint_independent_nat=true` (commit 745c1cc2) is a confirmed NO-OP** (parsed `option/tun.go:62`, never consumed; dead since sing-box 1.11; TUN already full-cone). Phase 0: rewrite the misleading comment `ConfigGenerator.cs:1127`, fix r10 GitHub notes, DON'T credit r10 for any "works" report. — v2.45.0-r11
- [ ] **P2** — Pending verification before final fix: run Dota `-console` on **VLESS** too (only AWG-fail + Hy2-ok logged so far); optional exit `tcpdump` (handoff 2) to formally rule out H2 — `plans/handoff-sdr-diagnosis-2026-07-02.md` — TBD

- [x] **P0 RESOLVED v2.44.3** — Auto-failover self-cancelling restart: fixed by `_sessionCts` — the failover restart runs under the session token, NOT the `Stop()`-cancelled probe token (source: `VpnEngine.cs:56-65` comment + implementation; audit batch-1 re-verified 2026-07-09) — `VPNRouter.Core/Services/VpnEngine.cs:1165`
- [x] **P0 RESOLVED v2.44.3** — "VpnEngine has zero start/stop/failover synchronization": `_lifecycleGate` (SemaphoreSlim) now serializes public StartAsync, public Stop() and the failover restart; Stop cancels `_sessionCts` before taking the gate so an in-flight restart aborts instead of resurrecting the tunnel (`VpnEngine.cs:56-66,291,497,722`). Residual NARROWED defect: ApplyAsync is not in the serialized set — tracked as the open P1 below (audit batch-1 #1, 2026-07-09)
- [x] **P1 RESOLVED v2.46.1 (2026-07-09)** — `ApplyAsync` not serialized by `_lifecycleGate`: now gated + session-token check (Stop cancels `_sessionCts` pre-gate, a queued-behind-Stop Apply bails instead of hot-reloading a torn-down sing-box / re-engaging true-split post-Disconnect). Pinned by `VpnEngineHotReloadLifecycleTests.Apply_AfterStop_ReturnsFalse_AndDoesNotRespawn` + `Apply_ConcurrentWithStop_NeverResurrectsTunnel` — `VPNRouter.Core/Services/VpnEngine.cs:579`
- [x] **P1 RESOLVED v2.44.3** — AutoFailover `ResetCycle()` has no production caller: `VpnEngine` OnConnected now calls `_failover?.ResetCycle()` (`VpnEngine.cs:1353`), pinned by `AutoFailoverRecoveryAndPersistTests.ResetCycle_AfterMaxAttemptsExhausted_RestoresFullPool` — `AutoFailoverEngine.cs:360`
- [x] **P1 RESOLVED v2.47.0-r3 (2026-07-10)** — clash_api exposed with no `secret`: now a per-install bearer secret auto-generated in `AppSettingsSane.EnsureSane` (desktop, persisted to config.yaml by `SettingsLoader`) / `AndroidStorage.GetClashApiSecret` (app-private SharedPreferences), emitted as `experimental.clash_api.secret` by `ConfigGenerator` + `CustomConfigInjector` (only when WE create the block), and sent by EVERY consumer — `ClashSingBoxApi` (Bearer header), `ClashLogStream` (WS `?token=`), `SingBoxManager.TryHotReload`/`IsClashApiAlive`, `ConfigSanityCheck.ProbeAsync`, `HealthMonitor`, desktop ConnStats, Android `AndroidSingBoxRuntime.IsRunningAsync` + the java stats-poller. Any installed app / hostile loopback XHR now gets 401. Pinned by `ClashApiSecretTests` (9) — `VPNRouter.Core/Models/VPNConfig.cs` `ClashApi.Secret`
- [ ] **P2** — clash_api secret edge: a user who hand-authors a custom-config `experimental.clash_api` block WITH their own `secret` keeps their block (we never graft ours onto a user-authored controller), but the in-app consumers still send the settings-backed secret → mismatch 401s liveness/hot-reload for THAT user (tunnel still works; status detection + hot-reload degrade to process-restart). Vanishingly rare (custom mode + hand-written clash_api + a secret) and they've taken over the API deliberately. Fix if reported: detect a user-authored clash_api secret at inject time and thread it to the consumer layer — `VPNRouter.Core/Services/CustomConfigInjector.cs` EnsureClashApi — TBD
- [x] **P1 RESOLVED v2.47.0-r3 (2026-07-10)** — LinuxFirewallManager / MacFirewallManager treated an EMPTY processNames list as "arm global kill-switch": a split-tunnel user whose 30s process scan timed out (empty list) had the whole host egress dropped on a crash. `IFirewallManager.CreateBlockRules` now takes an explicit `bool isFullTunnel`; the Linux/macOS managers arm on that INTENT, never on list emptiness (Windows ignores it — per-process rules), and `StartupPipeline` passes the real `settings.App.RoutingMode`. The nft/pf ruleset content + load path are UNCHANGED (only the arm decision moved from count→flag), so unit pins fully cover it. Pinned by the new `SplitTunnel_emptyList_scanTimeout_still_disarms` (Linux + Mac) + updated split tests — `Platform/Linux/LinuxFirewallManager.cs` / `Platform/macOS/MacFirewallManager.cs`
- [x] **P1 RESOLVED v2.44.3** — AutoFailover subscription-leak persist: switch-persist now RELOAD-FRESHes the on-disk settings and saves only the two selector fields, never `_store.Save(_settings)` with the transient resolver aggregate (source: `AutoFailoverEngine.cs:195-210` comment + implementation; pinned by `AutoFailoverRecoveryAndPersistTests`) — `AutoFailoverEngine.cs:202`
- [x] **P2 RESOLVED v2.47.0-r4 (2026-07-10)** — build-singbox-lx now asserts the checked-out HEAD == the pinned commit for BOTH the sing-box-lx fork AND the wireguard-go-awg2-lx submodule, fail-CLOSED (`Assert-GitHead` in .ps1 / `assert_git_head` in .sh) — a moved tag/branch or a dirty checkout can no longer silently bundle an unpinned tree. The go.mod replace-path half stays covered by the existing with_awg/with_xhttp Tags smoke assertion. `pwsh`/`bash -n` clean — `tools/build-singbox-lx.ps1` + `tools/build-singbox-lx.sh`
- [ ] **P2** — No SHA256 integrity pin of the bundled sing-box-lx.exe; build.ps1 `-SingBoxPath` override copies the binary with zero version/tag/checksum validation before bundling — `tools/build-singbox-lx.ps1:29` / `build.ps1:311` — TBD
- [ ] **P2** — AWG H4 reserved-byte receive-clear gate (v2.45.0-r6) is applied only to `conn/bind_std.go` (the active StdNetBind path for AWG). The WARP reserved clear in `conn/bind_windows.go` WinRingBind `receiveIPv4`/`receiveIPv6` (~L465/477) is NOT gated — defensive-only since sing-box-lx uses `NewStdNetBind` directly for AWG endpoints (winrio/WinRingBind never selected), but would clobber H4 if a future change ever routed AWG through WinRingBind — `tools/build-singbox-lx.ps1` (patch block) — TBD
- [ ] **P2** — The lx core carries 3 build-time source patches against the Go-1.26-affected `Leadaxe/wireguard-go-awg2-lx`: OOB nil-guard send+recv (golang/go#77875 WSAEFAULT) + the H4 receive-clear gate. Revisit/drop the two WSAEFAULT patches once the bundled Go toolchain is >= the version with the upstream fix (1.26.2/1.27); the H4 gate is permanent (fork behaviour) — `tools/build-singbox-lx.ps1` — TBD
- [ ] **P2** — LeakProtection.ValidateOutboundServersScopeAware never cross-checks the AWG peer endpoint IP (iterates only `config.Outbounds`; the AWG egress peer lives in `config.Endpoints[].Peers[]`) — defense-in-depth only, benign today — `VPNRouter.Core/Services/LeakProtection.cs:527` — TBD
- [x] **P1 RESOLVED v2.46.1 (2026-07-09)** — Custom-config raw JSON bypasses the awg/xhttp gates: `CustomConfigInjector` now detects AWG obfuscation fields (jc/jmin/jmax/s1-s4/h1-h4/i1-i5 on a wireguard endpoint; plain WG endpoints are official 1.11+ and stay ungated) + `transport.type=xhttp`, gates on `SingBoxFeatures` tags in BOTH `Validate` (actionable error at paste time) and `Inject` (runtime backstop throws instead of a sing-box FATAL); `Validate` also accepts a wireguard endpoint as the proxy egress. Pinned by `CustomConfigInjectorForkGateTests` (7) — `VPNRouter.Core/Services/CustomConfigInjector.cs`
- [x] **P2 RESOLVED v2.47.0-r3 (2026-07-10)** — SingBoxFeatures first probe could run synchronously on the UI thread (≤5s stall on an awg:// paste). Added `SingBoxFeatures.Prewarm()` (fires the cached, thread-safe probe on a background Task; no-ops when overrides set) called once at desktop startup (`Program.cs`, before OrphanCleanup). Pinned by `SingBoxFeaturesGateTests.Prewarm_WithOverridesSet_NoOps_AndDoesNotThrow` — `VPNRouter.Core/Services/SingBoxFeatures.cs`
- [ ] **P2** — SingBoxFeatures default probe reads the installed binary; on a dev box where that binary IS a sing-box-lx build, a test touching AwgAvailable/XhttpAvailable without an Override probes TRUE and inverts the default-closed contract (contained today by convention — all fork tests set overrides) — `VPNRouter.Core/Services/SingBoxFeatures.cs:67` — TBD
- [x] **P2 RESOLVED v2.47.0-r3 (2026-07-10)** — (Codex review) `LeakProtection` recognises the AWG `proxy` endpoint but didn't validate its contents. `ValidateConfig` now runs `ValidateProxyEndpoint` on the `proxy` endpoint (empty private_key / no local address / no peers / a peer missing public_key, endpoint address, or a valid port → actionable errors instead of a bare sing-box FATAL). Runs in both StartAsync + Apply. Pinned by `LeakProtectionAwgEndpointTests` (7) — `VPNRouter.Core/Services/LeakProtection.cs`
- [x] **P2 RESOLVED v2.45.0-r11 (bbcd500d): band-aid deleted entirely (DNS root cause fixed; Dota failure is WSAENOBUFS not DNS)** — (Codex review) game-DNS `ResolveGameDnsOffProxy` adds roblox/rbxcdn -> local-dns even when `StrictDns=true`, breaking "all DNS through VPN" — guard with `!strictDns` — `VPNRouter.Core/Services/ConfigGenerator.cs` BuildDns/game-DNS rule — TBD
- [x] **P2 RESOLVED v2.46.1 (2026-07-09, R1)** — (Codex review) DeepVerify AWG/XHTTP parity: BOTH halves shipped — real verify when the core carries the tag (AWG = top-level endpoint via the shipped `ConfigGenerator.BuildAmneziaWgEndpoint`, xhttp = mirrored transport with Vision-flow drop) AND typed `DeepVerifyFailurePhase.UnsupportedByVerifier` when it doesn't (mapper → Skipped → classifier reads "protocol untested", never a block). Pinned by `VlessDeepVerifierProtocolSupportTests` — `VlessDeepVerifier.cs`
- [x] **P2 RESOLVED v2.47.0-r3 (2026-07-10)** — (Codex review) UX: `SimpleInputDetector` now classifies `awg://`/`amneziawg://` as ServerUri (was "invalid link"; apply-time SingBoxFeatures gate still refuses on a non-lx core, like naive/dns-tunnel). The `ServerViewModel` half was STALE — the amneziawg/awg subtitle + ProtocolUseCase branches already shipped in v2.45.0-r3 (ServerViewModel.cs:555). Pinned by 2 new `SimpleInputDetectorTests` cases — `VPNRouter.App/SimpleInputDetector.cs`
- [x] **P2 STALE/RESOLVED (verified 2026-07-10)** — (Codex review) QUIC-reject suppression: current code (`ConfigGenerator.cs:135-139`) already keys it to `proxyIsUdpNative || proxyIsUdpNativeOutbound` (AWG endpoint OR a UDP-native Hy2/TUIC sole outbound), NOT `endpoints.Count>0` alone — fixed by the Hy2 sole-proxy work (v2.45.0/v2.46.0). No change needed — `VPNRouter.Core/Services/ConfigGenerator.cs:139`
- [x] **P2 RESOLVED v2.47.0-r3 (2026-07-10)** — (Codex review) ops script `plans/roblox-tester-exit-setup.sh` hardened: `umask 077` at top; hy2 installer downloaded to a temp file + sha256 printed for review/pinning (not `bash <(curl…)` straight to root); SSH-lockout guard detects real sshd port(s) via `sshd -T` + the current `SSH_CONNECTION` port and allows them before `ufw enable`; `chmod 600` on key.pem/config.yaml/tuic-config.json; secrets `tee`'d to a root-600 `/root/roblox-tester-secrets.txt` with a scrollback warning. `bash -n` clean — `plans/roblox-tester-exit-setup.sh`
- [x] **P2 → PROMOTED to CONFIRMED P1 in the SDR cluster at top of ## Open (2026-07-02)** — AWG endpoint can hit Windows UDP send-buffer exhaustion under game UDP load (`wsasendmsg: ... lacked sufficient buffer space or because a queue was full`); app-level fix is not clear from diag, needs live/core investigation without touching AWG v2.45.0-r6 blindly — `C:\Project\logs\codex-diag-20260630\awg\singbox-old-tail.log:14523` — TBD
- [x] **P1 → REFUTED 2026-07-02: the AWG tunnel forwards TCP+UDP fine (Roblox + Steam-TCP work live; SDR pings ride the tunnel); the bare-SOCKS-harness "stall" was DNS+MTU, not server forwarding — see SDR cluster above + [[awg-fulltunnel-payload-forwarding-gate]]** — AWG full-tunnel payload stalls after a successful peer handshake, REPRODUCED on 2 independent servers/keypairs: WINBRAT live test with `main-brat` (`10.66.0.22/32` -> `93.95.226.167:51822`) gets `received handshake response` / keepalives, but TCP via both TUN and a no-TUN SOCKS harness times out; SAME signature reproduced 2026-06-30 against a second, fully independent endpoint (`10.66.0.23/32` -> `104.194.156.93:51820`, different keys/jc/jmin/jmax/s1/s2/h1-h4) — handshake + bidirectional keepalives succeed, but a DNS-free raw-IP SOCKS probe (`curl --socks5 ... https://1.1.1.1/cdn-cgi/trace`) still times out (`http_code=000`) after the full 20s budget. Reproduction on 2 unrelated peer configs makes server-side `AllowedIPs` mismatch (silently drops real payload packets post-decrypt while empty keepalives — which carry no inner IP header — keep flowing) the leading hypothesis over a one-off VPS misconfig; `ip_forward`/MASQUERADE still possible. DNS-bootstrap fix itself is NOT implicated — 0 `DNS query loopback` in both runs, the DoH-bootstrap hostname resolves cleanly via real-NIC local-dns before the stall. SSH not available for either VPS; server proof/fix needs owner access — `plans/awg-server-forwarding-gate-2026-06-30.md`, `plans/goal-codex-awg-fulltunnel-dns-distribution-2026-06-30.md` Phase 1.1 evidence 2026-06-30 — TBD.
  **UNRESOLVED TENSION (2026-07-01)**: owner's live production app, same VPS (`93.95.226.167:51822`, `main-brat`), full-tunnel AWG connected — a browser leak-test through the tunnel got a real response (see the DNS-leak entry below) and Roblox works stably. That means TCP payload IS flowing through THIS AWG connection right now. The stalled-payload finding above used different-session AWG creds via a bare SOCKS harness (not the production TUN app) — those creds may simply be stale/mismatched (same class of problem as the xhttp path going stale below), not proof the server is universally broken. Don't treat the P1 above as still-confirmed without re-testing against the CURRENT production `main-brat` AWG credentials from `config.yaml` before acting on it either way.
- [ ] **P2** — Server-side: xhttp VLESS inbound on `93.95.226.167:9443` (path `/U4YILqMxZg2N6sIKQFLt4w`, `main-brat`) returned `v2ray-xhttp: unexpected status: 404 Not Found` on EVERY request (dozens of distinct DNS/connection attempts over the full 17.5s warm-up window, 0 successes) — client-side TLS+Reality handshake succeeded (got a real HTTP 404 back), config-gen verified correct (`path` round-trips byte-exact from the URI), so this was the server's xhttp inbound not routing this path, not a VPNRouter/sing-box-lx bug at the time. Owner reported the VPS got fixed and a connection briefly succeeded — **REOPENED same day**: owner now reports xhttp still does NOT connect from the VPNRouter app itself, while a non-VPNRouter iPhone client connects fine against the same server. No fresh diagnostics captured yet for this current failure mode (the 404 evidence above predates the VPS fix and no longer explains today's symptom) — needs a new diag bundle taken immediately after a fresh failed connect attempt, and a comparison of how sing-box-lx's xhttp client (mode/padding/headers) differs from whatever client the iPhone side used — `C:\Project\logs\VPNRouter-diagnostics-20260701-003612.zip` (stale, pre-fix) — TBD
- [ ] **P1** — DNS leak under AWG full-tunnel: owner connected `main-brat` (AWG, `93.95.226.167:51822`, `routing_mode: full`, confirmed "Подключено [full]"), ran a browser IP/DNS-leak test — IP correctly showed the tunnel's Iceland exit (no IP leak), but the DNS-leak panel of the same test showed 3 resolvers (`195.2.238.4`, `195.2.239.4`, `195.2.238.58`, all "Russia, Trytek LLC", 300/300 hits) that match NONE of the configured DNS servers (`vpn-dns`=AdGuard/local proxy-detour, `local-dns`=1.1.1.1 dns-direct) — real DNS leak to what looks like the user's local/ISP resolver despite `route.rules[protocol=dns, action=hijack-dns]` supposedly catching all DNS traffic. Current toggles at time of test (`config.yaml`): `strict_dns: true`, `force_ipv4_only: true`, `flush_dns_on_start: true`, but `dns_leak_lockdown: false` (the firewall port-53 backstop was OFF, so nothing would have blocked/surfaced this leak even as a hard failure). This is exactly the open question in `plans/tz-codex-awg-dns-leakprotection-audit-2026-06-28.md` item 1 ("does hijack-dns/dns.final actually work the same for an AWG `endpoints[]` wireguard block as for a normal VLESS outbound, on a live tunnel") — now with live reproduction. Suspected same root cause as the Dota2 entry below — TBD
- [x] **P2 → DIAGNOSED 2026-07-02: NOT DNS (live console showed config=OK), it is H1 (AWG single-socket WSAENOBUFS choke); Hy2 works — see CONFIRMED SDR cluster at top of ## Open. The "investigate DNS first, don't chase WSAENOBUFS" guidance below is now inverted.** — Dota2 matchmaking region-latency test fails for EVERY region simultaneously while connected via AWG full-tunnel (`main-brat`), generic client error "Не удалось вычислить задержку. Проверьте интернет-соединение." Owner: Roblox works stably on the same AWG connection; suspects DNS. All-regions-at-once (not just distant ones) is consistent with a systemic DNS-resolution failure for Dota2's own region-ping mechanism rather than per-region network timeouts — likely the SAME root cause as the DNS-leak entry above (if Dota2 resolves matchmaking hostnames via the leaking path, and/or the resulting IPs hit Russia's TSPU foreign-IP freeze per the Roblox-277 RCA pattern). Investigate the DNS-leak entry FIRST and retest Dota2 after — don't chase this as an independent WSAENOBUFS-class bug (see the existing P2 WSAENOBUFS entry above) until the DNS hypothesis is ruled out — TBD
- [x] **P1** — (Codex perf/stability hunt, validated) External/3rd-party sing-box treated as VPNRouter (false "connected" + takeover kills an unrelated tunnel) — RESOLVED v2.45.0 (`ProcessOwnership` image-path gate; commit b60d33fe)
- [x] **P1** — (Codex perf/stability hunt, validated) StrictDNS failover commits state + event before hot-reload success — RESOLVED v2.45.0 (commit-only-on-reload-success reorder; commit f22b7351)

- [x] **P2 RESOLVED v2.47.0-r4 (2026-07-10)** — Deep-verify-all on slow hardware gave false "protocol untested": the flat 1500ms SOCKS-bind warmup was too short when `MaxConcurrency=5` sing-box processes contend for CPU on a slow VM. The wait is now adaptive — `EffectiveSocksBindWait = SingBoxWarmup + 300ms × (MaxConcurrency−1)` (2700ms at the default 5), still bounded by the 12s OverallTimeout so a dead spawn never hangs. Pinned by `VlessDeepVerifierTests.EffectiveSocksBindWait_ScalesWithConcurrency` (3) — `VPNRouter.Core/Services/VlessDeepVerifier.cs`
### "VPNRouter deleted after reboot" (Windows) — investigated 2026-07-09, root cause = AV, NOT app self-deletion
- [x] **CORRECTION** — earlier "SelfRepair guts the install" and "RestoreSnapshot move-order guts app/ at boot" were BOTH refuted on re-check: (a) `install.ps1` has `$ErrorActionPreference="Stop"` (line 48) + downloads/sha-verifies BEFORE the wipe, so a network failure halts before touching `app/`; (b) `RestoreSnapshot` is only ever called from the running `App.exe` which lives inside `app/`, so `Directory.Move(app→stage)` fails FIRST (live-proven on brat: renaming a dir with a running exe throws "used by another process") — the dangerous `Move(bak→app)` window is unreachable. The brat "gutting" I saw was my own test overlay command hitting locked files, not either code path.
- [ ] **P1 — CI PREP LANDED 2026-07-10, ENROLLMENT USER-GATED** — Root cause (evidence-based): the binaries are UNSIGNED (`Get-AuthenticodeSignature` = NotSigned on App/sing-box/Service) + do TUN/process-scan/firewall, so an AV quarantines them on a reboot scan → the user sees "deleted". Mitigations landed 2026-07-09 (`antivirus-integrity.txt` diag capture; install.ps1 verified Defender exclusion + manual guidance). **Real fix = code-signing.** Now PREPPED: `.github/workflows/sign-windows.yml` (SignPath.io Foundation, free/OSS — manual `workflow_dispatch`, fail-fast until secrets exist, mirrors sign-android) + turnkey enrollment runbook `plans/code-signing-signpath-runbook-2026-07-10.md`. REMAINING = the SignPath OSS enrollment itself (account + application submission — owner-only, can't automate) + adding 5 repo secrets, then run the signer per ship. — `.github/workflows/sign-windows.yml` / `packaging/windows/install.ps1`
- [x] **P2 RESOLVED pending release (2026-08-01)** — (perf hunt F2 follow-up) Conn-stats poll had no visibility/minimized throttle — it polled `/connections` every 2s even when the stats line was off-screen or the window was minimized (streaming parse already landed in F2). Fixed: `MaybePollConnStats` now reuses `GetMainWindow()` and returns early for a null / hidden (`!IsVisible`) / minimized window before the in-flight `CompareExchange` + API dispatch (visible + non-minimized behavior unchanged). Pinned by `ConnStatsVisibilityThrottleTests.MaybePollConnStats_GuardsVisibilityBeforeInFlightAndApiCall` — `VPNRouter.App/ViewModels/MainWindowViewModel.ConnStats.cs`
- [x] **P2 RESOLVED v2.46.1 (2026-07-09, R5)** — (perf hunt F3 follow-up) AutoSelect `GetGroupNowAsync("proxy")` now polled every 3rd stats tick only (first tick immediate so the label appears right after connect; tick resets on disconnect); Windows characterization hash re-pinned as the entry predicted, Linux pin follows from CI — `VPNRouter.App/ViewModels/MainWindowViewModel.ConnStats.cs`
- [ ] **P2** — (test-harness hermeticity follow-up) On a non-elevated Windows dev session the full xUnit suite writes through `AppPaths.DataDir` to the real `%ProgramData%\VPNRouter`; after the shipped SEC-2 ACL hardening 23 tests fail with `UnauthorizedAccessException` (e.g. `VpnEngineDnsLockdownLifecycleTests`, `VpnEngineLifecycleTests`, `SingBoxManagerStateMachineTests`, `MainWindowViewModelWgturnTests`) instead of using an isolated temp data dir. Production ACL must NOT be weakened; likely fix is suite-level early `AppPaths.OverrideDataDir` or per-test injection, to be designed separately — `VPNRouter.Core/AppPaths.cs` — TBD

### dependency-review 2026-07-09 — executable-code download verification
- [x] **P1 RESOLVED v2.47.0-r4 (2026-07-10)** — TgProxyUpdater installed a Python interpreter + PyPI wheels (cffi/cryptography = compiled C/Rust) under the user's account with ZERO integrity check. Now fail-closed: wheels verified against PyPI's own `urls[].digests.sha256`; the python.org embeddable pinned to a captured-from-canonical sha256 constant (`PythonZipSha256`, recompute on version bump). Single primitive `VerifyPinnedSha256Static`, 4 pins in `TgProxyDigestVerifyTests` — `VPNRouter.Core/Services/TgProxyUpdater.cs` (dep-review P1-2)
- [ ] **P2** — ZapretUpdater verifies only the download SIZE of `Flowseal/zapret-discord-youtube` winws.exe (runs with admin); documented trade-off (auto-fresh bypass strategies). Action-minimum DONE 2026-07-10: trust root + size-only-verification + residual risk now documented in `tools/native-deps.md` (Trust roots section). REMAINING option (this entry): known-good tag pin in the VPNRouter release + explicit user confirm before upgrading to an unverified tag — `VPNRouter.Core/Services/ZapretUpdater.cs` — dep-review P2-1, TBD
- [ ] **P2** — WgturnUpdater's `expectedSha256` path is wired but `ResolveAssetFor` always returns null (upstream — our own repo — publishes no sidecar). Fix on the PUBLISHING side: emit `.sha256` sidecars in the wgturn release, then have ResolveAssetFor read them — `VPNRouter.Core/Services/WgturnUpdater.cs` — dep-review P2-2, TBD (cross-repo)
- [x] **P1 LANDED on main 2026-07-10 (net10 CI-green; ships in v2.47.0)** — .NET 8 EOL 2026-11-10: net8.0 → net10.0 TFM migration DONE (commit adb1d215). All desktop projects (App/CLI/Service/Tests/PoolAggregator/VpnRouterTestMcp) + root global.json → net10.0/10.0.301; Android was already net10.0-android36.0. **The spike's CS0122 root cause was NOT "stragglers"** — it was three `net8.0`-CONDITIONED MSBuild blocks in `VPNRouter.Core.csproj` that silently stopped applying on net10.0: the `<InternalsVisibleTo Include="VPNRouter.Tests">` ItemGroup (→ CS0122 on the internal SplitTunnelDriverProtocol/NicSnapshot/StartupHostInternal), the `PLATFORM_WINDOWS` DefineConstants PropertyGroup (would have SILENTLY compiled out every `#if PLATFORM_WINDOWS` block — firewall/TUN/ETW/registry — a functional break the 0-error build hides), and the Windows-only package ItemGroup. All rescoped net8.0→net10.0 / net8.0-android→net10.0-android. System.Management + Microsoft.Win32.SystemEvents + System.Drawing.Common 8.0.0→10.0.0. NuGet restore clean (0 NU1102 — no other package bumps needed). Characterization hash re-pinned (reflection-derived → net10 metadata shifts it, zero MVM source change). CI setup-dotnet 8.0.x→10.0.x (6 workflows). Verified: full sln 0 errors, 2550 local tests pass (28 = dev-box ProgramData-lock baseline), **Linux CI test+grep GREEN on net10**. A 14-agent adversarial validation workflow (2026-07-11) confirmed the migration itself clean (all TFM conditions rescoped, char re-pin legit — 1480 members identical net8↔net10, CI clean) but found + FIXED two blockers: **B1** `.mcp.json` still pointed at the net8 MCP dll path (→ net10.0-windows apphost); **B2 (critical)** the in-app update zip is APP-ONLY (no .NET runtime DLLs — only the full install zip has them), so a net8 user auto-updating to net10 would run net10-compiled app DLLs on the FROZEN net8 coreclr → guaranteed crash of the WHOLE installed base (the GUI trampoline's app-DLL-hash self-repair can't see a uniform runtime/app mismatch). Fixed in build.ps1: the runtime DLLs now travel in the update `_bootstrap/` (same rule as sing-box/cronet; the Go trampoline xcopies `_bootstrap/*` wholesale, so they apply). Also hardened the 6 CI workflows setup-dotnet `10.0.x`→`global-json-file`. **-r10 SHIPPED 2026-07-11** (commit a12c0bb3; 14 desktop assets, prerelease, Latest restored to v2.46.0, r9 release deleted; tag-CI green incl. Auto-Update Integration Test + Verify Release Integrity; commit-CI fully green). **B2 fix CONFIRMED at artifact level**: the r10 update-zip (71 MB, was app-only) now carries the full net10 runtime in `_bootstrap/` — coreclr.dll, clrjit.dll, System.Private.CoreLib.dll, hostfxr.dll, hostpolicy.dll, mscorlib.dll, clrgc.dll — verified by unzip listing. **windows-brat live-update gate — PASSED 2026-07-11 (B2 fix PROVEN, both production paths).** Clean net8 r9 install (coreclr 8.0.2426) → cross-major update to net10 r10:
(1) **trampoline-apply path** — r10 update-zip staged in appDir → real VPNRouter.GUI.exe xcopied `_bootstrap/*` → coreclr 8.0→10.0, App.exe (r10, commit a12c0bb3) launched + rendered full UI, clean log;
(2) **helper.cmd path (the v2.31.7 bug class)** — real `CLI test-update --staged-dir` → ApplyAsync → helper.cmd cmd.exe `xcopy _bootstrap\* → appDir` exit=0, coreclr 8.0→10.0 (relaunch suppressed only by VPNROUTER_CI=1); launched from the helper-produced appDir → net10 App.exe rendered.
Both paths: runtime major swapped, app boots + renders (Avalonia 12 on net10), no [ERR]/Exception/FATAL. Without the B2 fix the net10 app DLLs would have run on a frozen net8 coreclr → instant crash of the whole installed base. **B2 CLOSED.** — dep-review P1-1

## Resolved (history)

- [x] **P0 RESOLVED v2.48.0-r4 (2026-08-03)** — Android release CI accepted and published debug-signed APKs because the .NET Android publish command supplied the production keystore properties without the required `AndroidKeyStore=true`; rotating the GitHub secrets alone could not affect signing. Both temporary incompatible R4 APK asset pairs were deleted before handoff. Fixed by enabling the production keystore and pinning the finished APK certificate SHA-256 before upload (PR #111). The final R4 APK matches production cert `6e50af0f…45a221`, passed SHA256 verification, update-installed over `2.48.0-r1`, and launched as `2.48.0-r4` on device. GitHub signing secrets were restored from the verified encrypted backup; plaintext recovery files were removed after use — `.github/workflows/build-android.yml`

- [x] **P2 RESOLVED after v2.48.0-r4 (2026-08-03)** — the CI pre-push verifier could terminate before its own exit-code handling on Windows PowerShell 5.1 because native `gh` stderr becomes a terminating `ErrorRecord` while `$ErrorActionPreference=Stop`. Removed the redundant `gh auth status` preflight and scoped `Continue` only around the real `gh api` authentication/availability check, then restored the caller preference and preserved fail-closed exit 3. Verified both authenticated success and a forced API-error path — `tools/verify-last-commit-ci.ps1`

- [x] **P0** — Auto-failover false-positive teardown of a healthy connection (post-start delay-test 503 → tore down a working server) — RESOLVED v2.44.2 (warmup-confirmed gate, `VpnEngine.ShouldAutoFailoverAfterProbe`)
