# Open defect ledger

Canonical open-defect ledger read by `tools/check-open-p0.ps1` (the cut-stable
P0 gate, audit item 7, 2026-06-25). The `bug-hunt` skill appends survivors to
the **## Open** section below; the cut-stable gate (`cut-stable` skill pre-flight
condition 6.5) **BLOCKS a stable cut** while any `- [ ]` P0/P1 remains open here,
unless `tools/check-open-p0.ps1 -Waive '<reason>'` is run for that specific cut.

This file exists because a P0 found by the adversarial bug-hunt, written down and
deferred, reached v2.44.0/.1 stable and bit a user (auto-failover teardown, diag
20260624-235243) — nothing connected the deferred-defect record to the cut gate.

**Resolve** an entry: change `- [ ]` to `- [x]` and append `RESOLVED vX.Y.Z`.
Only the `## Open` section is gated; `## Resolved` is history. Keep entries to one
line: `- [ ] **P0** — <symptom> — <file:line or plan ref> — <target version>`.

## Open

### Qwen follow-up audit — 2026-07-31

Durable import of a Qwen follow-up mini-audit that previously existed only in
chat (no persisted record). Entries may be candidate/unverified; none are
confirmed defects and none are resolved. P0/P1 gating semantics unchanged (see
file header).

- [ ] **P1 IN PROGRESS** — QF-1: remote-only windows-brat verifier replaces the local VpnRouterTestMcp; implementation exists only on the open PR #88 branch (not merged) and live end-to-end verification is not yet evidenced — commit 1453fc16 / PR #88 — next rolling candidate
- [ ] **P2 CANDIDATE** — QF-2: screenshot matrix via Qwen Vision for Simple/Advanced × RU/EN × Light/Dark × DPI 100/125/150 × narrow width × Disconnected/Connected/Error/Update × top/bottom of scroll pages — depends on QF-1 — TBD
- [ ] **P2 CANDIDATE** — QF-3: add only ~20 stable AutomationId values for critical E2E controls after scenarios prove they are needed (current count 0) — depends on QF-1 — TBD
- [ ] **P2 CANDIDATE** — QF-4: measurement-first perf profiles (cold start, connect, 5-minute idle/minimized) with dotnet-counters, then dotnet-trace only for a proven hotspot; known candidate = hidden-window connection-stats polling — TBD
- [ ] **P3 DEFERRED** — QF-5: evaluate removal of ~1450 lines duplicated App localization + ~360 lines binding wrappers only after a UIA/headless safety net exists; user impact low, regression risk nontrivial — TBD
- [ ] **P1 IN PROGRESS** — QF-6: live run evidence 2026-07-31 — `tools/testvm-control.ps1` and `tools/brat-verify.ps1` resolved the DPAPI-encrypted credential files only from the current worktree root, so any verification invoked from a task worktree failed before the VM identity check. Fix: `Resolve-CredentialFile` helper duplicated in `tools/brat-verify.ps1` + `tools/testvm-control.ps1` (local-first checkout check, then primary-worktree fallback via `git rev-parse --git-common-dir`, fail-closed throw if neither exists; no credential copying). Contract test: `VPNRouter.Tests/BratVerifierContractTests.cs` `Scripts_ResolveCredentials_LocalFirst_WithPrimaryWorktreeFallback_AndNeverCopyThem`. Pending live E2E before RESOLVED — PR #88 before live E2E
- [ ] **P1 IN PROGRESS** — QF-7: pre-commit Gate 1/Gate 2 false-green — the Bash hook enables `set -e` (`.githooks/pre-commit:31`) and `set -u` (`:32`) but NOT `set -o pipefail`, while both build/test gates pipe dotnet into `tail`: Gate 1 `if ! dotnet build VPNRouter.sln -c Release --nologo --verbosity quiet 2>&1 | tail -5; then` (`:107`) and Gate 2 `if ! dotnet test VPNRouter.Tests/VPNRouter.Tests.csproj -c Release --nologo --verbosity quiet \ --filter "$TEST_FILTER" 2>&1 | tail -3; then` (`:135-136`). A pipeline's exit status is the LAST command's (`tail`), so a failing `dotnet` is masked by `tail`'s zero exit. Evidence (commit 20d141d6, this dev box): `dotnet` exited nonzero because SDK 10.0.301 was unavailable in PATH, yet `tail` returned zero and the hook printed "Gate 1: build clean." / "Gate 2: scoped tests green." and allowed the commit. Impact: ANY build/test failure (missing SDK, compile error, test failure) can be falsely green locally, defeating the methodology §1 hard gate. Fix: `set -o pipefail` added at `.githooks/pre-commit:33`; contract test `VPNRouter.Tests/ReleaseToolingContractTests.cs` `PreCommitHook_EnablesPipefailBeforeGate1Build`. Verified 2026-07-31: local fail-closed reproduction passed (system dotnet 8 / missing required SDK → hook EXIT=1, both Gate 1 and Gate 2 failures reported); local valid-path reproduction passed with SDK 10.0.301 (build 0 errors, 185 tests passed); remote PR CI for commit 9a589fac passed test/grep/go-test-windows. Pending merge of PR #88 before RESOLVED
- [ ] **P1 IN PROGRESS** — QF-8: verifier transport still pinned to the retired LAN endpoint after the windows-brat infrastructure migration — Evidence 2026-07-31: `tailscale status` shows the peer online as `windows-brat` / `windows-brat.tail9fd337.ts.net` / `100.115.182.0`; TCP 5985 on the Tailscale CGNAT address is reachable and `tailscale ping` succeeds via DERP `waw`, while the old LAN `192.168.0.106:5985` and the Proxmox host `192.168.0.169:8006` are both unreachable. The verifier, skill, tests and root instructions still hard-pin the dead LAN address: `tools/brat-verify.ps1:71` (`$BratIp = '192.168.0.106'`), `:73` (credential file name `.testpc-cred-192.168.0.106.xml`), `:3`/`:86`/`:437` (identity check phrased around `192.168.0.106`); `tools/testvm-control.ps1:48` (`$PveHost = '192.168.0.169'`), `:51` (`$VmIp = '192.168.0.106'`), `:123` (Proxmox API `https://${PveHost}:8006`), `:148-163` (`ensure-ready` powers VM 100 via Proxmox then waits for WinRM on `$VmIp:5985`); contract pins `VPNRouter.Tests/BratVerifierContractTests.cs:10` and `:70` (`Assert.Contains("192.168.0.106", …)` against script + skill); skill copy `.agents/skills/post-ship-mcp-verify/SKILL.md:3,8,39-40,138`; root instructions `AGENTS.md:88,129,197`. Impact: the mandatory post-ship remote brat E2E (rules #1a / #12) stops at the unreachable LAN endpoint even though the correct VM is reachable through Tailscale — mandatory post-ship E2E blocked after infrastructure endpoint migration. Target PR #88: keep the fixed target + WINBRAT identity check, switch the transport to the fixed Tailscale peer (`windows-brat.tail9fd337.ts.net` / `100.115.182.0`), reuse the existing DPAPI credential without copying it, and make `ensure-ready` accept an already-reachable Tailscale WinRM endpoint before falling back to the Proxmox power path. Implementation 2026-07-31 (branch qwen/remote-brat-verifier): transport switched to the fixed Tailscale peer 100.115.182.0 in `tools/brat-verify.ps1` (`$BratIp`) and `tools/testvm-control.ps1` (`$VmIp` default; `ensure-ready` probes Tailscale WinRM 5985 before its first Proxmox `Get-VmStatus`, while status/start/stop remain PVE-backed); legacy credential filename `.testpc-cred-192.168.0.106.xml` retained (no secret copy/migration) and passed explicitly to `deploy-to-testpc.ps1 -Credential`; `SKILL.md`/`AGENTS.md`/`CLAUDE.md`/`deploy-to-testpc.ps1` updated (CLAUDE infra table keeps the LAN address only as historical/fallback); contract test `VPNRouter.Tests/BratVerifierContractTests.cs` (new endpoint + WINBRAT, deploy `-Credential`, ensure-ready probe ordering, caller-supplied-host protections). Commit 90c886a2 pushed to PR #88; remote CI test/grep/go-test-windows passed. Live phase 2 2026-07-31: `tools/testvm-control.ps1 -Action ensure-ready` PASS with exact output "WinRM reachable at 100.115.182.0:5985" (no Proxmox needed); `tools/brat-verify.ps1 -Action identity` then failed closed before deploy/UI/logs with ServerNotTrusted because this non-elevated dev process cannot add the exact host 100.115.182.0 to local WinRM TrustedHosts (Start-Service WinRM / Set-Item denied). Remaining one-time prerequisite: an elevated exact-host TrustedHosts configuration for 100.115.182.0 (no wildcard), then rerun identity/deploy/screenshot/logs. Pending live identity/deploy/screenshot/log verification before RESOLVED — PR #88
- [ ] **P1 IN PROGRESS** — QF-9: the tracked `.claude/skills/post-ship-mcp-verify/` mirror still carries the retired local-MCP runbook while PR #88 migrated only the `.agents/` copy to the remote-only windows-brat contract — Evidence 2026-07-31: `.claude/skills/post-ship-mcp-verify/SKILL.md:3` (frontmatter pins the dead LAN target `192.168.0.106`), `:14` (STOP banner: "ALL post-ship verification runs on the test VM `windows-brat` (192.168.0.106) over WinRM"), `:19` (cred `.testpc-cred-192.168.0.106.xml`), `:63` (Phase 2 invokes its own `.claude/skills/post-ship-mcp-verify/scripts/post-ship-install-launch.ps1`), `:80,82,105` (Phase 3/4 drive the app via `mcp__vpnrouter-test__*` tools), `:114` (Phase 5 invokes `.claude/skills/post-ship-mcp-verify/scripts/post-ship-collect-logs.ps1`); the obsolete tracked scripts `.claude/skills/post-ship-mcp-verify/scripts/post-ship-install-launch.ps1` + `scripts/post-ship-collect-logs.ps1` and the six `references/checklist-*.md` files remain in the mirror, and the file never mentions `tools/brat-verify.ps1`, WINBRAT, or the Tailscale peer `100.115.182.0`. PR #88 (branch qwen/remote-brat-verifier) updated only `.agents/skills/post-ship-mcp-verify/SKILL.md` to `tools/brat-verify.ps1` + Tailscale, leaving the `.claude` copy stale, while `CLAUDE.md:210` (rule #12 "Реализация:") still explicitly points Claude agents at `.claude/skills/post-ship-mcp-verify/SKILL.md` even though `:206` describes the new remote-only WINBRAT / `tools/brat-verify.ps1` contract. Impact: a Claude agent following CLAUDE.md rule #12 lands on the stale mirror and can run the retired unsafe local-MCP workflow (seizing the operator's mouse/screen) or target the unreachable LAN `192.168.0.106`, defeating the new remote-only fail-closed safety contract (rules #1a/#12) and the QF-8 endpoint migration. Target PR #88: make the `.claude` SKILL.md + `references/` checklists match the `.agents` remote-only content, delete the obsolete `.claude` `scripts/post-ship-install-launch.ps1` + `scripts/post-ship-collect-logs.ps1`, and add a contract test that fails on any `.agents` vs `.claude` post-ship-mcp-verify mirror drift. Implementation 2026-07-31 (branch qwen/remote-brat-verifier): `.claude/skills/post-ship-mcp-verify/SKILL.md` and all six `references/checklist-*.md` made byte-for-byte identical to the `.agents` copies; obsolete `.claude` `scripts/post-ship-install-launch.ps1` + `scripts/post-ship-collect-logs.ps1` deleted with no replacements (`tools/brat-verify.ps1` is the single driver); contract test `VPNRouter.Tests/BratVerifierContractTests.cs` `ClaudeMirror_IsByteIdenticalToAgentsSkill_AndObsoleteLocalScriptsAreGone`. Implementation is in pushed commit 90c886a2 (PR #88); local tests and remote CI passed. Pending PR #88 merge before RESOLVED
- [ ] **P2 IN PROGRESS** — QF-10: `ClaudeMirror_IsByteIdenticalToAgentsSkill_AndObsoleteLocalScriptsAreGone` claims byte-for-byte mirroring but compares via `Read`/`File.ReadAllText`, so different BOM/encoding bytes can decode to equivalent strings and escape the contract — Evidence 2026-07-31 (Codex validation): the test name and QF-9 implementation note both assert "byte-for-byte identical" yet the assertions use the string-returning `Read` helper (`File.ReadAllText`), which normalises BOM and encoding differences; two files with identical text but different BOM (UTF-8 BOM vs no-BOM) or encoding (UTF-8 vs UTF-16LE) would pass the string comparison while differing on disk. Impact: the `.agents`↔`.claude` mirror contract is weaker than advertised — a future editor saving with a different BOM/encoding would introduce silent on-disk drift that the contract test cannot catch, undermining the QF-9 safety guarantee. Fix (PR #88): compare actual file byte arrays (`File.ReadAllBytes`) for the SKILL.md pair and all six checklist pairs; keep the two obsolete-script nonexistence assertions unchanged. Implementation 2026-07-31 (branch qwen/remote-brat-verifier): `VPNRouter.Tests/BratVerifierContractTests.cs` `ClaudeMirror_IsByteIdenticalToAgentsSkill_AndObsoleteLocalScriptsAreGone` now uses a `ReadBytes` helper (`File.ReadAllBytes`) for all seven mirror comparisons. The actual-byte ReadBytes contract is in pushed commit 90c886a2 (PR #88); local tests and remote CI passed. Pending PR #88 merge before RESOLVED
- [x] **P1 RESOLVED 2026-08-01** — QF-11: WinRM authentication to windows-brat fails closed with AccessDenied despite reachable transport and correct TrustedHosts — Evidence 2026-08-01: after an elevated exact-host TrustedHosts entry for 100.115.182.0 (no wildcard) was confirmed present, CI passes, and `tools/testvm-control.ps1 -Action ensure-ready` reports "WinRM reachable at 100.115.182.0:5985", `tools/brat-verify.ps1 -Action identity` still fails closed with AccessDenied before deploy/UI/logs. The resolved DPAPI credential is `C:\Project\VPNRouter\.testpc-cred-192.168.0.106.xml` (username `tester`, last modified 2026-06-20); a diagnostic attempt using the same secure password with the qualified username `WINBRAT\tester` also returns AccessDenied. Verified 2026-08-01 (follow-up): (1) the same DPAPI `tester` credential also returns AccessDenied over the previously validated Mac SSH tunnel to the old LAN WinRM endpoint `localhost:15985` → `192.168.0.106:5985`, excluding Tailscale transport and TrustedHosts as the cause; (2) the Proxmox API is reachable through a temporary Mac tunnel — VM 100 is running, config `agent=1`, uptime ~436000 s, but `POST agent/ping` returns "QEMU guest agent is not running"; (3) a graceful Proxmox `qmreboot` was attempted — its task result is stopped / "VM quit/powerdown failed - got timeout" and the log says guest-ping timed out; VM uptime kept increasing, so no reboot occurred; (4) no Tailscale peer advertises the 192.168.0.0/24 subnet, and tester SSH to `debian-xfce` is denied by publickey, so there is no non-destructive alternate admin path. Verified 2026-08-01 (follow-up 2, owner-supplied read-only diagnostics): (1) Proxmox access is restored through the persistent route Windows → Tailscale → pavels-mac-mini `100.116.97.112` → LAN → Proxmox `192.168.0.169:8006`, exposed locally as `https://127.0.0.1:8006`; (2) `tools/testvm-control.ps1 -Action status -PveHost 127.0.0.1` returns `VM 100 status: running` using the existing DPAPI token — no secret was requested or printed; (3) windows-brat Tailscale ports 5985 WinRM, 3389 RDP, and 445 SMB are all reachable; (4) Proxmox reports Name=windows-brat, Status=running, UptimeSec=438093, AgentConfig=1, OsType=win10, while agent/ping still reports "QEMU Guest Agent not running". Verified 2026-08-01 (follow-up 3, owner-authorized recovery): (1) the owner explicitly authorized Codex to perform the recovery, including a hard reset of the dedicated test VM 100, superseding the earlier owner-prohibited state; (2) before the reset, the exact destructive target was re-verified through the Proxmox API as vmid=100, name=windows-brat, status=running; (3) `POST /status/reset` returned a `qmreset` task whose status later reports stopped / ExitStatus OK / TASK OK, and WinRM on 100.115.182.0:5985 dropped and then returned, confirming the guest reset — Proxmox `uptime` is QEMU process lifetime and does NOT reset on `qmreset`, so the earlier uptime-based inference that no reset occurred was invalid; (4) after the reset, Proxmox agent/ping still reports "QEMU Guest Agent not running" and `tools/brat-verify.ps1 -Action identity` still fails closed with AccessDenied, so the authentication blocker is unchanged by the reset; (5) the Proxmox API token can successfully create an ephemeral VM 100 vncproxy ticket (port 5900, ticket present), but no local remote-viewer/vncviewer client is installed and mstsc is intentionally rejected because it would violate the remote-only / no-devbox-UI rule. Disposition remains P1 CONFIRMED / LIVE BLOCKER. Impact: the mandatory R3 remote E2E (rules #1a / #12) remains blocked — QF-8 transport is resolved but authentication is not. Root cause not yet confirmed: stale password or account state versus WinRM authorization policy. Next attempt: a one-shot, fixed-target, headless RFB console client under gitignored artifacts, using the ephemeral vncproxy ticket only in memory to inspect/recover the local `tester` account without displaying secrets or touching local UI — temporary operational recovery code, not product/PR scope, to be removed after use. No password or token is recorded in this backlog — PR #88. Verified 2026-08-01 (operational evidence, not a new defect): validation found `artifacts/` is NOT gitignored (only `.r-publish/` is, `.gitignore:147`), so the temporary recovery code/screenshots must live under the already-gitignored `.r-publish/brat-recovery/` (not `artifacts/`) to prevent accidental commit of recovery material; the fixed-target headless RFB capture client is therefore placed at `.r-publish/brat-recovery/pve_rfb_capture.py`. Verified 2026-08-01 (operational evidence, not a new defect): validation also found two late Qwen timeout sessions had created untracked temporary files under the non-ignored `artifacts/brat-recovery/` (`pve_rfb_capture.py`, `pve_rfb_console.py`, `__pycache__/pve_rfb_capture.cpython-311.pyc`), and the new ignored capture client discarded any WebSocket bytes received after the HTTP upgrade header in the same TLS read and did not validate `Sec-WebSocket-Accept`/subprotocol. Impact: accidental commit risk (recovery material under a tracked path) plus a possible hang and weak upgrade validation. Cleanup of the three stray `artifacts/brat-recovery/` files and the capture-client fix (preserve coalesced post-header bytes; validate HTTP 101 / `Sec-WebSocket-Accept` / binary subprotocol) occur before any ticket is supplied or the client is executed. Verified 2026-08-01 (operational evidence, not a new defect): the fixed-target headless VNC handshake/auth/capture succeeded (`QEMU (windows-brat)`, 1024x768) with secrets held only in the process environment, but the first PNG is uniformly black. Candidate cause: the guest display being asleep/locked after reset versus a framebuffer state issue. Before any account-changing input, add one safe wake-key experiment — a single SPACE keysym down/up via the optional `PVE_RFB_WAKE=1` flag in `.r-publish/brat-recovery/pve_rfb_capture.py` — then re-capture; no text, chords, or pointer input. Verified 2026-08-01 (operational evidence, not a new defect): the `PVE_RFB_WAKE=1` capture succeeded and produced a real 1024x768 Windows login screen for local user `tester` with an empty Password field. The earlier uniformly black frame was therefore display sleep, not a framebuffer fault; the guest is booted to the login screen and autologin is not active. The next nondestructive diagnostic is exactly one blank-password Enter attempt through the fixed-target headless console (optional `PVE_RFB_ENTER=1` in `.r-publish/brat-recovery/pve_rfb_capture.py`) — a single Enter keysym down/up after the wake, no password brute force, no text/chords/pointer input. Verified 2026-08-01 (operational evidence, not a new defect): the single blank-password Enter attempt (`PVE_RFB_ENTER=1`) produced the Windows message "The password is incorrect. Try again." — therefore the `tester` account does not have a blank password. The next bounded diagnostic is exactly one interactive login attempt using the already-stored DPAPI `tester` credential, supplied only in a child-process environment variable (`PVE_RFB_LOGIN_PASSWORD`) and never logged or persisted; this is not brute force — it is a single credential verification against the stored secret. Verified 2026-08-01 (operational evidence, not a new defect): the single interactive login attempt using the stored DPAPI `tester` credential (`PVE_RFB_LOGIN_PASSWORD`, supplied only in a child-process environment variable, never logged or persisted) returned to the `tester` Password screen with no successful desktop — therefore the stored credential is stale, not a transport / TrustedHosts / WinRM-policy issue. Read-only Proxmox VM 100 config confirms the recovery surface without any mutation: Windows system disk at `sata0`, a Windows 10 installation ISO already mounted at `ide2`, a VirtIO ISO at `ide0`, and boot order `sata0;ide2`. Reversible recovery plan (no external image/download): (1) create a named pre-recovery VM 100 snapshot; (2) temporarily set boot order `ide2;sata0`; (3) hard-reset the already-authorized dedicated VM 100; (4) send only repeated SPACE during the optical boot prompt via the fixed-target headless console (`PVE_RFB_BOOT_ISO=1` in `.r-publish/brat-recovery/pve_rfb_capture.py`) to catch the Windows ISO "Press any key" prompt; (5) inspect WinPE headlessly to repair the `tester` account; (6) restore the original boot order/config after account repair. Correction 2026-08-01 (operational evidence, recorded before any password change; supersedes the stale-password inference above): after the stored DPAPI `tester` password was submitted through the fixed-target headless console, a later headless capture displayed the explicit Windows message "Your password has expired and must be changed." The stored password is therefore VALID, not stale — the earlier inference that returning to the Password field meant a stale credential was wrong (that screen was the expired-password prompt, not a rejection). This confirms the root cause: local `tester` password expiry blocks WinRM authentication until an interactive password change is completed. The offline WinPE/utilman recovery path is CANCELLED and no system-file modification (e.g. utilman.exe replacement) is needed. The temporary Proxmox boot order will be restored from `ide2;sata0` back to the original `sata0;ide2` WITHOUT another hard reset, after which the normal Windows password-change UI will be completed through the headless console using a cryptographically random new password held only in memory and exported as the existing local DPAPI credential file (`C:\Project\VPNRouter\.testpc-cred-192.168.0.106.xml`); no password is recorded in this backlog or anywhere on disk in plaintext. The successful pre-recovery snapshot `pre-tester-recovery-20260801` remains available for rollback until verification completes. Verified 2026-08-01 (operational evidence, not a new defect): the combined login helper's fixed 0.5-second delay after dismissing the lock/error screen is nondeterministic — a later run returned "The password is incorrect" even though the earlier explicit "Your password has expired and must be changed." message proves the stored credential was accepted, so password validity must NOT be inferred from that raced attempt. Before the next attempt, use a staged flow: dismiss/capture first, then type+submit only after the focused Password field is proven visible. Verified 2026-08-01 (operational evidence, not a new defect; recorded before retry): after a clean staged capture proved the Password field was focused, `PVE_RFB_SUBMIT_PASSWORD` still produced "The password is incorrect", so the input race is inside RFB keystroke delivery/mapping, not field readiness. Separately, two SMB credential checks hung for 30 seconds despite TCP 445 being reachable and were killed/inconclusive — do NOT use them as evidence. Next retry slows key delivery (conservative inter-key and inter-character delays in `.r-publish/brat-recovery/pve_rfb_capture.py`); no extra password guesses. Correction 2026-08-01 (operational evidence, recorded before implementation; supersedes the "stored password is VALID" inference above): the explicit "Your password has expired and must be changed." dialog appeared after the authorized VM reset/boot with no DPAPI password supplied in that boot cycle, so it was most likely triggered by Windows Autologon — it proves the `tester` configured password is expired, but does NOT prove the local DPAPI copy is current. The stored DPAPI password has now failed a slow staged submit (`PVE_RFB_SUBMIT_PASSWORD`) into a proven-focused Password field, so the local credential must be treated as stale. Root cause is therefore: expired guest credential PLUS stale verifier DPAPI copy. Next nondestructive step: inspect the built-in Windows "Reset password" link on the login screen via the fixed-target headless console (`PVE_RFB_OPEN_RESET=1` in `.r-publish/brat-recovery/pve_rfb_capture.py`); offline WinPE recovery remains fallback. Verified 2026-08-01 (operational evidence, not a new defect; recorded before the next recovery attempt): the fixed-target headless click on the built-in Windows "Reset password" link (`PVE_RFB_OPEN_RESET=1`) left the UI on the `tester` Password field and did NOT open any security-question or reset flow — the built-in reset path is therefore unavailable/inconclusive (no usable security-question/reset flow on this local account). Resume the already snapshot-protected Windows ISO recovery (snapshot `pre-tester-recovery-20260801` remains in place for rollback). The previous ISO boot attempt likely missed the very short optical "Press any key" prompt because the RFB session was connected only AFTER the reset, so the SPACE burst arrived too late. Next attempt: (1) temporarily set the original verified VM 100 boot order to `ide2;sata0` again; (2) establish the fixed-target RFB session FIRST, before any reset; (3) start the existing 40-SPACE boot burst (`PVE_RFB_BOOT_ISO=1` in `.r-publish/brat-recovery/pve_rfb_capture.py`) while that session is live; (4) issue the authorized hard reset of VM 100 while the RFB session remains active so the burst spans the optical prompt; (5) inspect the result headlessly; (6) restore boot order to `sata0;ide2` afterward. Constraints: no external image, no other VM, and no system disk mutation until WinPE and the drive letters are visually confirmed. Verified 2026-08-01 (operational evidence, not a new defect; recorded before implementation): the local command policy rejected the background Start-Process orchestration before it ran, so no boot-order/reset mutation occurred in that attempt. Next: put the already-authorized reset inside the fixed-target temporary RFB client (`PVE_RFB_BOOT_ISO_WITH_RESET=1` in `.r-publish/brat-recovery/pve_rfb_capture.py`) so the RFB session is established before the reset — the client verifies the exact destructive target (vmid=100, name=windows-brat, status=running, boot=order=ide2;sata0, sata0=local:100/…, ide2=…en_windows_10_iot_enterprise_ltsc_2019…) through the in-memory PVE API auth, issues POST /status/reset, then sends SPACE down/up 60 times at 0.25 s to span the optical "Press any key" prompt, waits 3 s, and captures; if the RFB socket closes on reset it fails closed without retry or alternate target. Verified 2026-08-01 (operational evidence, not a new defect; recorded before retry): the fixed RFB session survived the reset and the reset was issued, but the VM still fell through to installed Windows; late SPACE keys visibly filled the new-password field of the expired-password change form. No password was submitted and no system-disk mutation occurred. Since boot order `ide2;sata0` permits immediate disk fallback when the optical prompt is missed, next temporarily use boot order `ide2` only (sata0 remains attached unchanged), run the same preconnected reset+SPACE flow, visually confirm Windows Setup/WinPE, then immediately restore original `sata0;ide2`. Verified 2026-08-01 (operational evidence, not a new defect; recorded before the cold restart): the second preconnected reset+SPACE attempt also fell through to installed Windows, and the late SPACE keys again visibly filled the Password field — no password was submitted and no system-disk mutation occurred. Proxmox `GET /pending` now proves why: the boot `current` value remains `order=sata0;ide2` while `pending` is `order=ide2` — `qmreset` resets the guest but does NOT restart QEMU or apply pending boot config, so the ide2-only order was never active during either reset. The next owner-authorized exact-target operation is one hard stop/start of vmid=100 name=windows-brat (a full QEMU restart, unlike qmreset), protected by snapshot `pre-tester-recovery-20260801`, to apply the ide2-only boot order; verify stopped/running status and the current boot state afterward, then attach RFB and catch the ISO "Press any key" prompt. The system disk remains attached and unmodified. Restore the original boot order `sata0;ide2` after recovery. CRITICAL operational incident 2026-08-01 (recorded before any further console input): after the cold stop/start applied the ide2-only boot order and the preconnected 60-SPACE flow entered the Windows ISO, the repeated SPACE keys continued past the optical prompt into Windows Setup and advanced it to "Installing Windows / Copying Windows files (0%)". Codex immediately hard-stopped the exact VM 100 at 0%, rolled back snapshot `pre-tester-recovery-20260801` with TASK OK, verified the stopped state and that the config boot order was restored to `sata0;ide2`, then started VM 100 successfully. No data loss is expected (setup halted at 0% copying, then rolled back to the pre-recovery snapshot), but post-rollback boot verification is still pending. All repeated optical-key bursts are PROHIBITED and ABANDONED effective immediately; any future console input must be a single, visually confirmed action only. Verified 2026-08-01 (operational evidence, not a new defect; recorded before the next ISO attempt): post-rollback capture shows the normal intact Windows lock screen, confirming rollback/boot recovery PASS and no observed data loss. Next ISO attempt uses no burst: cold-start with temporary ide2-only, fixed RFB captures a before image and waits without input; Codex visually confirms the optical "Press any key" prompt, then creates a fixed one-shot signal causing exactly one SPACE. No signal/timeout means no input. Original boot order restored afterward. Verified 2026-08-01 (operational evidence, not a new defect; recorded before the next action): the one-space confirmation session captured the BIOS splash "Press ESC for Boot Menu"; no Space signal was created, it timed out after 60s with FAIL and zero input. A concurrent later read-only capture showed Windows expiry again, so no mutation occurred. Safer next path: replace the one-space handshake with a one-ESC handshake — cold-start, visually confirm the same BIOS splash, signal exactly one ESC to open the persistent Boot Menu, then inspect each next action separately. Verified 2026-08-01 (operational evidence, not a new defect; recorded before retry): the visually confirmed BIOS one-ESC signal was safe but arrived after tool/UI latency; the after-capture showed installed Windows, so the ESC missed BIOS and caused no mutation. Two cold starts produced the identical BIOS splash. Next: replace the slow signal wait with an immediate single ESC sent right after saving the before capture — only one ESC, no burst, based on the twice-verified splash. Verified 2026-08-01 (operational evidence, not a new defect; recorded before the next action): the immediate single ESC was sent from a twice-confirmed BIOS splash; the original RFB session timed out only because the video mode changed. A fresh read-only capture proves a persistent SeaBIOS Boot Menu at 720x400. Option 1 is the Windows ISO: "DVD/CD [ata1-0: QEMU DVD-ROM ATAPI-4 DVD/CD] (ECS_X64FREO_EN-US_DV5)"; config ide2 maps to ata1-0. Options 4/5 are other CD entries including VirtIO. The next visually confirmed action is exactly key '1' (optional `PVE_RFB_SELECT_WINDOWS_DVD=1` in `.r-publish/brat-recovery/pve_rfb_capture.py`), then a read-only inspect of the optical prompt. Verified 2026-08-01 (operational evidence, not a new defect; recorded before the next action): selecting the visually confirmed Boot Menu option 1 showed the Windows boot logo, but with no second key it returned to the installed Windows expiry screen — this proves the DVD selection worked but the ISO's own short "Press any key" prompt timed out; no mutation occurred. Next fixed flow from the persistent Boot Menu: exactly key '1', wait 1.0 second, exactly one SPACE, then no more input / read-only capture (optional `PVE_RFB_SELECT_AND_BOOT_WINDOWS_DVD=1` in `.r-publish/brat-recovery/pve_rfb_capture.py`). No burst; a late Space cannot submit anything. Verified 2026-08-01 (operational evidence, not a new defect; recorded before the next action): the fixed '1' → 1s → single-SPACE boot (`PVE_RFB_SELECT_AND_BOOT_WINDOWS_DVD=1`) reached unattended Windows Setup and, with zero further input, auto-advanced to Copying Windows files (0%). Codex immediately stopped exact VM 100, rolled back snapshot `pre-tester-recovery-20260801` with TASK OK, verified the restored boot order `sata0;ide2`, and started VM 100; this proves the ISO is unattended and the earlier navigation bursts were NOT the second trigger. The scoped API token's ISO inventory GET returns 403, so no alternate rescue ISO can be enumerated. Next bounded attempt: from the Boot Menu select option 1, one SPACE, wait 12s, then send exactly one Shift+F10 chord to open the WinPE command prompt before any unattended disk work (`PVE_RFB_BOOT_DVD_AND_SHIFT_F10=1` in `.r-publish/brat-recovery/pve_rfb_capture.py`); this chord cannot confirm setup. If the command prompt is not proven, stop and roll back. Verified 2026-08-01 (operational evidence, not a new defect; recorded before the next action): with the restored default boot order `sata0;ide2`, the persistent SeaBIOS Boot Menu order differs from the earlier ide2-only layout — option 1 is now the AHCI HDD (the installed Windows system disk) and option 2 is the Windows ISO `DVD/CD [ata1-0: QEMU DVD-ROM ATAPI-4 DVD/CD] (ECS_X64FREO_EN-US_DV5)`. Therefore do NOT run the old key-'1' flow (it would select the HDD, not the ISO); select the visually confirmed option 2 (exactly one '2' key) in the `PVE_RFB_BOOT_DVD_AND_SHIFT_F10` flow. Verified 2026-08-01 (operational evidence, not a new defect; recorded before the next action): the 12s Shift+F10 attempt (`PVE_RFB_BOOT_DVD_AND_SHIFT_F10`) captured the Windows boot logo — the chord fired too early. A read-only capture 5s later showed unattended Setup already at Copying files (0%). Codex immediately stopped exact VM 100, rolled back snapshot `pre-tester-recovery-20260801` with TASK OK, and restored/started boot order `sata0;ide2`. Calibration: the Windows logo appears ~17s after DVD selection and Setup ~26s. Next final bounded ISO interception (`PVE_RFB_BOOT_DVD_ABORT_SETUP=1` in `.r-publish/brat-recovery/pve_rfb_capture.py`): select option 2, wait 1s, exactly one SPACE, wait 18s, exactly one Shift+F10, wait 2s, then type the exact ASCII `taskkill /f /im setup.exe` followed by one Enter; if the WinPE command prompt is not proven, stop, roll back, and abandon the ISO path. Verified 2026-08-01 (operational evidence, not a new defect): the final calibrated option-2 → 18s Shift+F10 → `taskkill /f /im setup.exe` attempt (`PVE_RFB_BOOT_DVD_ABORT_SETUP=1`) did not open the WinPE command prompt; a read-only capture still showed unattended Setup at Copying files (0%). Codex immediately stopped exact VM 100, rolled back snapshot `pre-tester-recovery-20260801` with TASK OK, and restored/started boot order `sata0;ide2`; a post-rollback capture proves the original expiry dialog is intact. ISO recovery is permanently abandoned. Next normal UI action: fixed click Cancel on the expiry dialog to return to the login screen and inspect the Reset password path (`PVE_RFB_CANCEL_EXPIRED=1` in `.r-publish/brat-recovery/pve_rfb_capture.py`). Verified 2026-08-01 (operational evidence, not a new defect; recorded before the next action): after the snapshot rollback and the Cancel click, the capture shows the ordinary `tester` login screen. A blank submit produced the incorrect-password dialog, and two separate ENTER actions did NOT dismiss the pointer-focused OK dialog (the second capture still shows the error). The next open-reset flow must therefore mouse-click the OK button center (512,507) and then the Reset-password link (512,513), not rely on Enter focus. Verified 2026-08-01 (operational evidence, not a new defect; recorded before the next action): the corrected mouse-click flow dismissed the incorrect-password dialog but the built-in Reset password path returned to the ordinary `tester` Password screen and exposed no usable reset/security-question form. Review of the already captured post-rollback screen proves a simpler native path: Windows Autologon reaches the explicit expired-password dialog with OK and Cancel; the earlier recovery clicked Cancel, which abandoned the change flow. Before the next action, record the bounded plan: exact-target VM 100 windows-brat hard reset with original boot order `sata0;ide2` and snapshot `pre-tester-recovery-20260801` intact, wait for the expiry dialog, then click only OK and capture the native password-change form. ISO remains permanently abandoned. Verified 2026-08-01 (operational evidence, not a new defect; recorded before implementation): the exact VM 100 reset with original boot order and snapshot intact returned to the proven expired-password dialog; clicking only OK opened the native Windows password-change form. The form shows tester plus old Password, New password, Confirm password rows and places focus on New password, demonstrating the Windows Autologon credential has already authenticated/provided the old credential. Bounded next action: generate one cryptographically random letters+digits new password only in the parent process memory, use one fixed-target RFB mode to type it into the proven-focused New password field, TAB, type the same into Confirm password, ENTER, capture; export the DPAPI credential only after visual/WinRM proof of success. No password value is recorded. Then set tester PasswordNeverExpires and restore QEMU Guest Agent over authenticated WinRM to prevent recurrence. Verified 2026-08-01 (operational evidence, recorded before implementation): the native new-password-only attempt correctly returned "The password is incorrect"; the waiting parent process was terminated before confirmation, so the official DPAPI credential was NOT overwritten and no password changed. Qwen review found that current type_password sends shifted ASCII (uppercase/punctuation) as direct keysyms and relies on QEMU Shift synthesis; all previously proven keys were unshifted, so stored DPAPI may be stale OR its prior rejection may be a shifted-character delivery false negative. Masked password dots cannot prove character correctness. Next zero-auth-attempt calibration: after reset and reopening the native form with New password focused, move back exactly twice to the visible username field, select its text, type hardcoded non-secret `aAbB1!` using current type_password, capture, and send no Enter. A later reset restores the field. Exact screenshot means DPAPI is stale; mismatch means implement explicit Shift sequences and recalibrate. ISO remains abandoned. Correction 2026-08-01 (operational evidence, recorded before implementation): the native new-password-only attempt correctly failed with "The password is incorrect" and the waiting parent process was terminated before confirmation, so the official DPAPI file was NOT overwritten and no password changed. Qwen review found the existing `type_password` sends shifted ASCII (uppercase/punctuation) as direct keysyms and relies on QEMU shift synthesis; all previously proven keys were unshifted, so the stored DPAPI credential may be genuinely stale OR may be a false negative from shifted-character delivery. Correcting the proposed diagnostic: masked password dots cannot prove character correctness, only count. The next zero-auth-attempt calibration must use a visible field: after reset and reopening the native change form with New password focused, move focus backward exactly twice to the visible username field, select its existing text, type hardcoded non-secret `aAbB1!` via the current `type_password`, capture, and do not submit. A later VM reset restores the form/user. If the screenshot is exact, DPAPI is genuinely stale; if not, implement explicit Shift modifier sequences and calibrate again. ISO remains abandoned. Verified 2026-08-01 (operational evidence, recorded before implementation): the non-secret visible-field calibration typed hardcoded `aAbB1!` with the current direct-keysym type_password and the capture visibly contains `aAbB1` followed only by the caret ? the final `!` is absent, while lowercase, uppercase, and digit characters are exact. This confirms QEMU VNC direct shifted-punctuation delivery is broken on this path and invalidates the earlier inference that failed DPAPI submissions prove the stored password stale. Root fix before any secret retry: type shifted ASCII using explicit Shift_L down + unshifted base key down/up + Shift_L up in the shared type_password, then repeat the same visible calibration and require exact `aAbB1!`. Only after exact proof may the stored DPAPI password be used once in the old Password field. No authentication was attempted by calibration; the temporary username edit will be discarded by reset. ISO remains abandoned. Verified 2026-08-01 (operational evidence, recorded before implementation): after the explicit-Shift type_password fix, the same calibration capture renders `AABB1!`, not expected `aAbB1!`. This proves shifted punctuation now arrives (`!` present) but the preceding explicit Shift+Tab navigation contaminated letter-case state; digit `1` remained unshifted, so do not infer a simple permanently held Shift. Eliminate keyboard modifier navigation from both calibration and recovery. Next calibration will mouse-click the proven username-field center (512,386), Ctrl+A, type `aAbB1!`, no Enter, and require exact text. The real change flow, if calibration passes, will also mouse-click old Password, New password, and Confirm password fields directly rather than use Shift+Tab/Tab. Reset discards this temporary edit. No auth attempt occurred. ISO remains abandoned. Verified 2026-08-01 (operational evidence, not a new defect): the focused `HelperCmdParserGuardTests` run did not start — this shell resolved the system dotnet SDK 8.0.418 while `global.json` requires 10.0.301, so the test host never launched; no test/code/VM mutation occurred and no secret was touched. Next retry must use the already-configured bundled workspace .NET 10 runtime path, then run the same focused filter. No other changes.
  Verified 2026-08-01 (operational evidence, recorded before implementation): after the explicit-Shift type_password fix, the same calibration capture renders `AABB1!`, not expected `aAbB1!`. Shifted punctuation now arrives (`!` present), but the preceding explicit Shift+Tab navigation contaminated letter-case state; digit `1` remained unshifted, so this is not a simple permanently held Shift. Eliminate keyboard-modifier navigation. Next calibration mouse-clicks the proven username field center (512,386), Ctrl+A, types `aAbB1!`, sends no Enter, and requires exact text. The real change flow will likewise mouse-click old Password, New password, and Confirm password fields directly. Reset discards the temporary username edit. No authentication was attempted. ISO remains abandoned.
  Verified 2026-08-01 (operational evidence, recorded before implementation): pointer-based calibration still renders `AABB1!`. With no Shift+Tab in that run, lowercase letters becoming uppercase while digit `1` stays unshifted confirms Caps Lock state remained on from the earlier modifier-navigation experiment and survived qmreset; punctuation delivery is fixed. Minimal correction: restore uppercase A-Z to the previously proven direct keysym path, keep explicit Shift only for shifted punctuation, and in the next calibration send exactly one Caps_Lock toggle (0xFFE5) before pointer-selecting username and typing `aAbB1!`. Require exact text before secret input. Actual recovery will use pointer field selection and no Shift+Tab/Tab. No auth attempt occurred. ISO remains abandoned.
  Verified 2026-08-01 (operational evidence, recorded before implementation): before the Caps-toggle calibration ran, the native change form had timed out back to the ordinary tester login screen. The mode sent one Caps_Lock toggle and then typed the six non-secret calibration characters into the focused masked Password field; capture shows exactly six dots and no Enter was sent, so no authentication occurred. The prior proven state was Caps Lock on, so the single toggle is now treated as returning it off. Next: remove the one-shot toggle from calibration, reset/open the native form, and run pointer calibration immediately; require visible exact `aAbB1!` before any secret input. ISO remains abandoned.
  Verified 2026-08-01 (operational evidence, recorded before implementation): after the one proven Caps Lock toggle, reset, immediate native-form reopen, pointer-only field selection, direct uppercase keysym, and explicit Shift only for punctuation, the visible capture is exactly `aAbB1!`. RFB password delivery is now proven for lower/upper/digit/shifted-symbol classes with no authentication attempt. Next single recovery submit: mouse-select username and restore non-secret `tester`; mouse-select old Password and type the stored DPAPI password once; mouse-select New password and Confirm password and type one in-memory cryptographically random letters+digits password twice; click the confirm arrow once; capture. Both secrets stay process-memory-only. Official DPAPI is updated only after visual success, then WinRM identity must prove WINBRAT\\tester. No retry loop. ISO remains abandoned.
  Verified 2026-08-01 (operational evidence, recorded before implementation): after exact visible input calibration passed, the single old+new+confirm submit returned "The user name or password is incorrect"; the waiting process was terminated without confirmation, so no password changed and official DPAPI was NOT overwritten. Stored DPAPI is therefore genuinely stale. The remaining non-ISO native recovery is a reversible clock rollback: official Proxmox documentation defines VM `startdate` as the initial RTC date (`now | YYYY-MM-DD | YYYY-MM-DDTHH:MM:SS`) and QEMU NIC `link_down=1` as disconnected like pulling the plug. Read-only exact VM100 state: boot `order=sata0;ide2`, net0 `e1000=BC:24:11:DE:2B:21,bridge=vmbr0,firewall=1`, startdate absent, snapshot `pre-tester-recovery-20260801` intact. Bounded plan: set net0 to the exact original plus `link_down=1`, set startdate `2026-07-01T12:00:00`, cold stop/start exact windows-brat, capture only. If Autologon reaches desktop, recover there; otherwise immediately stop, delete startdate, restore exact original net0, start, and verify. Never expose network while guest clock is rolled back. ISO remains abandoned.
  Verified 2026-08-01 (operational evidence, recorded before implementation): combined startdate+net0 config PUT failed atomically before mutation with scoped-token `SDN.Use` denial; read-back proves original net0 and absent startdate unchanged. Do not broaden token rights. The same scoped token has QEMU monitor access: read-only `info network` PASS identifies `net0` e1000/tap. Revised reversible plan: PUT only startdate `2026-07-01T12:00:00`; cold stop/start exact VM100; immediately issue monitor `stop`, `set_link net0 off`, then `cont` so the guest CPU cannot reach time sync before virtual link disconnect; capture only. Restore by monitor `set_link net0 on`, delete startdate, cold restart, verify original config/online state. If any monitor/config step fails, fail closed and restore before further guest input. Snapshot remains intact; ISO abandoned.
  Verified 2026-08-01 (operational evidence, recorded before implementation): scoped token permits read-only monitor `info network` but state-changing monitor `stop` is denied at `/` with `Sys.Modify`; no pause/link mutation occurred. The failed clock+monitor attempt's handler restored and read-back proves VM running with original net0, absent startdate, original boot, and no pending startdate. Do not broaden permissions. Final bounded clock attempt: set only startdate `2026-07-01T12:00:00`, cold stop/start exact VM100, capture immediately while network config remains original. Local Autologon may complete before normal time synchronization. If expiry remains, or any unexpected screen appears, immediately stop, delete startdate, start, verify original config, and abandon clock recovery. If desktop appears, keep the snapshot and recover the account through that authenticated session, then restore startdate/current-time boot before re-enabling normal verification. ISO remains abandoned.
  Verified 2026-08-01 (operational evidence, recorded before implementation): startdate-only July cold boot still reached expiry, then exact restore deleted startdate and verified VM running with original boot/net0 and no pending clock config. Mac-to-PVE root SSH is unavailable (publickey/password denied), so no host bypass. A separate least-privilege surface remains: scoped token has read access to VM100 firewall; current VM firewall options are absent/default and rules count is zero, while net0 already has firewall=1. Official Proxmox firewall schema supports VM `enable=1` and `policy_out=DROP`. Final isolated-clock attempt: set only VM firewall enable=1/policy_out=DROP, verify Tailscale/WinRM becomes unreachable, set July startdate, cold boot, capture. If expiry remains or isolation is unproven, immediately stop, delete startdate, restore absent/default firewall options, start, verify network/config, and abandon clock recovery. If desktop appears, recover offline, then restore all defaults before remote verification. Snapshot intact; ISO abandoned.
  Verified 2026-08-01 (current blocker): VM firewall options `enable=1, policy_out=DROP` applied/read back but did not isolate traffic (WinRM remained reachable), so options were deleted and read-back proves firewall enable/policy_out absent, startdate absent, original net0/boot, VM running. All autonomous non-ISO paths are now exhausted: guest password is expired, local DPAPI copy is proven stale, QGA is down, root SSH unavailable, and scoped API mutations need one missing `SDN.Use`. Exact least-privilege owner action for later: Proxmox Datacenter -> Permissions -> Add -> API Token Permission; Path `/sdn/zones/localnetwork/vmbr0`; API Token `root@pam!claude-testvm`; Role `PVESDNUser` (official role containing `SDN.Use`); Propagate off. No password or broader Sys.Modify is needed. After that one ACL assignment, resume exact net0 link_down + July startdate cold boot, recover tester in authenticated Autologon session, set PasswordNeverExpires, start QEMU Guest Agent, restore original config, then run full R3 verification. Until then QF-11 remains P1 LIVE BLOCKER. ISO remains abandoned.
  Verified 2026-08-01 (owner ACL applied; QGA alive; minimal QGA recovery path): the owner added exactly the requested PVESDNUser API-token permission for `root@pam!claude-testvm` at path `/sdn/zones/localnetwork/vmbr0`, Propagate off. The scoped token can now apply net0 `link_down=1`; exact VM100 windows-brat is running with network disconnected, original boot order, the named recovery snapshot `pre-tester-recovery-20260801` intact, and the temporary July startdate requested for cold boot. Crucial native recovery discovery: while network is isolated, `tools/pve-guest-exec.ps1` using the same DPAPI Proxmox token successfully executed `echo qga-alive` inside VM100 and returned exit 0 — QEMU Guest Agent is therefore running now; the previous "QGA is down" state is obsolete. Disposition / next minimal recovery: use QGA (not ISO/RFB UI) once to set tester to an in-memory random password, set PasswordNeverExpires, and ensure the guest-agent service remains automatic/running; then restore exact original Proxmox net0 and delete temporary startdate before restoring network, write the new password only to the existing local DPAPI credential after authenticated proof, and run remote R3 verification. No secret value in this backlog. ISO remains permanently abandoned.
  Correction 2026-08-01 (recorded before any further recovery action): the successful `echo qga-alive` was NOT VM100. `tools/pve-guest-exec.ps1` defaults to `VmId = 101`; because the call omitted explicit `-VmId 100`, read-only identification returned Linux host `debian-xfce`, Debian 12, uid 0 — it was VM101. No password/account/config mutation was made inside either guest; only `echo` and OS/service-identification attempts occurred. The preceding statement that QGA is alive on windows-brat is therefore RETRACTED; VM100 QGA status remains unproven/down until an explicit fixed-target `-VmId 100` call succeeds. Safety rule for this recovery and all future Windows guest-agent commands: always pass explicit `-VmId 100` and preflight VM name/status via the Proxmox API; never rely on the Linux-oriented helper default. VM100 remains network-isolated with snapshot `pre-tester-recovery-20260801` intact and temporary July startdate/config state to be restored after the recovery attempt. ISO remains abandoned. No secret values.
  Verified 2026-08-01 (operational evidence, recorded before implementation): explicit fixed-target Proxmox preflight proved VM100 name windows-brat / status running, but POST VM100 agent/ping still returned 500; QGA recovery is unavailable. The temporary isolated-clock config was then fully restored: VM100 is running with boot `order=sata0;ide2`, exact original net0 `e1000=BC:24:11:DE:2B:21,bridge=vmbr0,firewall=1`, startdate absent, no pending startdate, and snapshot `pre-tester-recovery-20260801` intact. A validation syntax error briefly left the already-restored VM stopped; a separate exact-state check then started it successfully. No guest account mutation occurred. Read-only configs show VM100 disk `local:100/vm-100-disk-2.qcow2` at sata0 and VM101 debian-xfce has only scsi0 with free secondary slots. VM101 QGA root access is proven only with explicit `-VmId 101`; it has ntfs-3g/lsblk/blkid/mount/python3, but no hivexregedit/reged/chntpw. Next minimal, reversible investigation: stop exact VM100, temporarily attach its system disk to VM101 read-only only, mount NTFS read-only only, and look for the configured Windows Autologon DefaultPassword. If present, keep it process-memory-only and use it once in the already proven native Windows password-change form. Never print/persist it. Detach disk and restore both VM configs before booting VM100. If read-only attach/mount or exact extraction cannot be proven, fail closed and make no disk write. No package install and no system/SAM/utilman modification. ISO remains abandoned.
  Verified 2026-08-01 (current narrow owner action): the bounded read-only disk-attach attempt first exact-checked VM100 windows-brat running, VM101 debian-xfce running, the VM100 disk and snapshot, then stopped only VM100 and attempted VM101 scsi1=`local:100/vm-100-disk-2.qcow2,backup=0,ro=1`. Proxmox rejected it before attachment with 403 `Permission check failed (/storage/local, Datastore.Audit)`; the failure handler removed scsi1 and restarted VM100. Final read-back proves VM100 windows-brat running with original boot/net0 and absent startdate, and VM101 running with scsi1 absent. No disk mount/read/write and no guest mutation occurred. Official/current Proxmox role enumeration from the local API shows built-in `PVEAuditor` includes `Datastore.Audit` and no datastore write/allocation privilege. Exact least-privilege owner action: Proxmox Datacenter -> Permissions -> Add -> API Token Permission; Path `/storage/local`; API Token `root@pam!claude-testvm`; Role `PVEAuditor`; Propagate off. Do not use PVEDatastoreUser because it also grants `Datastore.AllocateSpace` and is broader than the observed missing permission. After that one ACL assignment, retry only the previously recorded read-only attach/mount/extract flow; fail closed if any additional privilege is requested. ISO remains abandoned; no secrets.
  Verified 2026-08-01 (owner access applied): the owner explicitly supplied/authorized root Proxmox access for this recovery (root password NOT recorded here). Browser automation was unavailable — the Chrome control extension was not connected and Computer Use could not verify the Helium URL — so the same official Proxmox REST API was used over the existing local tunnel. Root authentication succeeded as `root@pam` without a 2FA challenge on Proxmox 8.4.1. The first raw Cookie-header PUT returned 401 with no mutation; a read-only WebRequestSession check then proved the session and ACL read access, and the corrected Cookie-session PUT added exactly Path `/storage/local`, token `root@pam!claude-testvm`, role `PVEAuditor`, propagate 0, with exact ACL read-back PASS. The root password/ticket were held only in process memory, never logged or persisted, and cleared after use; the scoped token must now be used for all following VM/storage actions and the root session is closed. Next action remains only the previously recorded read-only attach/mount/extract flow, failing closed on any extra privilege. ISO abandoned; no secrets.
  Verified 2026-08-01 (read-only disk access proven): after the PVEAuditor ACL, the scoped token successfully stopped exact VM100 windows-brat and hot-attached `local:100/vm-100-disk-2.qcow2` to exact VM101 debian-xfce as scsi1 with `backup=0,ro=1`; VM100 remains stopped and its snapshot `pre-tester-recovery-20260801` intact. Proxmox config read-back includes `ro=1`, and explicit VM101 `lsblk -J` independently shows a new 100G `/dev/sdb` and NTFS `/dev/sdb1` both `ro: true`. Mount succeeded at `/mnt/winbrat-recovery` using `ntfs-3g -o ro,norecover`; `findmnt` proves fuseblk `ro`; `Windows/System32/config/SOFTWARE` exists. No disk write occurred. The first exact marker probe failed because Debian VM101 has no `strings` binary — no hive content or secret was read or output; a later command explicitly confirmed `strings: command not found`. Do not implement a custom registry parser. `apt-cache policy` proves standard Debian bookworm package `chntpw` candidate 140201-1 is available and not installed. Minimal revised plan: temporarily install only `chntpw` in VM101, use `reged` read-only against the read-only hive to test/extract only Winlogon DefaultUserName/DefaultPassword into process memory without output/persistence, then purge chntpw and unmount/detach before starting VM100. If DefaultPassword is absent/protected or any write is requested, stop and fail closed. No SAM/utilman changes; ISO abandoned; no secrets.
  Verified 2026-08-01 (Autologon uses protected LSA secret): standard Debian chntpw/reged 140201-1 was installed temporarily on VM101 with no recommends, and the SOFTWARE hive remained on the independently proven read-only mount/device. `reged -x` exported only the Winlogon key to stdout captured in parent-process memory, never displayed/persisted; boolean-only result — export received, DefaultUserName present, AutoAdminLogon present, DefaultPassword absent — and no value/secret was logged. Therefore this machine's Autologon password is not plaintext in Winlogon and is likely protected as an offline LSA secret in SYSTEM+SECURITY (standard Sysinternals Autologon behavior); reged made no write. `apt-cache policy` proves standard Debian bookworm `python3-impacket` 0.10.0-4 is available and not installed. Next minimal path: temporarily install it, run its offline secretsdump against the read-only SYSTEM/SECURITY hives, capture stdout only in parent-process memory, parse only the `DefaultPassword` LSA secret without displaying/persisting any secret, and fail closed if absent/ambiguous; then purge both temporary packages and dependencies as appropriate, unmount/detach, start VM100, and use the recovered old password once in the native password-change flow. No SAM/utilman write; ISO abandoned; no secrets.
  Verified 2026-08-01 (Autologon old password recovered in memory): standard Debian `python3-impacket` 0.10.0-4 was installed temporarily on VM101 with no recommends, and the offline `impacket-secretsdump -system .../SYSTEM -security .../SECURITY LOCAL` ran only against the independently proven read-only NTFS mount. The first parser expected a bare header and reported zero blocks; a structure-only probe then proved exactly one DefaultPassword mention followed by one nonempty colon-delimited line, and a corrected parser proved a unique block and extracted a 14-character secret. The value, boot key, hashes, and secretsdump output were never displayed/persisted; variables were cleared after each probe, and no hive write occurred. Safe staged application plan (recorded before any password change): one long-lived parent process re-extracts the old secret, generates a random complexity-compliant new password, purges the temporary chntpw/python3-impacket packages, unmounts and detaches the Windows disk, restores/starts exact VM100, then pauses after each fixed-target RFB screenshot using named in-memory synchronization events while Codex visually approves expiry dialog -> native change form -> result. Old/new secrets stay only in that process/environment and are never printed/file-persisted; the official DPAPI credential is written only after visual success, followed by WinRM identity proof, and failure/abort leaves DPAPI untouched. Then set tester PasswordNeverExpires and QGA automatic/running over authenticated WinRM. ISO abandoned; no SAM/utilman write; no secrets.
  Verified 2026-08-01 (staged recovery safely aborted; extra current-password step): the long-lived staged process re-extracted the LSA secret and generated a new random password only in memory, purged explicit chntpw/python3-impacket packages, unmounted NTFS, detached VM101 scsi1, exact-checked original VM100 config/snapshot, started windows-brat, and paused after screenshots. Stage1 screenshot visually proved the tester expired-password dialog. After only the proven OK click, Stage2 unexpectedly showed the ordinary single focused `Password` field with arrow, NOT the multi-field native change form. The process was therefore terminated before signaling its change-password step; no fields were typed, no password changed, official DPAPI remains unchanged (file timestamp 2026-06-20), and in-memory old/new secrets died with the process. Post-abort verification: VM100 running; VM101 has no scsi1; `/mnt/winbrat-recovery` absent; lsblk only original 30G sda + sr0. No Windows disk write. Corrected retry needs four visual gates: expired dialog -> click OK -> single current-password field -> submit recovered old password once with existing `PVE_RFB_SUBMIT_PASSWORD` -> require multi-field native change form -> fill old/new/confirm with calibrated pointer mode -> require success -> prove WinRM before DPAPI write. Fail closed at any mismatch. Reattach/extract remains read-only and temporary packages are purged again. No secrets; ISO/SAM/utilman abandoned.
  Verified 2026-08-01 (retry pre-read interpolation failure; cleanup PASS): the corrected four-gate process exact-checked VM100 windows-brat and VM101 debian-xfce plus the VM100 disk and snapshot `pre-tester-recovery-20260801`, stopped only VM100, and hot-attached VM101 scsi1=`local:100/vm-100-disk-2.qcow2,backup=0,ro=1`, but failed before any mount, package install, or secret extraction because local PowerShell expanded the Linux read-only probe `$(cat /sys/block/sdb/ro)` inside a double-quoted guest command as the local path `C:\sys\block\sdb\ro`. The process emitted RECOVERY_FAIL and its finally block detached scsi1; a separate exact-state cleanup then proved VM100 stopped with original boot/net0 and absent startdate, snapshot intact, VM101 scsi1 absent, and started VM100 successfully. Final state: VM100 windows-brat/running; no mount, package install, disk read/write, secret extraction, or password/DPAPI change occurred. Minimal retry correction: avoid all shell command substitution in interpolated PowerShell strings and prove guest read-only with a literal no-dollar command such as `lsblk -dn -o RO /dev/sdb | grep -qx 1`; keep the same four visual gates and fail-closed cleanup. ISO/SAM/utilman abandoned; no secrets.
  Verified 2026-08-01 (Stage2 is timing-variable; adaptive visual branch required): the no-dollar prep retry succeeded — VM100 stopped, VM101 `scsi1` attached `ro=1`, guest `lsblk -dn -o RO /dev/sdb | grep -qx 1` proved RO, the read-only mount and Impacket install completed, and the staged process re-extracted the unique old secret in memory, purged the package, unmounted/detached, started exact VM100, and Stage1 again visually proved the expiry dialog. After the same single OK click, this Stage2 rendered the full native `tester` / Password / New password / Confirm password form with New password focused, whereas the prior Stage2 showed one ordinary Password field — the Windows transition is timing-variable. The running process expected the one-field branch and would have submitted the old password into the focused New password field, so Codex terminated it before any signal/input; no field was typed and no password changed (DPAPI timestamp still 2026-06-20, VM100 running, VM101 `scsi1` absent). Correct retry must wait on one of two explicit in-memory events selected only after screenshot review: FULL_FORM -> run the calibrated old/new/confirm pointer fill directly; SINGLE_CURRENT -> submit the recovered current password once, capture and require FULL_FORM, then fill. Never infer the branch from timing. Continue with result screenshot -> WinRM proof -> DPAPI write only on PASS. No secrets; ISO/SAM/utilman abandoned.
  Verified 2026-08-01 (adaptive prep output typo; cleanup PASS): adaptive prep completed its stop / read-only attach / RO mount / package install, then a local PowerShell typo `Write-Output'ADAPTIVE_PREP_PASS'` was parsed as the nonexistent command `Write-OutputADAPTIVE_PREP_PASS`, triggering the already-defined failure handler after prep. The handler unmounted, purged the package, detached scsi1, and restarted VM100. Read-back: VM100 running, VM101 scsi1 absent; no secret extraction, no password/DPAPI change, and no Windows disk write. Recorded before retry; the retry changes only the diagnostic to `Write-Output 'ADAPTIVE_PREP_PASS'`. No secrets/other changes.
  Verified 2026-08-01 (adaptive stage parser gate; prepared RO state preserved): adaptive stage script failed PowerShell parsing before execution because compressed `foreach($x in$bb)` / `foreach($e in$ev)` require spaces around `in`. No command ran, no secret extracted/input/password/DPAPI change. Read-back proves prepared state preserved exactly: VM100 stopped; VM101 scsi1 remains `local:100/vm-100-disk-2.qcow2,backup=0,ro=1,size=100G`; prior prep already proved RO mount/package. Retry changes only syntax to `foreach ($x in $bb)` and `foreach ($e in $ev)` and resumes from prepared state with adaptive visual branching. No secrets/other changes.
  Verified 2026-08-01 (PowerShell R alias collision; pre-input abort PASS): adaptive process successfully re-extracted old secret/new password in memory, purged package, unmounted/detached read-only disk, restored/started exact VM100; then failed before any RFB input because function name `R` resolved to built-in alias `Invoke-History`, which rejected positional argument `capture`. Process exited and secrets died. Verification: VM100 running, VM101 scsi1 absent, DPAPI timestamp still 2026-06-20; no password/input/disk write. Retry renames function/calls to unambiguous `Invoke-RecoveryRfb` and starts from normal state with a fresh read-only extraction. No secrets/other changes.
  Verified 2026-08-01 (tester credential recovery PASS; QGA residual): fresh read-only extraction/adaptive process succeeded. Stage1 visually proved expiry. Stage2 was FULL_FORM. Calibrated pointer-only fill submitted recovered current password + random new password twice; result screenshot explicitly said `Your password has been changed.` No secret displayed/persisted. Only after visual success, WinRM with new in-memory credential proved exact `WINBRAT\\tester`; then existing official DPAPI file was overwritten (new timestamp 2026-08-01 16:33:33). `tools/brat-verify.ps1 -Action identity` PASS, `tools/testvm-control.ps1 -Action ensure-ready -PveHost 127.0.0.1` PASS. Get-LocalUser: Enabled true, PasswordLastSet 2026-08-01 16:32:31, PasswordExpires null (PasswordNeverExpires effective). Both Windows services `QEMU-GA` / `QEMU Guest Agent` and `QEMU Guest Agent VSS Provider` are Running/Automatic. However explicit VM100 Proxmox agent/ping still returns `QEMU guest agent is not running`; this is a residual operational issue, not a credential blocker. Cleanup PASS: VM100 windows-brat/running with original boot/net0, absent startdate; VM101 scsi1 absent; chntpw/python3-impacket absent. No Windows disk write. Scoped token/root tickets/passwords cleared. Next bounded QGA fix: over authenticated WinRM inspect exact QEMU-GA service image/exit state, virtio serial device, and relevant event/log errors; first minimal mutation is restart only QEMU-GA and retry Proxmox ping. Escalate only from evidence. After QGA resolution run full R3 brat verification. No secrets.
  Verified 2026-08-01 (QGA root cause: missing VirtIO Serial driver): authenticated read-only diagnostics prove QEMU-GA service healthy Running/Auto, ExitCode 0, image `C:\Program Files\Qemu-ga\qemu-ga.exe -d --retry-path`, but no VirtIO Serial device appears. Get-PnPDevice/Win32_PnPEntity shows PCI `VEN_1AF4&DEV_1003&SUBSYS_00031AF4` Code 28 (missing driver), plus other unrelated Code28 devices; pnputil enum proves vioser absent. D: is mounted `virtio-win-0.1.262`; exact Windows 10 INF `D:\vioserial\w10\amd64\vioser.inf` exists (also other OS variants). This explains service Running while Proxmox agent/ping fails. First minimal fix: authenticated WinRM `pnputil /add-driver D:\vioserial\w10\amd64\vioser.inf /install`, require success, restart only QEMU-GA, verify device OK and Proxmox agent/ping PASS; reboot only if pnputil/evidence requires. No secrets/other changes.
  Verified 2026-08-01 (vioser install PASS; QGA restart stuck Stop Pending): authenticated pnputil installed existing Windows-10 `D:\vioserial\w10\amd64\vioser.inf` successfully before the command timed out. Restart-Service then waited 90s for QEMU-GA to stop and timed out. Read-back: vioser installed; device now `VirtIO Serial Driver`, same VEN_1AF4&DEV_1003, ConfigManagerErrorCode 0; Proxmox VM100 agent/ping PASS. QEMU-GA alone is `Stop Pending`/Degraded PID 2116, Auto, ExitCode 0. Root functional issue is fixed, but service state needs cleanup. Next bounded action: recheck after delay; only if exact QEMU-GA remains Stop Pending with same PID, terminate that exact service PID, Start-Service QEMU-GA, require Running/Automatic and PVE ping PASS. No reboot unless this fails. No secrets/other changes.
  Verified 2026-08-01 (QGA final PASS; R3 deploy launch blocked by sleeping/non-interactive session): the delayed recheck confirmed exact QEMU-GA still `Stop Pending`/Degraded with the same PID 2116 and image path `C:\Program Files\Qemu-ga\qemu-ga.exe`; terminated ONLY that exact qemu-ga.exe process (PID 2116, no other process touched), then Start-Service QEMU-GA. Final read-back: QEMU-GA Running/Automatic and Proxmox VM100 agent/ping PASS — the QF-11 QEMU Guest Agent residual is resolved. The mandatory post-ship skill phases then ran green: CI PASS, `tools/brat-verify.ps1 -Action identity` PASS, `tools/testvm-control.ps1 -Action ensure-ready` PASS, and the local R3 release ZIP SHA256 check PASS. Deploy installed R3 on WINBRAT, but the launch check reported `VPNRouter.App.exe` NOT running after the 6-second wait, so phase 4 (UIA walk) FAIL/STOP per the fail-closed skill contract. A read-only RFB capture at this point is uniformly black (display sleep), so the interactive login/desktop state on WINBRAT is unproven — the app-not-running result may be a sleeping/non-interactive session rather than a deploy fault. Next bounded action: before ANY wake key or OK click, add a pointer-move-only RFB mode (exactly one RFB PointerEvent type 5, button mask 0, coordinates 512,384, no click/key/text) to `.r-publish/brat-recovery/pve_rfb_capture.py`, capture and visually inspect to prove session state; only then finish the interactive login and rerun the R3 deploy/verify. No secrets.
  Verified 2026-08-01 (pointer-only wake PASS; tester session is at lock screen): Qwen-added pointer-move mode passed py_compile and sent exactly one mask-0 pointer event, no click/key/text. Capture woke display and shows ordinary Windows lock screen (time/date/background), no password-changed dialog and no logged-in desktop. Therefore R3 deploy phase4 launch failed because tester interactive session was locked/not logged in, not because binary install failed. Next bounded action: exactly one fixed-target `PVE_RFB_LOGIN_PASSWORD` attempt using the newly verified stored DPAPI tester credential only in child-process environment, capture; require desktop, then rerun R3 deploy from phase4. No secrets/other changes.
  Verified 2026-08-01 (operational evidence, not a new defect): after the lock screen and focused `tester` Password field were visually proven, the staged RFB submit (`PVE_RFB_LOGIN_PASSWORD`) using the official DPAPI tester credential was rejected by Windows with the visible message "The password is incorrect. Try again." The same stored credential had already passed WinRM identity as `WINBRAT\\tester`, so the current evidence points to an RFB keyboard translation/layout problem or stale console input rather than an account/credential failure. Fail closed: do not retry blindly; next action is a read-only/visible keyboard-layout or alternate secure login-path diagnosis. No secrets.
  Verified 2026-08-01 (evidence/root-cause, recorded before remediation): authenticated read-only checks proved no active interactive user session, Windows logon keyboard layout 00000409/en-US, AutoAdminLogon=1 for tester, no plaintext DefaultPassword, and no Autologon executable currently installed. The tester password was changed during recovery, but the pre-existing protected LSA Autologon secret was not refreshed; this is the simplest explanation for both no console session after reboot and why the post-ship deploy could not launch UI. Minimal permanent test-harness fix: use only Microsoft's official signed Sysinternals Autologon v3.10 transiently over authenticated WinRM, verify its Authenticode signer before execution, supply the current DPAPI credential only in process memory/no logs, refresh the encrypted LSA secret for tester/WINBRAT, delete the transient executable, perform one controlled guest restart, and visually require an interactive tester desktop. Then rerun R3 deploy phase 4. Fail closed if signature, tool exit, restart readiness, identity, or desktop proof fails. No secret values.
  Verified 2026-08-01 (operational evidence, recorded before implementation): an inline one-shot PowerShell attempt to refresh Windows Autologon was rejected by the local shell policy before execution, so no local or remote mutation occurred. Next minimal path is an ignored exact-purpose script (`.r-publish/brat-recovery/refresh-autologon.ps1`, no parameters, fail-closed) so the invoking command line carries no password logic or value; it loads the existing DPAPI credential only at runtime, refreshes the encrypted LSA secret for tester/WINBRAT over authenticated WinRM to the fixed 100.115.182.0 using the official signed Sysinternals Autologon tool, deletes the transient remote executable, verifies the Autologon postconditions and WINBRAT\tester identity, and only then issues one controlled guest restart. No secret values.
  Verified 2026-08-01 (local policy gate): the inline Autologon refresh command was rejected by the local shell policy before execution; no local or remote mutation occurred. Next minimal path is an ignored exact-purpose PowerShell helper whose command line contains no password logic/value; validate it before execution. No secrets.
  Verified 2026-08-01 (pre-run validation): correction to the prior pre-run note — the generated helper `.r-publish/brat-recovery/refresh-autologon.ps1` already enforced the Microsoft signer gate fail-closed (remote Authenticode Status must be Valid AND SignerCertificate.Subject must match CN=Microsoft Corporation, throwing otherwise), so the earlier claim that it "did not require the signer to be Microsoft" was wrong. The actual gap was that the safe `SignerIsMicrosoft` boolean was returned in `$invokeResult` but neither printed nor included in the `$allPass` reboot gate. Patched minimally: the post-condition output now prints the safe `SignerIsMicrosoft` boolean and `$allPass` includes `$invokeResult.SignerIsMicrosoft` before any reboot. Helper was not executed and no VM mutation occurred. No secrets.
  Verified 2026-08-01 (operational evidence, recorded before remediation): after Autologon refresh and controlled restart, brat identity PASS and quser proved tester console session 1 Active, but `tools/brat-verify.ps1 -Action screenshot` failed at line 498 because `Invoke-BratInteractive -Session $s` bound the literal string `-Session` to FromSession instead of a PSSession (`Cannot convert the "-Session" value ... to PSSession`). No screenshot was produced and no remote mutation occurred. RESOLVED by pending harness patch 2026-08-01: `Copy-Item -FromSession -Session $Session` corrected to `-FromSession $Session` in `tools/brat-verify.ps1`. No secrets.
  Verified 2026-08-01 (UIA Inspect finding; no UI mutation): UIA Inspect by Name passed for `Not connected` and `Advanced settings`, but adding `ControlType = Button` failed because the helper used `[System.Windows.Automation.ControlType].GetProperty(...)` while local reflection proves `Button` is a public static field returned by `GetField`. RESOLVED by pending harness patch 2026-08-01: `.GetProperty(...)` corrected to `.GetField(...)` with the same `Public,Static` binding flags in `tools/brat-verify.ps1`. No secrets.
  Verified 2026-08-01 (local SDK selection gate): the focused HelperCmdParserGuardTests command did not start because this shell resolved system .NET SDK 8.0.418 while global.json requires 10.0.301; no test, code, or VM mutation occurred. Retry with the configured bundled workspace .NET 10 path; do not edit global.json or install an SDK. No secrets.
  Verified 2026-08-01 (final verification PASS; QF-11 RESOLVED): official Microsoft-signed Sysinternals Autologon refreshed the protected LSA secret for tester/WINBRAT (no plaintext DefaultPassword, transient exe absent), followed by one controlled guest restart. `tools/brat-verify.ps1 -Action identity` PASS; `quser` proved tester console session 1 Active; headless RFB capture visually confirmed a normal desktop. QEMU-GA Running/Automatic and Proxmox VM100 agent/ping PASS. R3 release ZIP SHA256 check PASS, deploy PASS, `VPNRouter.App.exe` PID 4448 running. The two pending harness patches (`.GetProperty` → `.GetField` for ControlType.Button, `-FromSession` binding) then passed live: UIA found `Connect` (ControlType.Button), `Not connected`, and `Advanced settings`; screenshot action saved a baseline PNG. CLI doctor reports Version 2.48.0-r3, 0 errors, one environment DF-MTU warning. Remote last 500 log lines clean (no `[ERR]`/`Exception`/`FATAL`). Bundled .NET 10 focused tests passed HelperCmdParserGuardTests 7/7 and BratVerifierContractTests 4/4. R3 changed only Windows update-helper reliability (no UI/routing behavior); its generated-helper ordering regression is covered by the passing contract, and a true update away from R3 requires a later target version (not claimed here). Post-ship R3 verification PASS. QF-11 RESOLVED.

### Qwen full-application audit — 2026-07-28 (18 parallel read-only reviewers; Codex-verified)
- [x] **P0 RESOLVED v2.48.0-r1** — desktop update SHA256 is fetched but discarded by the `IUpdateSource` adapter (`FullChecksumUrl=null`), so normal GitHub release staging skips the hash gate — `VPNRouter.Core/Services/UpdateChecker.cs:119,251` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — `TunOwnershipLock` is a disposed singleton: the second connect/disconnect lifecycle reacquires the semaphore but `Dispose` returns early and never releases it, blocking other VPNRouter processes until app exit — `VPNRouter.Core/Services/TunOwnershipLock.cs:114` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — pre-start and post-start failover share one `_failover ??=` slot with incompatible delegates; pre-start wiring can later bypass teardown/lifecycle/session guards, orphan sing-box/TUN state, or revive a disconnected tunnel — `VPNRouter.Core/Services/VpnEngine.cs:1495,1527` — next rolling candidate
- [ ] **P1** — Linux kill-switch accepts IPv6 server literals and then removes them from its allow-list while dropping all IPv6, so a crash blocks reconnect to an IPv6-only server until manual nft cleanup — `VPNRouter.Core/Platform/Linux/LinuxFirewallManager.cs:205,226` — next rolling candidate
- [ ] **P1** — macOS kill-switch emits accepted IPv6 server literals as IPv4-only `inet` PF rules; atomic PF load fails and leak protection never engages — `VPNRouter.Core/Platform/macOS/MacFirewallManager.cs:340,359` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — settings save overwrites `config.yaml` directly; a crash or power loss during `File.WriteAllText` can truncate all settings and credentials — `VPNRouter.Core/Services/SettingsLoader.cs:536` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — subscription HTTP responses are buffered without a byte limit, allowing an untrusted provider/endpoint to exhaust process memory — `VPNRouter.Core/Services/PolicyHttpClient.cs:112-118` / `SubscriptionFetcher.cs:85` — next rolling candidate
- [ ] **P1** — Smart Connect stores its measured winning server, then `SaveSettings` immediately overwrites it from the stale selection and connects the wrong/dead server — `VPNRouter.App/ViewModels/MainWindowViewModel.SimpleMode.cs:536` / `MainWindowViewModel.cs:3783` — next rolling candidate
- [ ] **P1** — repair trampoline still uses inline PowerShell `-Command` download-and-execute, reintroducing the Defender ClickFix heuristic already removed from the app repair path — `VPNRouter.GUI/repair.go:58` — next rolling candidate
- [ ] **P1** — `vpnrouter stop` kills only the child PID; the owning `start` engine treats it as a crash, restarts sing-box after the state file is cleared, and leaves an untracked VPN — `VPNRouter.CLI/Commands/StopCommand.cs:23` — next rolling candidate
- [ ] **P1** — CLI stop kills the recorded PID without revalidating executable ownership; PID reuse can terminate an unrelated process tree — `VPNRouter.CLI/Commands/StopCommand.cs:23` — next rolling candidate
- [ ] **P1** — Android broadcasts and logs raw libbox exception messages that can contain server addresses, UUIDs, or config fragments — `VPNRouter.Android/VpnRouterService.java:682` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — macOS build references undefined `${ARCH}` under `set -u` and aborts whenever the wgturn source/build branch is reachable — `build-mac.sh:90,93` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — Linux release CI downloads and executes mutable `continuous` appimagetool without a digest — `.github/workflows/build-linux.yml:169` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — Linux release CI downloads and bundles the sing-box/libcronet archive without verifying its digest — `.github/workflows/build-linux.yml:107` — next rolling candidate
- [ ] **P1** — full credential-bearing subscription URLs are written to logs, exposing provider tokens — `VPNRouter.Core/Services/SubscriptionFetcher.cs:62,72,88,101,106,110,324,343` — next rolling candidate
- [x] **P1 RESOLVED v2.48.0-r1** — `%ProgramData%\VPNRouter` inherits local Users read access for config and logs containing VPN credentials — `VPNRouter.Core/AppPaths.cs:112` / `packaging/windows/install.ps1` — next rolling candidate
- [ ] **P1** — Zapret update swallows locked-file copy failures but still writes the new version marker, suppressing repair of a mixed old-driver/new-executable installation — `VPNRouter.Core/Services/ZapretUpdater.cs:347-374,619-636` — next rolling candidate
- [ ] **P1** — Clash log stream writes the full WebSocket URI including `clash_api_secret`; the crash-report scrubber does not remove `ws://` query tokens — `VPNRouter.Core/Services/ClashLogStream.cs:133` / `CrashReporter.cs:169-199` — next rolling candidate

### r9 Android cut-readiness audit — 2026-07-10 (device-verified; one P3 copy follow-up)
- [ ] **P3** — the r8 F2 fork-feature-gate rejection message is desktop-worded and misleading on Android. `CustomConfigInjector.CheckForkFeatureSupport` tells a user pasting an AWG/xhttp custom config to "Use a VPNRouter build bundling the lx core" — but there is NO lx Android build (Android ships upstream SFA-libbox with AWG/XHTTP off; **verified**: bundled `libbox.so` has `with_wireguard/with_quic/with_utls/with_v2ray_api` but NO `with_awg`/`with_xhttp`, and 0 amnezia symbols). The REJECTION ITSELF IS CORRECT on Android — pre-r8 such a config hit libbox's `unknown transport type` FATAL; r8 turns it into an actionable up-front error (a strict improvement, NOT a regression). Only the copy needs a platform-neutral rewording (e.g. "…this build's sing-box core lacks with_awg — remove the AWG fields, or use a desktop build with the lx core; AmneziaWG isn't available in the Android build"). Shared Core string, improves desktop too. Edge path (custom AWG/xhttp config on a phone) — not cut-blocking — `VPNRouter.Core/Services/CustomConfigInjector.cs` CheckForkFeatureSupport — batch into next Core change

### r8 post-ship live-verify findings — brat 2026-07-10 (both pre-existing, NOT r8 regressions)
- [x] **P2 RESOLVED v2.47.0-r9 (2026-07-10)** — deep-verify "Стоп" cancel was UNREACHABLE from the UI: the bare `[RelayCommand]` on the four async batch-test commands generated AsyncRelayCommands that DISABLE the bound button while running, so the `if (IsTesting*) Cancel()` re-entry branch never fired (live-proven on brat: four cancel attempts, the 21-server batch completed every time). All four now carry `[RelayCommand(AllowConcurrentExecutions = true)]`; re-entry is safe (everything before the IsTesting* flag assignment is synchronous on the dispatcher, so a second click can only take the cancel branch). Pinned by `ServerTestCancelReentryTests` (4 source pins) — `VPNRouter.App/ViewModels/MainWindowViewModel.ServerTesting.cs`
- [x] **P2 RESOLVED v2.47.0-r9 (2026-07-10)** — transient false "Подключено через службу" while deep-verify runs: the 2s poll's `RuntimeStatusDetector.IsVpnRunning` counted our OWN probe-spawned sing-box processes (ownership filter passes — they run from ProgramData\bin) as a live tunnel. (The brat Tailscale-adapter theory was WRONG — `TunOwnershipLock` is a named semaphore, not an adapter check; the poll path was simply single-signal.) Fix: `DeepVerifyProbe.BeginProbeScope()` marks the probe window in both desktop verifiers, and while any probe is in flight the detector requires the second signal (`TunOwnershipLock.IsOwnedByAnyone()` — held only by a real tunnel owner). Outside a probe window the single ownership signal keeps its existing semantics. Pinned by `DeepVerifyProbeScopeTests` (counter behaviour + detector/verifier source pins) — `VPNRouter.Core/Services/RuntimeStatusDetector.cs` + `DeepVerifyProbe.cs`

### r8 review findings — Fable deep review 2026-07-10 of v2.46.0..r7 (all fixed same-day; goal: plans/goal-v2.47.0-r8-review-findings-2026-07-10.md)
- [x] **P1 RESOLVED v2.47.0-r8 (2026-07-10)** — user Cancel mid-deep-verify false-branded in-flight servers `ProtocolHandshakeBlockedLikely` (12h Auto-pool exclusion + RU-block warning): `DeepVerifyProbe.ProbeViaSocksAsync` swallowed EXTERNAL cancellation as `"http timeout"` → `ProxiedHttp` FAIL instead of the typed `Cancelled` phase. Now rethrows when `ct.IsCancellationRequested` (mirrors `MeasureBandwidthViaSocksAsync`); pinned by `DeepVerifyProbeCancellationTests` (2) + the `TcpOk_UserCancelledDeepVerify_ClassifiesAsUntested_NeverBlocked` e2e guardrail — `VPNRouter.Core/Services/DeepVerifyProbe.cs`
- [x] **P1 RESOLVED v2.47.0-r8 (2026-07-10)** — endpoints-only (WG/AWG endpoint egress) custom config passed `Validate` (since r1's WG-endpoint acceptance) but `Inject` wrote the fabricated `"custom-proxy"` tag into `route.final`/rules (the proxy-tag resolver scanned only `outbounds`) → sing-box FATAL at start ("valid" → opaque crash). `FindProxyOutboundTag` now falls through to the first wireguard ENDPOINT's tag; pinned by 2 new positive-Inject `CustomConfigInjectorForkGateTests` — `VPNRouter.Core/Services/CustomConfigInjector.cs`
- [x] **P2 RESOLVED v2.47.0-r8 (2026-07-10)** — build-android AppVersion gate compared only the version CORE, letting an -rN suffix drift through (the v2.25.0-r1→r2 in-app update-loop class; the APK versionName is tag-forced but the EMBEDDED AppVersion drives the in-app check). Now a full-string compare — `.github/workflows/build-android.yml`

### true-split (W1) — reviewer P1 fixes 2026-07-05 (3 fixes landed: engage-fail badge cleanup, null-inet-NIC guard, not-yet-running excluded resolve) — residual follow-ups (fail-SAFE, post-capture covers them)
- [ ] **P2** — true-split: excluded apps NOT on PATH and not running at connect get no driver-level 0-gap until a config change re-engages (ETW-late-engage follow-up) — `VPNRouter.Core/Services/VpnEngine.cs` `TryEngageSplitDriverAsync` — TBD. Fail-safe: `ResolveRunningPath`→`ResolveNameToPath` covers running + on-PATH apps; anything unresolved (e.g. Discord in `%LocalAppData%`) is still routed by the post-capture `process_name → direct` rule. Upgrade: on `EtwProcessMonitor.ProcessStarted` for an excluded name, dorezolve the started process's image path and re-engage (arch plan §5.4).
- [ ] **P2** — true-split: `ProcessImagePath.ResolveNameToPath` resolves only apps on `PATH` (via `where.exe`); an excluded app installed off-PATH and not yet running can't be pre-registered, so it splits only after the ETW-late-engage above lands or a hot-apply re-engages while it's running — `VPNRouter.Core/Services/ProcessImagePath.cs` `ResolveNameToPath` — TBD (same residual root as the ETW item; post-capture routes it meanwhile).

### true-split (W1) — bug-hunt survivors 2026-07-05 (all P2; 3× P1 + 2× P2 already FIXED in the same hunt; these are non-gating hygiene/defense-in-depth, all fail-SAFE today)
- [x] **P2 RESOLVED v2.47.0-r4 (2026-07-10)** — Runtime `.sys` integrity: `EngageLocked` now runs `VerifySysIntegrityLocked()` before `EnsureServiceLocked` — reads the sibling `checksums.sha256`, compares the `.sys` sha256, and on MISMATCH logs a named warning (stale/partial driver, ABI mismatch possible) instead of an opaque later IOCTL failure. Fail-OPEN by design (Windows signature enforcement is the tamper gate; this only diagnoses a stale-but-signed swap). Pure `SplitTunnelPolicy.ParseSidecarHashFor` pinned by 7 `SplitTunnelProtocolTests` — `VPNRouter.Core/Services/SplitTunnelDriverManager.cs`
- [x] **P2 RESOLVED v2.47.0-r3 (2026-07-10)** — SCM `StartService` 1058 (ERROR_SERVICE_DISABLED) now gets a targeted `LastFailureReason` ("service disabled; re-run the installer or `sc config mullvad-split-tunnel start= demand`") alongside the pre-existing 1072 (MARKED_FOR_DELETE → reboot) message, instead of the generic "StartService err=N" (the v2.31.7 silent-generic-failure lesson) — `VPNRouter.Core/Services/SplitTunnelDriverManager.cs:528`
- [ ] **P2** — `GetInternetInterfaceAddresses` filters only our TUN (by name) + WG/AWG/Tailscale (by `IsWireGuardName`); another virtual adapter that is Up + has a v4 gateway (Hyper-V vSwitch, VMware, OpenVPN TAP) could be picked → excluded apps bind to a wrong/dead IP. Design-accepted (§9 "multi-NIC exotica"), diagnosable via the `Internet NIC for split-bind: {Name}` log; requires the rogue NIC to carry a default gateway (Host-Only usually doesn't). Extend the keyword filter if a real mis-pick is reported — `VPNRouter.Core/Services/SplitTunnelDriverProtocol.cs:462` — defer

### SDR / realtime-games cluster — ROOT CAUSE CONFIRMED 2026-07-02 (supersedes the older AWG-payload / Dota / WSAENOBUFS entries below)

Full research: `plans/sdr-research-realtime-games-nat-2026-07-02.md` (Fable, source-read of exact shipped fork) + `plans/mtu-fragmentation-robustness-2026-07-02.md` (my sing-tun/wireguard-go source-read) + action plan `plans/goal-sdr-games-action-plan-2026-07-02.md` + handoffs `plans/handoff-sdr-diagnosis-2026-07-02.md`. Memory `[[sdr-fullcone-nat]]`.

**Confirmed by live r10 diag (Dota `-console`, 2026-07-02):** SDR `RelayNetworkStatus: avail=Failed config=OK anyrelay=Failed` "Unable to communicate with ANY of 28" on **AWG** = pure UDP-relay-ping failure, **NOT DNS** (config=OK proves DNS/config-fetch works → r8 DNS fix validated). On **Hy2 the SDR-failure spam is ABSENT** (works). So it is **transport-dependent = H1**, not NAT/DNS/exit.

- [x] **P1 FIXED v2.45.0-r11 (a1a8997d): WSAENOBUFS send-retry in lx-core; live burst-effect pending tester** — **AWG single-socket WSAENOBUFS choke = the Dota/SDR root cause (H1, CONFIRMED).** SDR fires ~330 UDP relay-pings in a burst; all multiplex into the ONE physical AWG UDP socket; it hits `wsasendmsg: … buffer space … queue was full` (dozens/sec, live-confirmed 07-02), and wireguard-go **drops the batch without retry** → pings never leave → all relays unreachable. Fix options: (2C) **retry-with-backoff on ENOBUFS** in the lx-core bind (`submodules/wireguard-go/conn/bind_std.go` `Send`, `device/send.go`) + raise SO_SNDBUF (7MB now, `conn/controlfns_windows.go`) — touches lx-core, respect the 3 build patches [[awg-windows-lx-patches]]; and/or (2D) **auto-route game-UDP via a Hy2 sibling** when the active profile is AWG (Hy2 rides the burst over one QUIC → works). Immediate user workaround: **play on Hy2**. — `tools/build-singbox-lx.ps1` patch block — v2.45.0-r11+
- [x] **P1 FIXED v2.45.0-r11 (bbcd500d): AWG TUN = endpoint 1420** — **MTU regression: r8's `AwgEndpointMtu=1280` breaks SDR match-connect.** SDR sends UDP payloads ≤1300B (1328B IP) with NO PMTUD (GameNetworkingSockets#22); our 1280 cap drops them even once pings work. Stack has no fallback (source-confirmed: sing-tun system stack DROPS fragments `stack_system.go:571-575`, no MSS-clamp, no PMTUD-signaling; wireguard-go `DefaultMTU=1420`). Fix: endpoint 1280→**1420**, TUN=min(user,1420), no inversion — `VPNRouter.Core/Services/ConfigGenerator.cs:58` — v2.45.0-r11
- [x] **P2 RESOLVED v2.46.0 (Codex r6-r36 MTU cluster)** — **Path-MTU robustness (underlay <1500).** Asked for "path-MTU probe on connect OR a user MTU setting + guidance" — shipped as: `HealthCheck.CheckPathMtu` DF-probe (on-demand doctor/health-check, not per-connect — accepted), user MTU setting with auto-pick (DF-ping descending 1420→1240, refuses below the 1332 Steam-SDR floor), inline <1332 / >1420 warnings — `VPNRouter.Core/Services/HealthCheck.cs:169`
- [x] **NOTE** — **r10 `endpoint_independent_nat=true` (commit 745c1cc2) is a confirmed NO-OP** (parsed `option/tun.go:62`, never consumed; dead since sing-box 1.11; TUN already full-cone). Phase 0: rewrite the misleading comment `ConfigGenerator.cs:1127`, fix r10 GitHub notes, DON'T credit r10 for any "works" report. — v2.45.0-r11
- [ ] **P2** — Pending verification before final fix: run Dota `-console` on **VLESS** too (only AWG-fail + Hy2-ok logged so far); optional exit `tcpdump` (handoff 2) to formally rule out H2 — `plans/handoff-sdr-diagnosis-2026-07-02.md` — TBD

- [x] **P0 RESOLVED v2.44.3** — Auto-failover self-cancelling restart: fixed by `_sessionCts` — the failover restart runs under the session token, NOT the `Stop()`-cancelled probe token (source: `VpnEngine.cs:56-65` comment + implementation; audit batch-1 re-verified 2026-07-09) — `VPNRouter.Core/Services/VpnEngine.cs:1165`
- [x] **P0 RESOLVED v2.44.3** — "VpnEngine has zero start/stop/failover synchronization": `_lifecycleGate` (SemaphoreSlim) now serializes public StartAsync, public Stop() and the failover restart; Stop cancels `_sessionCts` before taking the gate so an in-flight restart aborts instead of resurrecting the tunnel (`VpnEngine.cs:56-66,291,497,722`). Residual NARROWED defect: ApplyAsync is not in the serialized set — tracked as the open P1 below (audit batch-1 #1, 2026-07-09)
- [x] **P1 RESOLVED v2.46.1 (2026-07-09)** — `ApplyAsync` not serialized by `_lifecycleGate`: now gated + session-token check (Stop cancels `_sessionCts` pre-gate, a queued-behind-Stop Apply bails instead of hot-reloading a torn-down sing-box / re-engaging true-split post-Disconnect). Pinned by `VpnEngineHotReloadLifecycleTests.Apply_AfterStop_ReturnsFalse_AndDoesNotRespawn` + `Apply_ConcurrentWithStop_NeverResurrectsTunnel` — `VPNRouter.Core/Services/VpnEngine.cs:579`
- [x] **P1 RESOLVED v2.44.3** — AutoFailover `ResetCycle()` has no production caller: `VpnEngine` OnConnected now calls `_failover?.ResetCycle()` (`VpnEngine.cs:1353`), pinned by `AutoFailoverRecoveryAndPersistTests.ResetCycle_AfterMaxAttemptsExhausted_RestoresFullPool` — `AutoFailoverEngine.cs:360`
- [x] **P1 RESOLVED v2.47.0-r3 (2026-07-10)** — clash_api exposed with no `secret`: now a per-install bearer secret auto-generated in `AppSettingsSane.EnsureSane` (desktop, persisted to config.yaml by `SettingsLoader`) / `AndroidStorage.GetClashApiSecret` (app-private SharedPreferences), emitted as `experimental.clash_api.secret` by `ConfigGenerator` + `CustomConfigInjector` (only when WE create the block), and sent by EVERY consumer — `ClashSingBoxApi` (Bearer header), `ClashLogStream` (WS `?token=`), `SingBoxManager.TryHotReload`/`IsClashApiAlive`, `ConfigSanityCheck.ProbeAsync`, `HealthMonitor`, desktop ConnStats, Android `AndroidSingBoxRuntime.IsRunningAsync` + the java stats-poller. Any installed app / hostile loopback XHR now gets 401. Pinned by `ClashApiSecretTests` (9) — `VPNRouter.Core/Models/VPNConfig.cs` `ClashApi.Secret`
- [ ] **P2** — clash_api secret edge: a user who hand-authors a custom-config `experimental.clash_api` block WITH their own `secret` keeps their block (we never graft ours onto a user-authored controller), but the in-app consumers still send the settings-backed secret → mismatch 401s liveness/hot-reload for THAT user (tunnel still works; status detection + hot-reload degrade to process-restart). Vanishingly rare (custom mode + hand-written clash_api + a secret) and they've taken over the API deliberately. Fix if reported: detect a user-authored clash_api secret at inject time and thread it to the consumer layer — `VPNRouter.Core/Services/CustomConfigInjector.cs` EnsureClashApi — TBD
- [x] **P1 RESOLVED v2.47.0-r3 (2026-07-10)** — LinuxFirewallManager / MacFirewallManager treated an EMPTY processNames list as "arm global kill-switch": a split-tunnel user whose 30s process scan timed out (empty list) had the whole host egress dropped on a crash. `IFirewallManager.CreateBlockRules` now takes an explicit `bool isFullTunnel`; the Linux/macOS managers arm on that INTENT, never on list emptiness (Windows ignores it — per-process rules), and `StartupPipeline` passes the real `settings.App.RoutingMode`. The nft/pf ruleset content + load path are UNCHANGED (only the arm decision moved from count→flag), so unit pins fully cover it. Pinned by the new `SplitTunnel_emptyList_scanTimeout_still_disarms` (Linux + Mac) + updated split tests — `Platform/Linux/LinuxFirewallManager.cs` / `Platform/macOS/MacFirewallManager.cs`
- [x] **P1 RESOLVED v2.44.3** — AutoFailover subscription-leak persist: switch-persist now RELOAD-FRESHes the on-disk settings and saves only the two selector fields, never `_store.Save(_settings)` with the transient resolver aggregate (source: `AutoFailoverEngine.cs:195-210` comment + implementation; pinned by `AutoFailoverRecoveryAndPersistTests`) — `AutoFailoverEngine.cs:202`
- [x] **P2 RESOLVED v2.47.0-r4 (2026-07-10)** — build-singbox-lx now asserts the checked-out HEAD == the pinned commit for BOTH the sing-box-lx fork AND the wireguard-go-awg2-lx submodule, fail-CLOSED (`Assert-GitHead` in .ps1 / `assert_git_head` in .sh) — a moved tag/branch or a dirty checkout can no longer silently bundle an unpinned tree. The go.mod replace-path half stays covered by the existing with_awg/with_xhttp Tags smoke assertion. `pwsh`/`bash -n` clean — `tools/build-singbox-lx.ps1` + `tools/build-singbox-lx.sh`
- [ ] **P2** — No SHA256 integrity pin of the bundled sing-box-lx.exe; build.ps1 `-SingBoxPath` override copies the binary with zero version/tag/checksum validation before bundling — `tools/build-singbox-lx.ps1:29` / `build.ps1:311` — TBD
- [ ] **P2** — AWG H4 reserved-byte receive-clear gate (v2.45.0-r6) is applied only to `conn/bind_std.go` (the active StdNetBind path for AWG). The WARP reserved clear in `conn/bind_windows.go` WinRingBind `receiveIPv4`/`receiveIPv6` (~L465/477) is NOT gated — defensive-only since sing-box-lx uses `NewStdNetBind` directly for AWG endpoints (winrio/WinRingBind never selected), but would clobber H4 if a future change ever routed AWG through WinRingBind — `tools/build-singbox-lx.ps1` (patch block) — TBD
- [ ] **P2** — The lx core carries 3 build-time source patches against the Go-1.26-affected `Leadaxe/wireguard-go-awg2-lx`: OOB nil-guard send+recv (golang/go#77875 WSAEFAULT) + the H4 receive-clear gate. Revisit/drop the two WSAEFAULT patches once the bundled Go toolchain is >= the version with the upstream fix (1.26.2/1.27); the H4 gate is permanent (fork behaviour) — `tools/build-singbox-lx.ps1` — TBD
- [ ] **P2** — LeakProtection.ValidateOutboundServersScopeAware never cross-checks the AWG peer endpoint IP (iterates only `config.Outbounds`; the AWG egress peer lives in `config.Endpoints[].Peers[]`) — defense-in-depth only, benign today — `VPNRouter.Core/Services/LeakProtection.cs:527` — TBD
- [x] **P1 RESOLVED v2.46.1 (2026-07-09)** — Custom-config raw JSON bypasses the awg/xhttp gates: `CustomConfigInjector` now detects AWG obfuscation fields (jc/jmin/jmax/s1-s4/h1-h4/i1-i5 on a wireguard endpoint; plain WG endpoints are official 1.11+ and stay ungated) + `transport.type=xhttp`, gates on `SingBoxFeatures` tags in BOTH `Validate` (actionable error at paste time) and `Inject` (runtime backstop throws instead of a sing-box FATAL); `Validate` also accepts a wireguard endpoint as the proxy egress. Pinned by `CustomConfigInjectorForkGateTests` (7) — `VPNRouter.Core/Services/CustomConfigInjector.cs`
- [x] **P2 RESOLVED v2.47.0-r3 (2026-07-10)** — SingBoxFeatures first probe could run synchronously on the UI thread (≤5s stall on an awg:// paste). Added `SingBoxFeatures.Prewarm()` (fires the cached, thread-safe probe on a background Task; no-ops when overrides set) called once at desktop startup (`Program.cs`, before OrphanCleanup). Pinned by `SingBoxFeaturesGateTests.Prewarm_WithOverridesSet_NoOps_AndDoesNotThrow` — `VPNRouter.Core/Services/SingBoxFeatures.cs`
- [ ] **P2** — SingBoxFeatures default probe reads the installed binary; on a dev box where that binary IS a sing-box-lx build, a test touching AwgAvailable/XhttpAvailable without an Override probes TRUE and inverts the default-closed contract (contained today by convention — all fork tests set overrides) — `VPNRouter.Core/Services/SingBoxFeatures.cs:67` — TBD
- [x] **P2 RESOLVED v2.47.0-r3 (2026-07-10)** — (Codex review) `LeakProtection` recognises the AWG `proxy` endpoint but didn't validate its contents. `ValidateConfig` now runs `ValidateProxyEndpoint` on the `proxy` endpoint (empty private_key / no local address / no peers / a peer missing public_key, endpoint address, or a valid port → actionable errors instead of a bare sing-box FATAL). Runs in both StartAsync + Apply. Pinned by `LeakProtectionAwgEndpointTests` (7) — `VPNRouter.Core/Services/LeakProtection.cs`
- [x] **P2 RESOLVED v2.45.0-r11 (bbcd500d): band-aid deleted entirely (DNS root cause fixed; Dota failure is WSAENOBUFS not DNS)** — (Codex review) game-DNS `ResolveGameDnsOffProxy` adds roblox/rbxcdn -> local-dns even when `StrictDns=true`, breaking "all DNS through VPN" — guard with `!strictDns` — `VPNRouter.Core/Services/ConfigGenerator.cs` BuildDns/game-DNS rule — TBD
- [x] **P2 RESOLVED v2.46.1 (2026-07-09, R1)** — (Codex review) DeepVerify AWG/XHTTP parity: BOTH halves shipped — real verify when the core carries the tag (AWG = top-level endpoint via the shipped `ConfigGenerator.BuildAmneziaWgEndpoint`, xhttp = mirrored transport with Vision-flow drop) AND typed `DeepVerifyFailurePhase.UnsupportedByVerifier` when it doesn't (mapper → Skipped → classifier reads "protocol untested", never a block). Pinned by `VlessDeepVerifierProtocolSupportTests` — `VlessDeepVerifier.cs`
- [x] **P2 RESOLVED v2.47.0-r3 (2026-07-10)** — (Codex review) UX: `SimpleInputDetector` now classifies `awg://`/`amneziawg://` as ServerUri (was "invalid link"; apply-time SingBoxFeatures gate still refuses on a non-lx core, like naive/dns-tunnel). The `ServerViewModel` half was STALE — the amneziawg/awg subtitle + ProtocolUseCase branches already shipped in v2.45.0-r3 (ServerViewModel.cs:555). Pinned by 2 new `SimpleInputDetectorTests` cases — `VPNRouter.App/SimpleInputDetector.cs`
- [x] **P2 STALE/RESOLVED (verified 2026-07-10)** — (Codex review) QUIC-reject suppression: current code (`ConfigGenerator.cs:135-139`) already keys it to `proxyIsUdpNative || proxyIsUdpNativeOutbound` (AWG endpoint OR a UDP-native Hy2/TUIC sole outbound), NOT `endpoints.Count>0` alone — fixed by the Hy2 sole-proxy work (v2.45.0/v2.46.0). No change needed — `VPNRouter.Core/Services/ConfigGenerator.cs:139`
- [x] **P2 RESOLVED v2.47.0-r3 (2026-07-10)** — (Codex review) ops script `plans/roblox-tester-exit-setup.sh` hardened: `umask 077` at top; hy2 installer downloaded to a temp file + sha256 printed for review/pinning (not `bash <(curl…)` straight to root); SSH-lockout guard detects real sshd port(s) via `sshd -T` + the current `SSH_CONNECTION` port and allows them before `ufw enable`; `chmod 600` on key.pem/config.yaml/tuic-config.json; secrets `tee`'d to a root-600 `/root/roblox-tester-secrets.txt` with a scrollback warning. `bash -n` clean — `plans/roblox-tester-exit-setup.sh`
- [x] **P2 → PROMOTED to CONFIRMED P1 in the SDR cluster at top of ## Open (2026-07-02)** — AWG endpoint can hit Windows UDP send-buffer exhaustion under game UDP load (`wsasendmsg: ... lacked sufficient buffer space or because a queue was full`); app-level fix is not clear from diag, needs live/core investigation without touching AWG v2.45.0-r6 blindly — `C:\Project\logs\codex-diag-20260630\awg\singbox-old-tail.log:14523` — TBD
- [x] **P1 → REFUTED 2026-07-02: the AWG tunnel forwards TCP+UDP fine (Roblox + Steam-TCP work live; SDR pings ride the tunnel); the bare-SOCKS-harness "stall" was DNS+MTU, not server forwarding — see SDR cluster above + [[awg-fulltunnel-payload-forwarding-gate]]** — AWG full-tunnel payload stalls after a successful peer handshake, REPRODUCED on 2 independent servers/keypairs: WINBRAT live test with `main-brat` (`10.66.0.22/32` -> `93.95.226.167:51822`) gets `received handshake response` / keepalives, but TCP via both TUN and a no-TUN SOCKS harness times out; SAME signature reproduced 2026-06-30 against a second, fully independent endpoint (`10.66.0.23/32` -> `104.194.156.93:51820`, different keys/jc/jmin/jmax/s1/s2/h1-h4) — handshake + bidirectional keepalives succeed, but a DNS-free raw-IP SOCKS probe (`curl --socks5 ... https://1.1.1.1/cdn-cgi/trace`) still times out (`http_code=000`) after the full 20s budget. Reproduction on 2 unrelated peer configs makes server-side `AllowedIPs` mismatch (silently drops real payload packets post-decrypt while empty keepalives — which carry no inner IP header — keep flowing) the leading hypothesis over a one-off VPS misconfig; `ip_forward`/MASQUERADE still possible. DNS-bootstrap fix itself is NOT implicated — 0 `DNS query loopback` in both runs, the DoH-bootstrap hostname resolves cleanly via real-NIC local-dns before the stall. SSH not available for either VPS; server proof/fix needs owner access — `plans/awg-server-forwarding-gate-2026-06-30.md`, `plans/goal-codex-awg-fulltunnel-dns-distribution-2026-06-30.md` Phase 1.1 evidence 2026-06-30 — TBD.
  **UNRESOLVED TENSION (2026-07-01)**: owner's live production app, same VPS (`93.95.226.167:51822`, `main-brat`), full-tunnel AWG connected — a browser leak-test through the tunnel got a real response (see the DNS-leak entry below) and Roblox works stably. That means TCP payload IS flowing through THIS AWG connection right now. The stalled-payload finding above used different-session AWG creds via a bare SOCKS harness (not the production TUN app) — those creds may simply be stale/mismatched (same class of problem as the xhttp path going stale below), not proof the server is universally broken. Don't treat the P1 above as still-confirmed without re-testing against the CURRENT production `main-brat` AWG credentials from `config.yaml` before acting on it either way.
- [ ] **P2** — Server-side: xhttp VLESS inbound on `93.95.226.167:9443` (path `/U4YILqMxZg2N6sIKQFLt4w`, `main-brat`) returned `v2ray-xhttp: unexpected status: 404 Not Found` on EVERY request (dozens of distinct DNS/connection attempts over the full 17.5s warm-up window, 0 successes) — client-side TLS+Reality handshake succeeded (got a real HTTP 404 back), config-gen verified correct (`path` round-trips byte-exact from the URI), so this was the server's xhttp inbound not routing this path, not a VPNRouter/sing-box-lx bug at the time. Owner reported the VPS got fixed and a connection briefly succeeded — **REOPENED same day**: owner now reports xhttp still does NOT connect from the VPNRouter app itself, while a non-VPNRouter iPhone client connects fine against the same server. No fresh diagnostics captured yet for this current failure mode (the 404 evidence above predates the VPS fix and no longer explains today's symptom) — needs a new diag bundle taken immediately after a fresh failed connect attempt, and a comparison of how sing-box-lx's xhttp client (mode/padding/headers) differs from whatever client the iPhone side used — `C:\Project\logs\VPNRouter-diagnostics-20260701-003612.zip` (stale, pre-fix) — TBD
- [ ] **P1** — DNS leak under AWG full-tunnel: owner connected `main-brat` (AWG, `93.95.226.167:51822`, `routing_mode: full`, confirmed "Подключено [full]"), ran a browser IP/DNS-leak test — IP correctly showed the tunnel's Iceland exit (no IP leak), but the DNS-leak panel of the same test showed 3 resolvers (`195.2.238.4`, `195.2.239.4`, `195.2.238.58`, all "Russia, Trytek LLC", 300/300 hits) that match NONE of the configured DNS servers (`vpn-dns`=AdGuard/local proxy-detour, `local-dns`=1.1.1.1 dns-direct) — real DNS leak to what looks like the user's local/ISP resolver despite `route.rules[protocol=dns, action=hijack-dns]` supposedly catching all DNS traffic. Current toggles at time of test (`config.yaml`): `strict_dns: true`, `force_ipv4_only: true`, `flush_dns_on_start: true`, but `dns_leak_lockdown: false` (the firewall port-53 backstop was OFF, so nothing would have blocked/surfaced this leak even as a hard failure). This is exactly the open question in `plans/tz-codex-awg-dns-leakprotection-audit-2026-06-28.md` item 1 ("does hijack-dns/dns.final actually work the same for an AWG `endpoints[]` wireguard block as for a normal VLESS outbound, on a live tunnel") — now with live reproduction. Suspected same root cause as the Dota2 entry below — TBD
- [x] **P2 → DIAGNOSED 2026-07-02: NOT DNS (live console showed config=OK), it is H1 (AWG single-socket WSAENOBUFS choke); Hy2 works — see CONFIRMED SDR cluster at top of ## Open. The "investigate DNS first, don't chase WSAENOBUFS" guidance below is now inverted.** — Dota2 matchmaking region-latency test fails for EVERY region simultaneously while connected via AWG full-tunnel (`main-brat`), generic client error "Не удалось вычислить задержку. Проверьте интернет-соединение." Owner: Roblox works stably on the same AWG connection; suspects DNS. All-regions-at-once (not just distant ones) is consistent with a systemic DNS-resolution failure for Dota2's own region-ping mechanism rather than per-region network timeouts — likely the SAME root cause as the DNS-leak entry above (if Dota2 resolves matchmaking hostnames via the leaking path, and/or the resulting IPs hit Russia's TSPU foreign-IP freeze per the Roblox-277 RCA pattern). Investigate the DNS-leak entry FIRST and retest Dota2 after — don't chase this as an independent WSAENOBUFS-class bug (see the existing P2 WSAENOBUFS entry above) until the DNS hypothesis is ruled out — TBD
- [x] **P1** — (Codex perf/stability hunt, validated) External/3rd-party sing-box treated as VPNRouter (false "connected" + takeover kills an unrelated tunnel) — RESOLVED v2.45.0 (`ProcessOwnership` image-path gate; commit b60d33fe)
- [x] **P1** — (Codex perf/stability hunt, validated) StrictDNS failover commits state + event before hot-reload success — RESOLVED v2.45.0 (commit-only-on-reload-success reorder; commit f22b7351)

- [x] **P2 RESOLVED v2.47.0-r4 (2026-07-10)** — Deep-verify-all on slow hardware gave false "protocol untested": the flat 1500ms SOCKS-bind warmup was too short when `MaxConcurrency=5` sing-box processes contend for CPU on a slow VM. The wait is now adaptive — `EffectiveSocksBindWait = SingBoxWarmup + 300ms × (MaxConcurrency−1)` (2700ms at the default 5), still bounded by the 12s OverallTimeout so a dead spawn never hangs. Pinned by `VlessDeepVerifierTests.EffectiveSocksBindWait_ScalesWithConcurrency` (3) — `VPNRouter.Core/Services/VlessDeepVerifier.cs`
### "VPNRouter deleted after reboot" (Windows) — investigated 2026-07-09, root cause = AV, NOT app self-deletion
- [x] **CORRECTION** — earlier "SelfRepair guts the install" and "RestoreSnapshot move-order guts app/ at boot" were BOTH refuted on re-check: (a) `install.ps1` has `$ErrorActionPreference="Stop"` (line 48) + downloads/sha-verifies BEFORE the wipe, so a network failure halts before touching `app/`; (b) `RestoreSnapshot` is only ever called from the running `App.exe` which lives inside `app/`, so `Directory.Move(app→stage)` fails FIRST (live-proven on brat: renaming a dir with a running exe throws "used by another process") — the dangerous `Move(bak→app)` window is unreachable. The brat "gutting" I saw was my own test overlay command hitting locked files, not either code path.
- [ ] **P1 — CI PREP LANDED 2026-07-10, ENROLLMENT USER-GATED** — Root cause (evidence-based): the binaries are UNSIGNED (`Get-AuthenticodeSignature` = NotSigned on App/sing-box/Service) + do TUN/process-scan/firewall, so an AV quarantines them on a reboot scan → the user sees "deleted". Mitigations landed 2026-07-09 (`antivirus-integrity.txt` diag capture; install.ps1 verified Defender exclusion + manual guidance). **Real fix = code-signing.** Now PREPPED: `.github/workflows/sign-windows.yml` (SignPath.io Foundation, free/OSS — manual `workflow_dispatch`, fail-fast until secrets exist, mirrors sign-android) + turnkey enrollment runbook `plans/code-signing-signpath-runbook-2026-07-10.md`. REMAINING = the SignPath OSS enrollment itself (account + application submission — owner-only, can't automate) + adding 5 repo secrets, then run the signer per ship. — `.github/workflows/sign-windows.yml` / `packaging/windows/install.ps1`
- [ ] **P2** — (perf hunt F2 follow-up) Conn-stats poll has no visibility/minimized throttle — it polls `/connections` every 2s even when the stats line is off-screen or the window is minimized (streaming parse already landed in F2) — `VPNRouter.App/ViewModels/MainWindowViewModel.ConnStats.cs` — TBD
- [x] **P2 RESOLVED v2.46.1 (2026-07-09, R5)** — (perf hunt F3 follow-up) AutoSelect `GetGroupNowAsync("proxy")` now polled every 3rd stats tick only (first tick immediate so the label appears right after connect; tick resets on disconnect); Windows characterization hash re-pinned as the entry predicted, Linux pin follows from CI — `VPNRouter.App/ViewModels/MainWindowViewModel.ConnStats.cs`

### dependency-review 2026-07-09 — executable-code download verification
- [x] **P1 RESOLVED v2.47.0-r4 (2026-07-10)** — TgProxyUpdater installed a Python interpreter + PyPI wheels (cffi/cryptography = compiled C/Rust) under the user's account with ZERO integrity check. Now fail-closed: wheels verified against PyPI's own `urls[].digests.sha256`; the python.org embeddable pinned to a captured-from-canonical sha256 constant (`PythonZipSha256`, recompute on version bump). Single primitive `VerifyPinnedSha256Static`, 4 pins in `TgProxyDigestVerifyTests` — `VPNRouter.Core/Services/TgProxyUpdater.cs` (dep-review P1-2)
- [ ] **P2** — ZapretUpdater verifies only the download SIZE of `Flowseal/zapret-discord-youtube` winws.exe (runs with admin); documented trade-off (auto-fresh bypass strategies). Action-minimum DONE 2026-07-10: trust root + size-only-verification + residual risk now documented in `tools/native-deps.md` (Trust roots section). REMAINING option (this entry): known-good tag pin in the VPNRouter release + explicit user confirm before upgrading to an unverified tag — `VPNRouter.Core/Services/ZapretUpdater.cs` — dep-review P2-1, TBD
- [ ] **P2** — WgturnUpdater's `expectedSha256` path is wired but `ResolveAssetFor` always returns null (upstream — our own repo — publishes no sidecar). Fix on the PUBLISHING side: emit `.sha256` sidecars in the wgturn release, then have ResolveAssetFor read them — `VPNRouter.Core/Services/WgturnUpdater.cs` — dep-review P2-2, TBD (cross-repo)
- [x] **P1 LANDED on main 2026-07-10 (net10 CI-green; ships in v2.47.0)** — .NET 8 EOL 2026-11-10: net8.0 → net10.0 TFM migration DONE (commit adb1d215). All desktop projects (App/CLI/Service/Tests/PoolAggregator/VpnRouterTestMcp) + root global.json → net10.0/10.0.301; Android was already net10.0-android36.0. **The spike's CS0122 root cause was NOT "stragglers"** — it was three `net8.0`-CONDITIONED MSBuild blocks in `VPNRouter.Core.csproj` that silently stopped applying on net10.0: the `<InternalsVisibleTo Include="VPNRouter.Tests">` ItemGroup (→ CS0122 on the internal SplitTunnelDriverProtocol/NicSnapshot/StartupHostInternal), the `PLATFORM_WINDOWS` DefineConstants PropertyGroup (would have SILENTLY compiled out every `#if PLATFORM_WINDOWS` block — firewall/TUN/ETW/registry — a functional break the 0-error build hides), and the Windows-only package ItemGroup. All rescoped net8.0→net10.0 / net8.0-android→net10.0-android. System.Management + Microsoft.Win32.SystemEvents + System.Drawing.Common 8.0.0→10.0.0. NuGet restore clean (0 NU1102 — no other package bumps needed). Characterization hash re-pinned (reflection-derived → net10 metadata shifts it, zero MVM source change). CI setup-dotnet 8.0.x→10.0.x (6 workflows). Verified: full sln 0 errors, 2550 local tests pass (28 = dev-box ProgramData-lock baseline), **Linux CI test+grep GREEN on net10**. A 14-agent adversarial validation workflow (2026-07-11) confirmed the migration itself clean (all TFM conditions rescoped, char re-pin legit — 1480 members identical net8↔net10, CI clean) but found + FIXED two blockers: **B1** `.mcp.json` still pointed at the net8 MCP dll path (→ net10.0-windows apphost); **B2 (critical)** the in-app update zip is APP-ONLY (no .NET runtime DLLs — only the full install zip has them), so a net8 user auto-updating to net10 would run net10-compiled app DLLs on the FROZEN net8 coreclr → guaranteed crash of the WHOLE installed base (the GUI trampoline's app-DLL-hash self-repair can't see a uniform runtime/app mismatch). Fixed in build.ps1: the runtime DLLs now travel in the update `_bootstrap/` (same rule as sing-box/cronet; the Go trampoline xcopies `_bootstrap/*` wholesale, so they apply). Also hardened the 6 CI workflows setup-dotnet `10.0.x`→`global-json-file`. **-r10 SHIPPED 2026-07-11** (commit a12c0bb3; 14 desktop assets, prerelease, Latest restored to v2.46.0, r9 release deleted; tag-CI green incl. Auto-Update Integration Test + Verify Release Integrity; commit-CI fully green). **B2 fix CONFIRMED at artifact level**: the r10 update-zip (71 MB, was app-only) now carries the full net10 runtime in `_bootstrap/` — coreclr.dll, clrjit.dll, System.Private.CoreLib.dll, hostfxr.dll, hostpolicy.dll, mscorlib.dll, clrgc.dll — verified by unzip listing. **windows-brat live-update gate — PASSED 2026-07-11 (B2 fix PROVEN, both production paths).** Clean net8 r9 install (coreclr 8.0.2426) → cross-major update to net10 r10:
(1) **trampoline-apply path** — r10 update-zip staged in appDir → real VPNRouter.GUI.exe xcopied `_bootstrap/*` → coreclr 8.0→10.0, App.exe (r10, commit a12c0bb3) launched + rendered full UI, clean log;
(2) **helper.cmd path (the v2.31.7 bug class)** — real `CLI test-update --staged-dir` → ApplyAsync → helper.cmd cmd.exe `xcopy _bootstrap\* → appDir` exit=0, coreclr 8.0→10.0 (relaunch suppressed only by VPNROUTER_CI=1); launched from the helper-produced appDir → net10 App.exe rendered.
Both paths: runtime major swapped, app boots + renders (Avalonia 12 on net10), no [ERR]/Exception/FATAL. Without the B2 fix the net10 app DLLs would have run on a frozen net8 coreclr → instant crash of the whole installed base. **B2 CLOSED.** — dep-review P1-1

## Resolved (history)

- [x] **P0** — Auto-failover false-positive teardown of a healthy connection (post-start delay-test 503 → tore down a working server) — RESOLVED v2.44.2 (warmup-confirmed gate, `VpnEngine.ShouldAutoFailoverAfterProbe`)
