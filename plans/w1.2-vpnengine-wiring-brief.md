# W1.2 — VpnEngine wiring: inject the split-tunnel driver into the lifecycle

**Owner**: Claude session (Opus 4.8)
**Branch**: main (continues P3 `3cac6f43`)
**Roadmap ref**: `plans/goal-w1.1-implementation-2026-07-04.md` (W1.2) · arch `plans/w1.1-architecture-and-plan-2026-07-04.md` §5
**Effort**: ~1-1.5 day
**Risk**: LOW-MED — patterns are ready (`IWindowsDnsHardening` seam), but the touch points are inside the 1459-line **god-file `VpnEngine.cs`**, so each hook must be surgical + fail-open.
**Blast radius**: `VpnEngine.cs` (+ctor param + 4 hooks), `PlatformServices.cs` (create the real manager on Windows), `ProcessImagePath.cs` (+`ResolveNameToPath` extraction, §1.4), new `VPNRouter.Tests/Fakes/FakeSplitTunnelDriver.cs` + lifecycle tests.
**Rollback**: `git revert` — the manager stays inert if the field is null (non-Windows / not created).

## Why
P1-P3 delivered + live-proved the driver core, but nothing wires it into the app yet. W1.2 makes the exclude-mode connect actually engage the driver (and disconnect disengage it), so excluded apps get the true OS-level split — while keeping the hard fail-open invariant: a null/failing driver leaves today's post-capture routing untouched.

## What (4 hooks, arch §5.1 — all fail-open, manager never throws)
- **Ctor** (`VpnEngine.cs:203`): add `ISplitTunnelDriver? splitDriver = null` param + `private readonly ISplitTunnelDriver? _splitDriver` field (mirror `IWindowsDnsHardening`). Null on non-Windows / tests-without-fake.
- **Hook 1 — engage** (end of `StartAsyncInternal` success branch, by the Unix-DNS block ~line 364): gate on `SplitTunnelPolicy.ShouldEngage(OperatingSystem.IsWindows(), settings.App.RoutingMode, settings.App.RoutingAppsMode, hasExcluded, driverSetting)` + yaml `true_split_driver: auto|off` (default auto). Resolve `settings.App.RoutingAppsExclude` names → DOS paths via `ProcessImagePath.ResolveNameToPath`; unresolved → log + skip (post-capture still covers). `await _splitDriver.EngageAsync(new SplitTunnelEngageRequest(dosPaths, settings.Tun?.Ipv4Address, settings.Tun?.Ipv6Address), ct)`. Covers failover restarts (they re-enter StartAsyncInternal) — engage is idempotent.
- **Hook 2 — re-engage** (end of successful `ApplyAsync` ~line 505): if `_splitDriver?.IsEngaged == true` OR `ShouldEngage` now true → re-`EngageAsync` with re-resolved paths (closes excluded-list change, TUN-IP change, include→exclude flip). `ShouldEngage`-false now (went include/full) → `DisengageAsync`.
- **Hook 3 — disengage** (`TeardownInternal` ~line 663, AFTER `_singBox?.Dispose()`): `_splitDriver?.DisengageAsync(...)` synchronously awaited with a 5 s timeout (teardown is sync; manager never-throws). Order matters — excluded stay on NIC binds until the last ms, then TUN is gone so new binds hit the same physical NIC. 0-gap.
- **Hook 4 — dispose** (`Dispose` ~line 757): `try { _splitDriver?.Dispose(); } catch { }` in both branches.
- **Startup sweep**: call `SweepStaleStateAsync` once at engine start (heals a stale ENGAGED from a crashed prior session in a never-engage session).

## Reuse (§1.4)
Extract `FirewallManager.ResolveProcessPath` logic into `internal static ProcessImagePath.ResolveNameToPath(string name, IProcessRunner runner, ILogger?)` (running-process image via `ResolveRunningPath` + `where.exe` fallback); FirewallManager delegates there (root-cause single source of truth, ~30-line diff).

## Contract with W0.1 (§5.3 — pin with a test + a comment at hook 3)
HealthMonitor recovery (`OnSingBoxCrashed → AttemptRestart → Restart()`) happens **inside a live VpnEngine, without Stop()/StartAsync** → hooks 1/3 don't fire → **driver stays ENGAGED through the whole down-window + restart**. That IS the point: excluded live through the NIC while sing-box is down. After relaunch the TUN gets the same IP → no re-register needed.

## Tests (fake driver, zero Windows deps — arch §5.6)
`FakeSplitTunnelDriver` (capture-only, in Tests/Fakes) + lifecycle tests mirroring `VpnEngineDnsLockdownLifecycleTests`: engage called after Start in exclude-mode; NOT in include/full/empty-list/`off`; disengage in Stop before firewall-teardown + in Dispose; ApplyAsync excluded-list change → re-engage; include flip → disengage; fake returning `false` from Engage → Start still succeeds (fail-open sweep).

## Verification gate
- [ ] Gate 1 build 0 err · [ ] Gate 2 suite green (new lifecycle tests) · [ ] Gate 3 docs · [ ] Gate 4 simplify + security-review (god-file edit) + independent review · [ ] Gate 5 **LIVE W0.1-contract on brat**: connect exclude→engaged; disconnect→disengaged; excluded-list change→re-engage; `taskkill /f sing-box` → HealthMonitor recovery while an excluded curl-loop loses **zero** requests · [-] Gate 6 (characterization: VpnEngine is a god-file — if the wiring risks behavior drift, snapshot the non-split lifecycle before/after). `bug-hunt` skill after this phase.

## Outcome — CODE (filled 2026-07-05; live W0.1-contract pending)

**Status**: PARTIAL — code done + unit-verified; the live W0.1-contract (Gate 5) is the remaining step.
**Ponytail simplifications vs the brief:**
- **Hook 2 — I first DROPPED it on a wrong trace, the review caught it, RESTORED.** My reasoning ("forceRestart re-enters StartAsyncInternal so hook 1 re-fires") was false: `ApplyAsync` forceRestart calls `SingBoxManager.Restart()` (sing-box PROCESS restart), NOT `StartAsyncInternal`. So an excluded-set edit while connected would leave the driver's `SET_CONFIGURATION` stale until reconnect (un-excluding an app wouldn't take effect — post-capture covers traffic, but the driver's OS-split lags). Added a 1-line `await TryEngageSplitDriverAsync` at the forceRestart-success point. **Lesson: lazy about the trace = confident wrong fix; the independent review earned its keep.**
- **Reused** `ProcessImagePath.ResolveRunningPath` directly (no `ResolveNameToPath` extraction — YAGNI until a second non-running-app caller needs the where.exe fallback).
- **Skipped** the `true_split_driver` yaml toggle (hardcoded `"auto"` + ponytail comment) and the standalone startup `SweepStaleStateAsync` call (the engage path already RESETs stale state) — add each only when a real need lands.

**Files**: `VpnEngine.cs` (+field +ctor param +`TryEngageSplitDriverAsync` +hooks 1/3/4), `PlatformServices.cs` (+`CreateSplitTunnelDriver`), `SplitTunnelDriverManager.cs` (interface+record internal→public so the public ctor/factory can take them, mirroring `IWindowsDnsHardening`), `VpnEngineDnsLockdownLifecycleTests.cs` (+`BuildEngine` param +3 tests), new `Fakes/FakeSplitTunnelDriver.cs`, Core CLAUDE.md.

**Gate results:**
- [x] Gate 1 — build: 0 errors.
- [x] Gate 2 — tests: **79/79** SplitTunnel+wiring green; the 3 new wiring tests (`SplitDriver_StartHook_EngagesInWindowsExcludeMode` [Windows-conditional], `_SkipsIncludeMode`, `_Dispose_ReleasesDriver`) pass. The only VpnEngine*Lifecycle failures are the CI-only dev-box ProgramData baseline (unchanged by this edit).
- [x] Gate 3 — docs: CLAUDE.md VpnEngine row + this brief.
- [x] Gate 4 — self-review: **independent review** of the god-file wiring → confirmed deadlock-safe (`.Wait(5s)` under `_lifecycleGate`, manager `ConfigureAwait(false)` throughout — no sync-over-async deadlock even from the UI thread), ordering correct (0-gap), fail-open airtight, decision logic + public-interface flip clean. Caught **1 MED**: my "drop hook 2" was a wrong trace (forceRestart restarts the sing-box process, not StartAsyncInternal) → **hook 2 restored** + false comments fixed (VpnEngine hook 1, CLAUDE.md).
- [ ] Gate 5 — **LIVE W0.1-contract on brat** — PENDING (real app connect in exclude-mode → engaged; disconnect → disengaged; `taskkill /f sing-box` → HealthMonitor recovery while an excluded curl-loop loses ZERO requests). Bigger live setup than P3 (full app + real VPN) — scoped follow-on.
- [-] Gate 6 — characterization: N/A (wiring is additive + fail-open; no non-split behavior path changed — hooks are `_splitDriver?.X()` no-ops when null).

**Rollback**: `git revert` — driver stays inert if the field is null.
