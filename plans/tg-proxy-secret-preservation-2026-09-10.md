# Telegram proxy secret preservation checkpoint

Inspected tip `94e89ad0523359838a969e53ec7094e9a1cec486` of `sentinel-redact-tg-proxy-secret-7376106324028721870`. Its commit adds explicit tg[_-]?proxy[_-]?secret to the log-key regex and a test for underscore and hyphen spellings. Main `0bcc8166510310d39e442b09a3e0e804eb94dd01` already tests tg_proxy_secret through Logs_RedactPrefixedSecretKeys, but the inspected test file has no tg-proxy-secret input.

Current regex can match proxy-secret after the first hyphen; replacement preserves unmatched prefix and captured key, so partial matching alone does not establish either key corruption or value leakage. Separatorless spellings accepted by the old optional separators need separate comparison. No runtime failure inferred. Follow-up: GitHub identifies this as closed PR178; full merge-base net diff is exactly the inspected two files (13 additions, one deletion). PR body specifies underscore and hyphen spellings, not separatorless forms; optional-separator regex breadth is therefore not by itself proof of intended missing functionality. Closure explicitly cites #193 prefixed-secret production redactor plus #201 tg_proxy_secret regression coverage and says no unique change remains. Current source supports the intended underscore/hyphen behavior, but the original hyphen test case is not explicit in the inspected current fixture. Retain pending disposition of that small test-data difference rather than treating the closure comment as proof of all original assertions. This is not authorization to expand the approved auth_key/client_key test-only change or to alter production regex.
