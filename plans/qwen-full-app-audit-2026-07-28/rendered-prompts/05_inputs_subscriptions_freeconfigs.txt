READ-ONLY AUDIT. Do not edit, create, delete, format, or stage any repository file. Do not launch VPNRouter, sing-box, installers, services, firewall commands, network-changing tests, or anything that writes ProgramData. Read AGENTS.md and the relevant zone CLAUDE.md before reviewing. Inspect the current working tree, not only the latest diff. Trace real callers and callees end to end. Ignore formatting, naming taste, speculative refactors, and issues already listed in plans/OPEN-DEFECTS.md unless you have new evidence that materially changes their severity or scope. Report only reproducible code defects or missing tests that conceal a concrete defect. Every item must cite an existing file and exact line. If nothing survives, output []. Output only one JSON array using the schema from the reviewer brief.

ASSIGNED REVIEW
Title: Settings, migrations, subscriptions, profiles and free configs
Scope: SettingsLoader/Migrator, ProfileManager, SubscriptionFetcher/Resolver, VlessUriParser, VlessServersResolver, Services/FreeConfigs, user profile/import parsing, and their callers
Distinct lens: Treat YAML, JSON, URIs, HTTP bodies, cache files, profile files and remote pools as untrusted. Check size/time bounds, malformed-data recovery, atomic writes, stale-cache preservation, dedupe identity, secret handling, parser ambiguity, migration data loss, and subscription-to-generator ordering.

The verbatim project reviewer brief follows. Its diff placeholders are intentionally superseded by the live subsystem scope above; inspect those files and their real callers/callees in the current working tree.

# Review-agent prompt â€” VPNRouter

Adapted from `methodology-toolkit/METHODOLOGY.md` Â§7.1 (audit P0 item 3). Spawn
an INDEPENDENT reviewer BEFORE every commit/ship that touches code â€” skip ONLY
if the hotfix short-circuit applies (â‰¤5 lines + ONE surface + no contract/
behaviour drift, Â§5). Brief it like a new colleague: paste the FULL diff +
invariants verbatim, never "as discussed above". The agent sees only what you paste.

    Agent(subagent_type: "general-purpose", prompt: <the block below + the diff>)

---

```
You are an independent code reviewer for VPNRouter, a process-based split-tunnel
VPN router for Windows/macOS/Linux/Android (.NET 10 + Avalonia MVVM + sing-box
TUN + VLESS/Reality). You haven't seen the design discussion, only the diff below.

Architectural invariants (cannot be violated):
- AppVersion.Version (VPNRouter.Core/AppVersion.cs) MUST equal the release tag
  EXACTLY, including the -rN suffix. A mismatch silently breaks update-check.
- process_name matching is CASE-SENSITIVE (sing-box Go map). NEVER ToLowerInvariant()
  in ConfigGenerator / ProcessScanner / HealthMonitor. Dedupe via
  StringComparer.OrdinalIgnoreCase but preserve original filesystem casing.
- Handle safety: NEVER GetProcessesByName(...).Length on a polling path â€” use
  ProcessQuery.AnyAlive / CountAlive (they dispose the Process[]). pre-commit
  Gate 7 hard-fails the bare pattern.
- VlessServersResolver.Resolve MUST run before ConfigGenerator.Generate (or go
  through VpnEngine, which does) â€” else a silent leak (no proxy outbound, v2.28.2).
- Fail-closed: route.final = proxy in full-tunnel / exclude mode; a route rule
  pointing at a missing 'proxy' outbound is a leak (LeakProtection catches some).
- SingBoxManager.Stop(): set _process.EnableRaisingEvents = false BEFORE Kill(),
  else the Exited callback fires as a false crash.
- Auto-failover restart must NOT re-enter StartAsync under a token Stop() cancels
  (the _probeCts self-cancel, v2.44.2/.3) â€” use a fresh lifetime token. And it
  must not resurrect the tunnel after a user Disconnect.
- Firewall kill-switch: an EMPTY processNames list must NOT silently mean "arm
  global egress drop" â€” full-tunnel intent must be explicit (split-tunnel
  host-brick risk; open P1 in plans/OPEN-DEFECTS.md).
- All user-visible strings go through Strings.cs (Ru/En). No hardcoded Russian/
  English in ViewModels / XAML / toasts.
- NO emoji in code/config/docs (project rule). Technical symbols are fine.

Secrets â€” never log / commit / leak in error messages:
- Subscription URLs, Reality public_key/short_id, uuid, clash_api token. Outward-
  facing errors stay generic (no host/IP/key/path leak).

Files changed:
{paste: git diff --name-only HEAD~N..HEAD}

Diff:
{paste: git diff HEAD~N..HEAD  â€” full, no truncation; split if huge}

Find issues in priority order:
1. CORRECTNESS â€” bugs, swallowed errors (empty catch{}), async races, leaked
   handles/timers/HttpClient, cancellation-token lifetime bugs, unhandled
   exceptions on the connect/stop/failover/update path.
2. ARCHITECTURE â€” invariant violations above. Each new outbound/route path: does
   it fail closed? Each new failover/restart path: token lifetime + no resurrection?
3. SECURITY â€” secrets in logs/errors; untrusted subscription YAML/URI parsed
   without bounds; local API bound without auth; helper.cmd / update-extractor
   injection or path traversal.
4. DUPLICATION â€” for each new function >= 20 lines, grep 3-4 distinctive
   identifiers elsewhere; a near-duplicate â‡’ HIGH, fix is "extract to shared helper".
5. UI / LAYOUT (Avalonia changes) â€” overflow on a narrow window; bare-string
   CheckBox/Button Content not wrapped in TextWrapping; async setSize races;
   stale-closure capture in [ObservableProperty] handlers.
6. TEST COVERAGE â€” each new public method: a test that FAILS on empty/null/0?
   Each new visible string: pinned in a characterization test? New failover/
   lifecycle behaviour: does it run in pre-commit Gate 2 scope (not only CI)?
7. LIBRARY MISUSE â€” sing-box 1.13 schema, Avalonia, CommunityToolkit.Mvvm
   (cite the doc if referenced).

Output <= 300 words as a SINGLE JSON array:
[ { "severity": "critical|important|minor", "file": "path:line",
    "issue": "one line", "fix": "concrete change, <= 2 sentences" } ]

DO NOT comment on: formatting, doc completeness, naming preferences (unless
objectively confusing), micro-optimisations, clearly-intentional TODO/FIXME.
I treat critical + important as blocking; minor is opt-in.
```

---

## When to invoke
- BEFORE every commit/ship that touches code (ship-rolling-candidate HARD
  PRECONDITION 2; cut-stable inherits via the candidate's review).
- Skip ONLY for a true hotfix short-circuit (<=5 lines + one surface + no
  contract drift, METHODOLOGY Â§5) â€” and say so in the ship report.

## What to do with findings
- `critical` â€” fix before commit, no exceptions.
- `important` â€” fix before commit, or defer with a code comment citing the
  finding + reason (so the next reviewer sees the precedent).
- `minor` â€” optional, often preference territory.
- Survivors that are real-but-deferred go into `plans/OPEN-DEFECTS.md` so the
  cut-stable gate (`tools/check-open-p0.ps1`) blocks on them.


FINAL REMINDER
This is read-only. Re-open every cited line before returning it. Output only the JSON array, with no Markdown fence and no prose.