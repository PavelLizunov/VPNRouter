# v2.39 — comprehensive regression + bug sweep (2026-06-02)

Pre-cut verification gate for the v2.39.0 cycle. Requested by user:
"по завершению нужна комплексная проверка на регрессию и баги для 2.39".

## Scope

All commits since the `v2.38.2` stable tag, with deep focus on the four
P0/P1 audit fixes implemented this session:

| # | Fix | Severity | Files |
|---|---|---|---|
| 145 | Android: never wipe Servers list on apply-fallback | P0 data-loss | `AndroidApp.FreeConfigs.cs`, `Strings.cs`, `Localization.cs` |
| 146 | Saved recheck must not report success after deep-verify failure | P0 | `FreeConfigFreshness.cs`, `FreeConfigDeepVerifier.cs` |
| 147 | Custom-JSON enforce Apps Include/Exclude + Full Tunnel | P0 leak | `CustomConfigInjector.cs` |
| 148 | Android gate Connect behind deep-verify + Verified target | P1 | `AndroidFreeConfigsOrchestrator.cs`, `AndroidApp.FreeConfigs.cs` |

Earlier in the cycle (already shipped r1-r6): diagnostics export +
secret redaction (r1-r3), pool.json.gz fetch (r4), ExcludeRu on cached
Verified (r5), removed-apps routing scrub (r6).

## Automated gates — PASS

| Gate | Result |
|---|---|
| Core build (Release) | 0 errors |
| Android build (Release, local libbox.aar) | 0 errors |
| Full logic/unit test suite | 1519 passed / 4 skipped / 0 failed (3m11s) |
| HeadlessGui rendering smoke | 8 passed (isolated) |
| VisualDiffTests | 3 FAIL (page-dpi-bypass 26%, page-tools 26%, page-telegram 11%) — PRE-EXISTING stale-baseline drift, NOT a v2.39 regression (see below) |
| CustomConfigInjector suite | 31 passed incl. 3 `sing-box check` integration cases (exclude/split, include/split, include/full) on real x25519 Reality material |
| Diagnostics redactor suite | all green incl. new numeric-secret regression |
| LeakProtection suite | all green |
| AndroidApp source-surface characterization | re-pinned for new `ApplyFcConnectGate`, passes |

`sing-box check` validates the generated custom-config JSON is
schema-valid AND correctly routed across all three apps-policy modes.

## Adversarial review — 3 independent agents

Spawned three read-only reviewers (one per high-risk change + one
cross-cutting). They surfaced **3 real issues, all fixed before cut**:

### FIXED — CRITICAL: custom-JSON DNS leak (full-tunnel / exclude)
#147 flipped `route.final` to the proxy in full-tunnel and exclude mode
(closing the traffic leak), but `StripUnsupportedFeatures` still forced
`dns.final` to the LOCAL resolver, so DNS queries for the tunnelled
traffic leaked to the ISP. `CustomConfigInjector` now mirrors
`ConfigGenerator.BuildDns`: full tunnel OR exclude OR StrictDns =>
`dns.final` = remote/proxy DNS. +3 `dns.final` assertions across the
mode tests so it can never regress silently.

### FIXED — MEDIUM: all-numeric secrets leaked into diagnostics bundle
`DiagnosticsRedactor`'s `_numberLike` fast-path returned values verbatim
for ANY key before the allowlist ran, so an all-digit Reality `short_id`
or numeric password survived into the exported support bundle. Added a
`SecretKeys` set; credential keys now skip the numeric/bool fast-path and
fall through to `***`. Legitimate numeric ports/counts unaffected.
+1 regression test.

### FIXED — HIGH (UX): Android Connect CTA auto-light-up dead path
#148's `OnFcEntryUpgraded` auto-select only fired when nothing was
selected, but `OnFcFound` auto-selects the first Ok candidate — so on the
common "first candidate fails deep verify, later one passes" path the CTA
never auto-enabled. Now selection is promoted off any non-Verified row to
the freshly verified one. (Safety was never affected — gate + backstop
held; this was convenience-only.)

### Verified CORRECT by the reviewers (no change needed)
- #147: route.final survives Strip/EnsureUrltest; include-split empty-list
  is byte-identical to old behaviour; process_name case preserved;
  per-app DNS direction correct.
- #148: persistence is Verified-only end-to-end; UI gate is
  single-source-of-truth; fail-closed Connect backstop; format-string
  arities all match; loop terminates; no infinite selection recursion.
- r4 pool.gz: bounded streaming decompress (decompression-bomb guarded),
  atomic temp+rename, raw fallback, 7 tests.
- r5 ExcludeRu: only over-filters RU (intended direction).
- r1-r3 diagnostics: fail-safe allowlist (unknown keys -> `***`),
  parse-failure omits whole file.

### Deferred (LOW / NIT — not blockers)
- r6 routing scrub `TryRemoveProcessName` is a no-op on macOS/Linux (`.exe`
  contract) and doesn't scrub the inactive Exclude list — correct on
  Windows (primary), weaker cross-platform. Backlog.
- URL userinfo (`user:pass@host`) preserved by `RedactUrlKeepHost` — very
  low likelihood (UI never generates userinfo URLs). Backlog.
- `\bauth\b` log regex misses `Authorization:` headers — JWTs still caught
  by the long-base64 scrubber. Backlog.
- Android cancel-during-deep-verify reports "Sources exhausted" instead of
  "Cancelled" (cosmetic; no weak candidate persisted). Backlog.

## Pre-existing issue found (NOT a v2.39 regression) — stale VisualDiff baselines

`VisualDiffTests` fails on `page-dpi-bypass` (26%), `page-tools` (26%),
`page-telegram` (11%) against the pinned baselines. Confirmed pre-existing:
- No v2.39 commit (`v2.38.2..HEAD`) touches these pages or the baseline PNGs.
- The baselines were last refreshed 2026-05-09 (`7b688f7`), BEFORE the v2.37
  Zapret one-button redesign (r21-r54, late May: Hero cards, ToggleButton
  tab strip) and the v2.38 changes that overhauled exactly these pages.
- VisualDiffTests `OperatingSystem.IsWindows()`-skip on CI's Linux runners,
  so the drift has been invisible — local Windows runs aren't in the ship
  flow. The 10-26% magnitude reflects the whole redesign, not subtle AA.

So this is accumulated baseline tech debt from already-shipped (and
MCP-verified) v2.37/v2.38 designs, surfaced by this sweep — not caused by
any v2.39 change. Flagged for a baseline refresh (desktop-UI; benefits from
a human eyeball of the three pages before re-pinning). Does NOT block r7.

## Runtime verification notes
- #147 custom-config live-connect not run: no custom test config available
  (user confirmed). Covered instead by unit tests + `sing-box check`
  across all modes (proves loadability + routing) — generated-mode
  equivalent is exercised by prior live-tested ship cycles.
- Mandatory post-ship MCP+UIA verify runs after the r7 build is published
  (per rule #1a / #12) — desktop install + launch + diagnostics-export
  redaction spot-check + log scan.

## Ship + post-ship MCP verify (v2.39.0-r7) — PASS

Shipped `v2.39.0-r7` (tag on `682ef9d`). Tag CI green on all platforms:
Windows auto-update integration test, Linux AppImage+.deb, macOS DMG,
Verify Release Integrity all `success`; Android APK skipped (NU1102, built
locally); 14 assets. r6 prerelease deleted, v2.38.2 restored as Latest.

Post-ship MCP verify (mandatory, rule #1a/#12) on the published Windows
binary installed over the prod app dir:
- Phase 2 install+launch: PASS (SHA256 ccb89cc… verified, PID 11392).
- Phase 3 smoke: PASS — main window renders (Not-connected, Config·Mode
  manual·full, Connect), no crash dialog.
- Phase 4 navigation: PASS — Simple -> Advanced -> Settings/Routing ->
  Applications all render crash-free. Routing UI shows Split/Full Tunnel
  (Full selected) + "Russian traffic via real IP"; Applications shows
  "Full-tunnel mode is active. App selection is ignored — all traffic goes
  through VPN" — the desktop messaging matches exactly the routing
  semantics #147 enforces in the generated config.
- Phase 5 log scan: CLEAN (no [ERR]/Exception/FATAL in last 200 lines).
- Core-only / Android-only labels: #147 custom-JSON routing + dns.final and
  #146 deep-verify truthfulness are Core (verified via unit + sing-box
  check); #145 + #148 are Android (verified via clean Release build + the
  3-agent review + source-surface characterization — no device attached for
  a device smoke). Diagnostics numeric-secret redaction verified via unit
  test; the export UI was not exercised end-to-end (needs a numeric-secret
  config to be meaningful).

## Verdict
GREEN. All P0/P1 fixes + the 3 review findings implemented and verified;
full suite green; all platform builds green; 14 assets; post-ship MCP
verify PASS. v2.39.0-r7 shipped as prerelease. Stable cut awaits explicit
user command (rule #6) + the mandatory pre-cut live-update gate.
