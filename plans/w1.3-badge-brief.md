# W1.3 — "True split active" badge + caveats

**Roadmap ref**: `plans/goal-w1.1-implementation-2026-07-04.md` (W1.3) · arch §6.1. **Effort**: ~0.5d. **Risk**: LOW.

## Why
Give exclude-mode users honest feedback that the OS-level split is live, with the bind-redirect caveats on hover — not a new page, one status-zone badge (goal Scope: "caveats на виду").

## What (lean — no new page, semantic tokens per App design rules)
- `VpnEngine`: `event Action<bool>? TrueSplitEngagedChanged` (forwarded from `_splitDriver.EngagedChanged` in the ctor) + `bool IsTrueSplitEngaged`.
- `MainWindowViewModel`: `[ObservableProperty] bool IsTrueSplitActive` + subscribe `TrueSplitEngagedChanged` → `Dispatcher.UIThread.Post(() => IsTrueSplitActive = engaged)`.
- `Strings.cs` + `L_` getters: `TrueSplitBadge` ("True split: активен" / "active") + `TrueSplitTooltip` (4 caveats: svchost-DNS, excluded localhost-UDP, multicast-receive, UWP — bilingual).
- `MainWindow.axaml`: a `SuccessBg`/`RadiusPill` badge in the status StackPanel, `IsVisible="{Binding IsTrueSplitActive}"`, `ToolTip.Tip="{Binding L_TrueSplitTooltip}"`.
- Characterization hash: Windows bumped (`a8d490eb…`); Linux drifts in lock-step (cross-platform members) → capture from CI first-failure.

## Verification
- [x] Gate 1 build 0 err · [x] Gate 2: characterization (Win) + HeadlessGuiTests + wiring = 12/12 green · [x] Gate 3 docs (this + CLAUDE.md VpnEngine row) · [-] Gate 4: additive UI seam, no security surface (VpnEngine forward reviewed inline) · [ ] Gate 5 **MCP e2e** (badge appears on exclude-mode engage; tooltip shows caveats) — live follow-up · [-] Gate 6 N/A (characterization hash IS the surface pin).

## Outcome
Code done + unit-verified; MCP e2e (badge visible on live engage) is the remaining acceptance. Linux hash bump follows CI first-failure.
