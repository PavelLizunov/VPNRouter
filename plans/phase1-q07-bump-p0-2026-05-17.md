# Phase 1 — Q7: Bump 5 P0 safe-patch packages

**Owner**: Claude session-id (Wave 1)
**Roadmap ref**: plans/v3.0-refactor-roadmap.md Phase 1 #7, plans/nuget-audit-2026-05-17.md "5 P0 safe-patch bumps"
**Effort**: 30 minutes
**Risk**: LOW (patch-level bumps only — same major + minor, just bugfix releases)

## Why
Audit C identified 5 packages as P0 — safe-patch bumps that close minor maintenance gaps without breaking API. Bumping now keeps us on supported versions and reduces lag for the Phase 3 modernization sweep.

## What
The exact 5 P0 packages need to be read from the audit report `plans/nuget-audit-2026-05-17.md`. The audit grouped packages into P0 (5 safe), P1 (5 coordinated v3.0 sweep), P2 (2 milestones), P3 (2 defer).

Agent must:
1. Open `plans/nuget-audit-2026-05-17.md`
2. Find the P0 section
3. For each of the 5 P0 packages, identify which `.csproj` file references it + what version to bump to
4. Apply the bumps via Edit tool (preserve XML formatting)

Likely candidates (verify in audit):
- Serilog sink versions
- Spectre.Console patch
- xUnit / Microsoft.NET.Test.Sdk patch
- Microsoft.Extensions.* patch
- (one more from audit)

**DO NOT bump anything labeled P1 or higher** — those need coordinated v3.0 work.

## Verification gate
- [ ] **Pre-check**: re-read audit P0 section, list the 5 with target versions
- [ ] **Gate 1 — Build clean**: `dotnet build VPNRouter.sln -c Release` → 0 errors
- [ ] **Gate 2 — Tests green**: `dotnet test` → all 765 pass
- [ ] **Sanity**: `dotnet list package --outdated` no longer lists the 5 P0 packages
- [ ] **Hook gates**: pre-commit + commit-msg both green

## Outcome

**Run by**: Phase 1 Q7 agent, 2026-05-17.

### P0 audit list (5 items)

Per `plans/nuget-audit-2026-05-17.md` "Recommendations → P0":

1. CommunityToolkit.Mvvm → 8.4.2 in App + Android (drift fix)
2. Avalonia.Diagnostics → 11.3.15 in App
3. Newtonsoft.Json → 13.0.4 (Core, CLI, Service, Android)
4. Microsoft.Diagnostics.Tracing.TraceEvent → 3.2.2 (Core, Win-only)
5. ZXing.Net → 0.16.11 in Android

### Bumps applied (4 of 5)

| # | Package | From | To | csproj(s) |
|---|---|---|---|---|
| 1 | CommunityToolkit.Mvvm | 8.2.1 (App), 8.4.0 (Android) | 8.4.2 | `VPNRouter.App/VPNRouter.App.csproj`, `VPNRouter.Android/VPNRouter.Android.csproj` |
| 3 | Newtonsoft.Json | 13.0.3 | 13.0.4 | `VPNRouter.Core/VPNRouter.Core.csproj`, `VPNRouter.CLI/VPNRouter.CLI.csproj`, `VPNRouter.Service/VPNRouter.Service.csproj`, `VPNRouter.Android/VPNRouter.Android.csproj` |
| 4 | Microsoft.Diagnostics.Tracing.TraceEvent | 3.1.7 | 3.2.2 | `VPNRouter.Core/VPNRouter.Core.csproj` (Win-only ItemGroup) |
| 5 | ZXing.Net | 0.16.10 | 0.16.11 | `VPNRouter.Android/VPNRouter.Android.csproj` |

### Bumps reverted (1 of 5)

| # | Package | Reason |
|---|---|---|
| 2 | Avalonia.Diagnostics 11.3.12 → 11.3.15 | `NU1605` package-downgrade error. Avalonia.Diagnostics 11.3.15 demands `Avalonia >= 11.3.15` transitively, but App + Android + Tests pin Avalonia 11.3.12 explicitly. Restore fails. The audit narrative implies bumping the whole Avalonia 11.3.12 → 11.3.15 family together, but only `Avalonia.Diagnostics` is in the explicit P0 list — bumping the rest of Avalonia.* would exceed the strict P0 scope (audit didn't enumerate Avalonia / Avalonia.Desktop / Avalonia.Themes.Fluent / Avalonia.Fonts.Inter / Avalonia.Android / Avalonia.Headless / Avalonia.Headless.XUnit individually under P0). Per task constraint "If a P0 bump triggers test failures or compile errors, REVERT that single bump and continue with the rest" — Avalonia.Diagnostics left at 11.3.12. Recommend a separate ticket to coordinate the full Avalonia 11.3.x patch bump. |

### Verification

- `dotnet restore VPNRouter.sln`: success, 0 errors after Avalonia.Diagnostics revert.
- `dotnet build VPNRouter.sln -c Release`: **Build succeeded. 0 Warning(s), 0 Error(s). 1.40s.**
- `dotnet test VPNRouter.Tests/VPNRouter.Tests.csproj -c Release --filter "FullyQualifiedName!~Headless&FullyQualifiedName!~PageScreenshot&FullyQualifiedName!~VisualDiff" --no-build`:
  - Total: 842, Passed: 832-834, Skipped: 3, Failed: 5-7 (count varies across runs due to concurrent worktree file-lock noise).
  - **All failures are pre-existing, unrelated to P0 bumps**:
    - `VlessUriParserTests.{TryParse_ValidUri_ReturnsEntry, Parse_RealityUri_ExtractsAllFields, Parse_RealityUri_ExtractsTransport, Parse_RealityUri_ExtractsRealityConfig}` — broken by commit `d041ec8` (v2.32.3 placeholder-exorcism, 2026-05-16). Tests still use placeholder reality.public_key which now throws `PlaceholderConfigException`. Pre-existing main-branch debt.
    - `AppAutostartTgProxyTests.Bootstrap_IsInvokedFromConstructor` — looks for `BootstrapAutostartAsync` literal in source; pre-existing.
    - `MainWindowViewModelAppsModeTests.OnRoutingAppsModeChanged_FiresIsCheckedNotifications` (intermittent) — `config.yaml` file lock from concurrent worktree tests. Environmental, not a regression.
  - None of the failures trace through Newtonsoft.Json, CommunityToolkit.Mvvm, ZXing.Net, or TraceEvent code paths.
- `dotnet list package --outdated`:
  - Newtonsoft.Json, CommunityToolkit.Mvvm, Microsoft.Diagnostics.Tracing.TraceEvent, ZXing.Net **no longer listed** — confirmed at-latest.
  - Avalonia.Diagnostics still listed (expected — reverted, see above).
  - Outdated package-reference entries reduced from ~47 → 39 (8 entries cleared = 4 P0 packages × number of csprojs each references).

### Files staged

`VPNRouter.App/VPNRouter.App.csproj`, `VPNRouter.Core/VPNRouter.Core.csproj`, `VPNRouter.CLI/VPNRouter.CLI.csproj`, `VPNRouter.Service/VPNRouter.Service.csproj`, `VPNRouter.Android/VPNRouter.Android.csproj`. **Not committed** per task instruction.
