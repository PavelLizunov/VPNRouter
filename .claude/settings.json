{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"$ts = Get-Date -Format 'yyyy-MM-dd HH:mm:ss'; $logFile = 'C:\\Project\\VPNRouter\\.claude\\phase-logs\\tool-trail-' + (Get-Date -Format 'yyyy-MM-dd') + '.log'; $logDir = Split-Path $logFile -Parent; if (-not (Test-Path $logDir)) { New-Item -ItemType Directory -Path $logDir -Force | Out-Null }; $env:CLAUDE_TOOL_NAME + ' ' + $env:CLAUDE_TOOL_INPUT_file_path | Out-File -FilePath $logFile -Append -Encoding utf8\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"$ts = Get-Date -Format 'yyyy-MM-dd HH:mm:ss'; $logFile = 'C:\\Project\\VPNRouter\\.claude\\phase-logs\\tool-trail-' + (Get-Date -Format 'yyyy-MM-dd') + '.log'; $logDir = Split-Path $logFile -Parent; if (-not (Test-Path $logDir)) { New-Item -ItemType Directory -Path $logDir -Force | Out-Null }; \\\"$ts BASH: $env:CLAUDE_TOOL_INPUT_command\\\" | Out-File -FilePath $logFile -Append -Encoding utf8\""
          }
        ]
      }
    ]
  }
}
