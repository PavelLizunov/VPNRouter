<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:VPNRouter.App.ViewModels"
             xmlns:pages="using:VPNRouter.App.Views.Pages"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="520" d:DesignHeight="500"
             x:Class="VPNRouter.App.Views.Pages.ToolsPage"
             x:CompileBindings="True"
             x:DataType="vm:MainWindowViewModel">

    <Grid RowDefinitions="Auto,*">

        <!-- Sub-tab strip -->
        <ListBox Grid.Row="0"
                 SelectedIndex="{Binding SelectedToolIndex}"
                 Background="Transparent"
                 BorderThickness="0,0,0,1"
                 BorderBrush="{DynamicResource BorderDefaultBrush}"
                 Padding="6,2">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="4"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBoxItem Content="{Binding LblToolZapret}"  Padding="10,4" FontSize="11"/>
            <ListBoxItem Content="{Binding LblToolTgProxy}" Padding="10,4" FontSize="11"/>
            <!-- v2.32.2 (W-4) — third Tools sub-tab for the wgturn-core
                 Emergency Channel card. Title reuses the same Strings.cs
                 key the card title uses so the tab caption + card title
                 stay in sync. -->
            <ListBoxItem Content="{Binding L_LblToolEmergencyChannel}" Padding="10,4" FontSize="11"/>
        </ListBox>

        <!-- Sub-pages -->
        <Grid Grid.Row="1">
            <pages:DpiBypassPage IsVisible="{Binding IsZapretToolSelected}"/>
            <pages:TelegramPage  IsVisible="{Binding IsTgProxyToolSelected}"/>
            <!-- v2.32.2 (W-4) — Emergency Channel (wgturn) sub-page. -->
            <pages:EmergencyChannelPage IsVisible="{Binding IsEmergencyChannelToolSelected}"/>
        </Grid>

    </Grid>
</UserControl>
